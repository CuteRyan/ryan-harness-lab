# 체크리스트: /feedback 스킬 인코딩 3 레이어 패치

approved: true

근거: `docs/research/feedback-encoding-fix/03_fix-plan.md` (Step A + B + D3)
확정 결정: D1=BOM 유지, D2=Gemini argv 우회 미룸, D3=한글 경로 점검 포함

## 작업 항목

### Step A — L1 + L3 최소 패치
- [x] A-1. `skills/feedback/scripts/_encoding.ps1` 신규 작성 (UTF-8 BOM 포함 저장 필수 — PS 5.1 한글 파서 이슈 회피)
- [x] A-2a. `run-claude-sub.ps1` L33-37 인라인 인코딩 설정 → `. (Join-Path $PSScriptRoot '_encoding.ps1')` 치환
- [x] A-2b. `run-codex.ps1` 상단에 dot-source 1줄 추가
- [x] A-2c. `run-gemini.ps1` 상단에 dot-source 1줄 추가
- [x] A-3. `orchestrate.ps1` Start-Job ScriptBlock 첫 줄에 dot-source 추가 + `$ArgumentList`에 `$scriptDir` 추가 + 부모 세션에도 dot-source 적용

### Step B — 진단 보강
- [x] B-2. `orchestrate.ps1` Step 1 이후 `Write-Verbose` 1줄 (부모 세션 인코딩 상태 기록). V-1 실측 출력 `Parent encoding: Console=utf-8 Output=utf-8` 확인

### D3 — 한글 경로 안전성 점검 (코드 수정 아닌 조사)
- [x] D3-1. 격리 디렉토리는 `$HOME\codex-cwd\<slug>` = 영문 경로 (CLAUDE.md 표준 패턴이 이미 방어). 대상 파일명이 한글이면 argv로 전달되지만 Step A+B로 커버
- [x] D3-2. V-1 실측에서 Codex/Claude Sub/Gemini 모두 한글 파일명 정상 전달 확인
- [x] D3-3. 이슈 없음. 신규 레이어 L4(**CLI가 내부에서 spawn하는 PS 서브프로세스의 파일 읽기**)는 Codex에서 관찰됐으나 Codex 자체가 스스로 회복. 별도 이월 항목으로 03_fix-plan.md 후속 보강 예정

### 동기화 및 검증
- [x] 스테이징(`skills/feedback/scripts/`) → 운영(`~/.claude/skills/feedback/scripts/`) 복사 (SHA256 8/8 MATCH, BOM 7/7 유지)
- [x] V-1 스모크 (352초): 3 CLI 모두 파일 생성, **한글 정상 + mojibake 0건** ✅. Validation 1/3(Claude Sub VALID, Codex/Gemini는 태깅 포맷 누락 — Day 10 이월 ④와 동일 맥락, 인코딩 패치 범위 밖)
- [x] V-3 병렬 스모크 완료 (17분): **mojibake 3회 × 3 CLI = 0건 ✅** (인코딩 패치 목표 달성). Claude Sub 3회 모두 성공(한글 900/541/161자). **Codex/Gemini는 V-3에서 3회 연속 async timeout + sync retry 실패** — V-1 단발성은 성공이었으므로 인코딩 regression 아님. CLI 연속 호출 시 stateful 누적(Codex stdin, Gemini IDE client)이 원인으로 추정 → **이월 ⑤ 신규 등록**

### 기록
- [ ] `docs/history/2026-04-23.md`에 Day 10 Part 3 섹션 추가
- [ ] 이월 ⑤ — CLI 연속 호출 stateful 누적 (Codex stdin, Gemini IDE client) 등록

## 수정 대상 파일

### 신규
- `skills/feedback/scripts/_encoding.ps1` — UTF-8 I/O + `$PSDefaultParameterValues` 고정 헬퍼. **UTF-8 BOM 포함 저장 필수**

### 수정
- `skills/feedback/scripts/run-claude-sub.ps1` — L33-37 인라인 설정 → dot-source
- `skills/feedback/scripts/run-codex.ps1` — $ErrorActionPreference 직후 dot-source
- `skills/feedback/scripts/run-gemini.ps1` — $ErrorActionPreference 직후 dot-source
- `skills/feedback/scripts/orchestrate.ps1` — Start-Job ScriptBlock 첫 줄 dot-source + argv 1개 추가 + Step 1 이후 Write-Verbose 1줄

### 운영 동기화
- `C:\Users\rlgns\.claude\skills\feedback\scripts\` 전체 위 파일들 덮어쓰기 (6개 ps1 전원, BOM 유지)

### 문서 보강
- `docs/history/2026-04-23.md`

## 검증 항목

- [ ] PowerShell 구문 체크 (파싱 에러 없음) — 각 ps1 `powershell -Command "& { . <path>; }"` 또는 `Get-Command -Syntax`
- [ ] 모든 수정된 ps1이 UTF-8 BOM 포함인지 재확인 (`Get-Content -Raw -Encoding Byte`의 첫 3바이트 `EF BB BF`)
- [ ] V-1 스모크 통과 (3 CLI VALID + 한글 정상)
- [ ] V-3 병렬 3회 통과
- [ ] 기존 /feedback 동작 회귀 없음 (스테이징/운영 SSOT drift 없음 = diff 0)
- [ ] `.checklist.md` 외 의도치 않은 파일 수정 없음 (`git status` 확인)

## 더블 체크

- [ ] 체크리스트에 빠진 항목 없는가? (Step A 5개, B 1개, D3 3개, 동기화 1개, 검증 2개, 기록 1개 = 13 구현 항목)
- [ ] 실제 반영 안 된 것은 없는가? — 구현 완료 시 각 파일 Read 후 대조
- [ ] 다른 코드/문서와 일관성 유지되는가? — `SKILL.md`의 스크립트 파일 목록(L128-135)에 `_encoding.ps1` 추가 필요 여부 판단
- [ ] 스테이징/운영 drift 없는가? — 운영 복사 후 diff 0 확인
- [ ] `03_fix-plan.md`의 체크리스트 초안 11항과 매핑되는가?
