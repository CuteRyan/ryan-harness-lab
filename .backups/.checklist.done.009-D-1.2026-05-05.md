# 체크리스트: #009-D-1 scripts/ 6 PowerShell 헬퍼 신설 (preflight·resolve-preset·run-team·monitor-team·validate-team·shutdown-team)

---
approved: true
status: approved
mode: code
작성일: 2026-05-05
승인일: 2026-05-05 (사용자 명시 "진행하자 이어서" = #009-C 직후 묵시적 승인 + Day 20 turn 3 즉시 진입)
근거: `.todo.md` #009-D (Day 20 turn 2 #009-C 完 직후) + 사용자 명시 "이어서 진행" + Step C SKILL.md v2 §6 명령어 (`/agent-team run --preset <이름>`) 가 호출하는 자동화 헬퍼 미작성 = 본 turn 산출물. v2 spec `04_redesign-spec.md §1·§2·§3` (단독 구현 금지된 v2 spec 의 scripts 명세 부분만 차용, preset YAML 파싱은 본 비전 Day 20 turn 1 양식 기준 — D-17 정합).
---

## 배경 / 목적

#009 3-step 본 사이클 (A·B·C) 全 PASS 후 잔여 #009-D 中 **part 1 = scripts/ 6**. SKILL.md v2 §6 명령어 (`/agent-team run --preset`) 가 자동 호출하는 PowerShell 헬퍼 6개 신설. **결정론은 스크립트, 판단·종합은 LLM** 원칙 (feedback 차용, v2 spec §2). LLM 의 역할 = TeamCreate/TaskCreate/Agent spawn/SendMessage primitive 호출 + 종합 보고서 작성. 스크립트의 역할 = 환경 검증 / preset 파싱 / sentinel 기록 / 모니터링 / 검증 / 종료.

**part 2 (#009-D-2)**: reference/ 4 (patterns·anti-patterns·errors·presets) + SKILL.md v2 → v2.5 (scripts/reference 호출 박기) — 별도 turn.
**#009-E**: feature·security 2 preset (마스터플랜 §2.4 中 보류 2/7) — 멤버가 Step A 12 agent 에 없음 (lead/frontend/backend/tester + SAST/DAST/compliance) → 새 agent 7 신설 선행 필요. 별도 turn.

## 작업 전 기준선 (git status --short)

```
(clean — Day 20 turn 2 commit 37b20fb push 완료 후 working tree clean)
skills/agent-team-manager/SKILL.md (v2 355줄, turn 2 신설) + presets/*.yaml 5 (Day 20 turn 1) + agents/*.md 12 (turn 11) = 全 입력 자료 존재
skills/agent-team-manager/scripts/ 디렉토리 부재 (본 turn 신설)
```

## 작업 항목

### Step 1: scripts/ 디렉토리 + 공통 헬퍼 양식 확정
- [ ] 1-1. `skills/agent-team-manager/scripts/` 스테이징 디렉토리 신설
- [ ] 1-2. 6 PowerShell 스크립트 공통 양식 확정:
  - 헤더 주석 (Name + Why + Inputs + Outputs + Exit codes + Source 출처)
  - PowerShell 5.1 호환 (`$ErrorActionPreference = 'Stop'` + try/catch + JSON stdout)
  - 한글 인코딩 = UTF-8 BOM 또는 출력 시 명시 (`$OutputEncoding = [Text.UTF8Encoding]::new($false)`)
  - 종속성: `pyyaml` 모듈 부재 시 fallback 안내 (Day 20 turn 1 검증 PASS — pyyaml installed)
- [ ] 1-3. 본 비전 preset YAML 양식 (Day 20 turn 1) 매핑 확정:
  - `name` → team name 후보
  - `members[].name` / `members[].model` / `members[].sequence` / `members[].blocked_by` → spawn 인자
  - `task_template.subject` / `description` / `output_format_required` → SendMessage 페이로드
  - `protocol.steps` / `review_cycle_cap` → SKILL.md Phase 흐름

### Step 2: preflight.ps1 — 환경·버전·subagent·tmux 체크
- [ ] 2-1. `scripts/preflight.ps1` 신설 (~80~120줄)
- [ ] 2-2. 4 검사 항목:
  - **env 검사**: `CLAUDE_CODE_SUBAGENT_MODEL` 부재 확인 (fallback C+ 정합, turn 8 #019 PASS)
  - **version 검사**: `claude-code` CLI 버전 (≥ 2.1.126, turn 7 강제 훅 정합, version 부재 시 warn)
  - **subagent 컨텍스트**: 메인 Claude (사장) 인지 확인 (`$env:CLAUDE_CODE_SUBAGENT == ''` 또는 부재)
  - **PowerShell 버전**: 5.1 또는 7+ (글로벌 룰 정합)
- [ ] 2-3. JSON stdout 양식: `{"ok": true|false, "checks": [...], "abort_reason": null|"..."}`
- [ ] 2-4. Exit codes: 0=OK / 1=실패 (메인 Claude 가 stdout 파싱 후 abort 결정)

### Step 3: resolve-preset.ps1 — preset YAML → JSON 메타 변환
- [ ] 3-1. `scripts/resolve-preset.ps1` 신설 (~150~200줄, 가장 복잡)
- [ ] 3-2. 인자: `-Preset <name>` (필수) | `-List` | `-ValidateOnly -Path <file>`
- [ ] 3-3. preset 검색 경로 (스테이징 우선):
  - `$ProjectRoot\presets\<name>.yaml` (스테이징)
  - `$HOME\.claude\presets\<name>.yaml` (운영, fallback)
- [ ] 3-4. YAML 파싱 = Python 호출 (`python -c "import yaml; ..."`) 또는 PowerShell-Yaml 모듈
- [ ] 3-5. 본 비전 preset 양식 → JSON 메타 변환:
  ```json
  {
    "preset": "review",
    "team_name_template": "review-{slug}-{timestamp}",
    "members": [
      {"name": "security-reviewer", "model": "sonnet", "subagent_type": "security-reviewer",
       "sequence": 1, "blocked_by": [], "dimension": "보안", "focus_areas": [...]}
    ],
    "task_graph": [
      {"id": "1", "owner": "security-reviewer", "subject_template": "...", "blocked_by": []}
    ],
    "task_template": {...},
    "protocol_steps": [...],
    "review_cycle_cap": 3
  }
  ```
- [ ] 3-6. `-List` 옵션: 5 preset 카탈로그 표 출력 (이름·team_size·members 명단)
- [ ] 3-7. `-ValidateOnly`: 양식 검증 (Step B 검증 항목 차용 — members.name 필드 존재 + model in {opus,sonnet,haiku} + protocol.steps=4 + review_cycle_cap=3 + output_format_required len=4)

### Step 4: run-team.ps1 — sentinel 등록 (start_time + deadline)
- [ ] 4-1. `scripts/run-team.ps1` 신설 (~80~120줄)
- [ ] 4-2. 인자: `-Team <name>` (필수) | `-SentinelInit` | `-TimeoutMinutes 30`
- [ ] 4-3. sentinel 파일: `~/.claude/tasks/<team>/.sentinel.json`
  ```json
  {"team": "...", "start_time": "ISO8601", "deadline": "ISO8601",
   "members": [...], "preset": "...", "review_cycle_count": 0}
  ```
- [ ] 4-4. **스크립트는 tool 호출 안 함** (v2 spec §2 경계선 원칙). TeamCreate/TaskCreate 는 LLM (메인 Claude) 책임.

### Step 5: monitor-team.ps1 — 주기 상태 덤프
- [ ] 5-1. `scripts/monitor-team.ps1` 신설 (~100~150줄)
- [ ] 5-2. 인자: `-Team <name>` (필수) | `-Format table|json`
- [ ] 5-3. `~/.claude/tasks/<team>/` 디렉토리 스캔:
  - 각 task 의 status (pending/in_progress/completed/blocked)
  - 각 멤버의 inbox/outbox 메시지 수
  - sentinel.json 의 deadline 잔여 시간
- [ ] 5-4. 출력 양식 = 표 (사람 읽기) + JSON (LLM 수용)
- [ ] 5-5. Exit codes: 0=정상 / 1=좀비 (deadline 초과) / 2=고아 (sentinel.json 부재)

### Step 6: validate-team.ps1 — 고아·좀비·데드라인·중복 owner 검증
- [ ] 6-1. `scripts/validate-team.ps1` 신설 (~120~180줄)
- [ ] 6-2. 인자: `-Team <name>` (필수) | `-AllTeams` | `-Format json|table`
- [ ] 6-3. 4 검증 항목 (v2 spec §1 인용):
  - **고아 디렉토리**: `~/.claude/teams/<team>/` 존재하지만 sentinel.json 부재
  - **미완료 task**: deadline 초과 + status != completed
  - **데드라인 초과**: sentinel.deadline < now
  - **중복 owner**: 동일 task 의 owner 중복 (preset 양식 위반)
  - **review_cycle_cap 초과**: review_cycle_count > 3 (사용자 에스컬레이션 신호)
- [ ] 6-4. JSON 출력: `{"valid": true|false, "issues": [...], "recommendations": [...]}`
- [ ] 6-5. Exit codes: 0=valid / 1=issue 발견 (LLM이 종합 보고서 작성 시 issues 반영)

### Step 7: shutdown-team.ps1 — 좀비 정리 + orphan 청소
- [ ] 7-1. `scripts/shutdown-team.ps1` 신설 (~80~120줄)
- [ ] 7-2. 인자: `-Team <name>` (필수) | `-Force` (사용자 컨펌 우회, R-5 위반 경고)
- [ ] 7-3. **삭제 전 사용자 컨펌 의무** (R-5, feedback `.dev-checklist.md` 삭제 금지 원칙 차용):
  - 기본 = `~/.claude/teams/<team>/.archived_<timestamp>/` 로 이동 (삭제 X)
  - `-Force` 옵션 = 즉시 삭제 (사용자 명시 시만)
- [ ] 7-4. 좀비 teammate 종료 신호: `~/.claude/tasks/<team>/.shutdown_signal` 파일 생성 (각 teammate 가 SendMessage poll 시 감지)

### Step 8: 단위 테스트 (각 스크립트 1~2 케이스)
- [ ] 8-1. `preflight.ps1` 호출 → JSON stdout valid 확인
- [ ] 8-2. `resolve-preset.ps1 -List` → 5 preset 표 출력 확인
- [ ] 8-3. `resolve-preset.ps1 -Preset review` → JSON 메타 출력 + members[3] 정합 확인
- [ ] 8-4. `resolve-preset.ps1 -ValidateOnly -Path presets/debug.yaml` → valid 확인
- [ ] 8-5. `validate-team.ps1 -AllTeams` → 현재 활성 팀 0건 정상 출력 확인 (또는 잔존 팀 issues)

### Step 9: 운영 sync 6 PowerShell + SHA256 MATCH
- [ ] 9-1. `skills/agent-team-manager/scripts/*.ps1` → `~/.claude/skills/agent-team-manager/scripts/*.ps1` 단방향 sync (D-11 정책)
- [ ] 9-2. SHA256 MATCH 6쌍 검증

### Step 10: history + .todo + commit + .checklist archive
- [ ] 10-1. `docs/history/2026-05-05.md` §12 신설 (~50줄 예상) — Day 20 turn 3 #009-D-1 PASS 요약
- [ ] 10-2. `docs/history/index.md` Day 20 행에 turn 3 결과 추가
- [ ] 10-3. `.todo.md` #009-D 갱신 (Step D-1 完, D-2 + #009-E 잔여 표기)
- [ ] 10-4. **commit + push** = `feat+docs: Day 20 turn 3 — #009-D-1 PASS (scripts/ 6 PowerShell 헬퍼 신설, preset YAML 파싱 자동화)`
- [ ] 10-5. `.checklist.md` → `.backups/.checklist.done.009-D-1.2026-05-05.md` archive (git add -f)

## 수정 대상 파일

### 신설 (1 디렉토리 + 6 파일)
- `skills/agent-team-manager/scripts/` (디렉토리 신설)
- `skills/agent-team-manager/scripts/preflight.ps1`
- `skills/agent-team-manager/scripts/resolve-preset.ps1`
- `skills/agent-team-manager/scripts/run-team.ps1`
- `skills/agent-team-manager/scripts/monitor-team.ps1`
- `skills/agent-team-manager/scripts/validate-team.ps1`
- `skills/agent-team-manager/scripts/shutdown-team.ps1`

### 운영 sync (6쌍, git 외부)
- `~/.claude/skills/agent-team-manager/scripts/*.ps1` × 6

### 기존 수정 (3)
- `docs/history/2026-05-05.md` — §12 신설
- `docs/history/index.md` — Day 20 행 turn 3 결과 추가
- `.todo.md` — #009-D 갱신

## 검증 항목

- [ ] **mode: code 필수 검증** (PowerShell 스크립트):
  - [ ] 6 스크립트 모두 `$ErrorActionPreference = 'Stop'` + try/catch 구조
  - [ ] JSON stdout 양식 valid (PowerShell `ConvertFrom-Json` 파싱 성공)
  - [ ] Exit codes 명시 (0=정상 / 1·2=실패)
  - [ ] **스크립트가 claude-code tool 호출하지 않음** (v2 spec §2 경계선 원칙)
  - [ ] PowerShell 5.1 + 7+ 양쪽 호환 (한글 출력 한 번 검증)
- [ ] **본 비전 preset YAML 양식 정합 검증**:
  - [ ] `resolve-preset.ps1 -Preset review` → members[3] (security/performance/correctness-reviewer) JSON 추출 정확
  - [ ] `members[].sequence` + `members[].blocked_by` 필드 그대로 mapping
  - [ ] `task_template.output_format_required` 4 요소 보존
- [ ] **운영 sync 검증**: 6 스크립트 SHA256 MATCH 6쌍
- [ ] **작업 후 `git status --short` / `git diff --stat`**: 변경 = 6 신설 (scripts/) + history 2건 + .todo 1건 = **9 파일** + .checklist archive 1
- [ ] **사이드 이펙트 검증**: SKILL.md v2 (turn 2) + presets/*.yaml 5 (turn 1) + agents/*.md 12 (turn 11) 무영향 확인 (본 turn = scripts 만 신설)
- [ ] **단위 테스트 출력 응답 인용** (Step 8 결과)

## 롤백 계획 (실패 시 복구 방법)

- **실패 판정 기준**:
  - PowerShell 스크립트 문법 오류 (parser 실패)
  - JSON stdout 양식 invalid
  - resolve-preset 의 본 비전 양식 파싱 실패 (Day 20 turn 1 양식 미정합)
  - 운영 sync 後 SHA256 MISMATCH
  - 단위 테스트 PASS rate < 80%
- **복구 절차**:
  - `git restore docs/history/2026-05-05.md docs/history/index.md .todo.md`
  - 신설 6 스크립트 삭제 = `Remove-Item skills\agent-team-manager\scripts\*.ps1`
  - 운영 6 스크립트 삭제 = `Remove-Item ~/.claude/skills/agent-team-manager/scripts/*.ps1`
  - 운영 디렉토리 신설했다면 삭제 = `Remove-Item ~/.claude/skills/agent-team-manager/scripts/ -Recurse`
  - `.checklist.md` → `.backups/.checklist.md.실패_009-D-1_2026-05-05.md` 이동
- **부분 실패 시**: 6 스크립트 中 일부만 PASS 시 부분 결과 commit + 나머지 별도 turn (`.todo.md` #009-D 갱신, Step D-1 진행 갱신)
- **보존 정책**: 체크리스트 삭제 금지, `.backups/` 이동만

## 더블 체크

- [ ] 체크리스트에 빠진 항목 없는가?
  - 6 스크립트 + 단위 테스트 + 운영 sync + 본 비전 양식 정합 + history + .todo + commit 全 망라
- [ ] 실제 반영 안 된 것은 없는가?
  - 각 스크립트 신설 후 단위 테스트 응답 인용
  - resolve-preset.ps1 -List 출력 인용 (5 preset 카탈로그 표)
  - resolve-preset.ps1 -Preset review 의 JSON members 인용 (3명 정합)
  - 운영 sync SHA256 MATCH 6쌍 응답 인용
- [ ] 다른 코드/문서와 일관성 유지되는가?
  - SKILL.md v2 §6 명령어 (`/agent-team run --preset`) → scripts/ 호출 흐름 정합
  - presets/*.yaml 5 (Day 20 turn 1) 양식 그대로 파싱 (D-17 정합)
  - agents/*.md 12 (turn 11) frontmatter `model:` + `name:` 그대로 활용
  - 글로벌 강제 훅 (turn 7 #018) + fallback C+ (turn 8 #019) 정합
  - 글로벌 `rules/agent-spawn-model.md` (model 명시) 정합
  - 메모리 `feedback_no_haiku.md` (Haiku 0건) — preset 검증에서 Haiku model fail 처리

## 승인 규칙

- 본 체크리스트 = 사용자 명시 "진행하자 이어서" = 묵시적 승인 + Day 20 turn 3 즉시 진입.
- **치명/높음 항목 변경 (재승인 필요)**:
  - PowerShell 5.1 호환 실패 (UTF-8 BOM·CP949 인코딩 회귀, Day 10 이월 ⑤ 재발) → 재승인
  - resolve-preset.ps1 의 본 비전 양식 파싱 실패 → 재승인 (양식 재설계 필요 = 범위 초과)
  - 운영 sync 後 SHA256 MISMATCH → 재승인 (sync 메커니즘 점검)
  - feature·security 2 preset 본 turn 진입 시도 → 재승인 (#009-E 분리 정책 위반)

## 작업 시간 추정

- Step 1 (scripts/ 디렉토리 + 양식 확정) = 5분
- Step 2 (preflight.ps1) = 10분
- Step 3 (resolve-preset.ps1, 가장 복잡) = 20분
- Step 4·5·6·7 (run/monitor/validate/shutdown 각 7~10분) = 30~40분
- Step 8 (단위 테스트) = 10분
- Step 9 (운영 sync + SHA256) = 5분
- Step 10 (history + .todo + commit + archive) = 10분
- **Total ≈ 90~100분** (1 turn 가능, 단 분량 大)
