# 체크리스트: #009-B presets/ 5 YAML 신설 (review·debug·research·docs-research·harness-design, Step A 12 agent 호출 박음 + 4-step 프로토콜 + review cycle cap 3)

---
approved: true
status: approved
승인일: 2026-05-05
mode: mixed
작성일: 2026-05-04
근거: `.todo.md` #009 Step B (turn 11 Step A 完 직후) + 사용자 명시 "오늘 안 마무리" + #009-A turn 11 산출물 (`agents/*.md` 12 신설) 호출 박는 자산 = preset YAML. 마스터플랜 §2.4 ② 회의실 preset 표 + §10.2 v2 P0 (O1 4-step 프로토콜) + §9.1 (review cycle cap 3) + wshobson `plugins/agent-teams/skills/team-composition-patterns/references/preset-teams.md` (HEAD `ece811f2`) preset 양식 (Configuration + Members + Task Template + Variations) 차용.
---

## 배경 / 목적

#009 3-step 분할 中 **Step B**. Step A (12 agent) 와 Step C (SKILL.md v2) 사이 위치 = preset YAML 이 12 agent 를 어떻게 조합·호출하는지 명세하는 자산. **agent-team-manager SKILL.md (Step C)** 가 본 preset YAML 을 `resolve-preset.ps1` 헬퍼 (별도 turn 신설 예정) 로 읽어 멤버 직책 spawn. 본 turn 산출물 = `presets/*.yaml` 5건 + 운영 sync = `~/.claude/presets/*.yaml` 5건 + 마스터플랜 §2.4 정합 검증.

## 작업 전 기준선 (git status --short)

```
(clean — turn 11 commit 7cf701d push 완료 후 working tree clean)
presets/ 디렉토리 부재 (본 turn 신설)
```

## 작업 항목

### Step 1: presets/ 디렉토리 + YAML 양식 확정
- [ ] 1-1. `presets/` 스테이징 디렉토리 신설
- [ ] 1-2. 5 YAML 공통 양식 확정 = wshobson preset-teams.md 차용 + 본 비전 확장:
  - `name` (preset 이름) + `description` (마스터플랜 §2.4 표 "적합 작업" 그대로)
  - `source` (마스터플랜 §2.4 + 외부 출처 인용)
  - `pm_lead` (모든 preset 의 lead = `pm` agent, Opus)
  - `team_size` (3 또는 4)
  - `display_mode` (tmux 권장, wshobson 차용)
  - `members` (Step A 12 agent 中 N명 조합 — name + model + dimension + focus_areas)
  - `task_template` (Subject + Description + Output format = 4 요소 강제)
  - `protocol` (4-step 강제 = TeamCreate → TaskCreate → Agent spawn [model 명시] → SendMessage)
  - `review_cycle_cap: 3` (마스터플랜 §9.1 + aws-samples)
  - `escalation_after_cap` (PM 재설계 또는 사장 컨펌)
  - `variations` (옵션, wshobson 차용)

### Step 2: review preset YAML
- [ ] 2-1. `presets/review.yaml` 신설 (team_size=3, members = security/performance/correctness-reviewer)
- [ ] 2-2. dimension 명시 (보안·성능·정확성), focus_areas Step A `agents/{name}.md §전문 영역` 인용
- [ ] 2-3. variations: `security_focused` (2명: security + correctness) + `full_review` (3명, 기본)

### Step 3: debug preset YAML
- [ ] 3-1. `presets/debug.yaml` 신설 (team_size=3, members = hypothesis-investigator/reproducer/solver)
- [ ] 3-2. 단계 의존성 명시 (가설 → 재현 → 해결 순차) — `addBlockedBy` 체인 (zircote Pipeline 패턴)
- [ ] 3-3. variations: `--hypotheses N` (가설 갯수 변동)

### Step 4: research preset YAML
- [ ] 4-1. `presets/research.yaml` 신설 (team_size=3, members = docs-researcher/community-researcher/analyst)
- [ ] 4-2. 단계 의존성 = docs + community (병렬) → analyst (종합) — Pipeline + Parallel Specialists 혼합 (zircote 7패턴 中 Research+Implementation 변형)

### Step 5: docs-research preset YAML
- [ ] 5-1. `presets/docs-research.yaml` 신설 (team_size=4, members = research 3 + architect)
- [ ] 5-2. analyst 종합 후 architect ADR 도출 단계 추가

### Step 6: harness-design preset YAML
- [ ] 6-1. `presets/harness-design.yaml` 신설 (team_size=3, members = docs-researcher [통합] + auditor + architect)
- [ ] 6-2. **D-15 명시** (harness-design "researcher" = docs-researcher 통합, 별도 신설 X)
- [ ] 6-3. 단계 의존성 = docs-researcher (조사) → architect (설계) → auditor (검증) Pipeline

### Step 7: 운영 sync 5 YAML
- [ ] 7-1. `presets/*.yaml` → `~/.claude/presets/*.yaml` 단방향 sync (D-11 정책)
- [ ] 7-2. SHA256 MATCH 5쌍 검증
- [ ] 7-3. 운영 디렉토리 신설 확인 (`~/.claude/presets/` 부재 시 mkdir)

### Step 8: 마스터플랜 §2.4 정합 검증
- [ ] 8-1. **review preset (3명: 보안/성능/정확성)** = `presets/review.yaml` members ✅
- [ ] 8-2. **debug preset (3명: 가설/재현/해결)** = `presets/debug.yaml` members ✅
- [ ] 8-3. **research preset (3명: 공식docs/커뮤니티/analyst)** = `presets/research.yaml` members ✅
- [ ] 8-4. **docs-research preset (4명: 위 3 + architect)** = `presets/docs-research.yaml` members ✅
- [ ] 8-5. **harness-design preset (3명: researcher/auditor/architect)** = `presets/harness-design.yaml` members (researcher=docs-researcher 통합) ✅
- [ ] 8-6. feature + security preset = 본 turn 범위 외 (별도 turn)

### Step 9: 4-step 프로토콜 강제 + review cycle cap 명시
- [ ] 9-1. 모든 5 YAML 에 `protocol` 섹션 = 4-step (TeamCreate → TaskCreate → Agent spawn[model] → SendMessage) 명시
- [ ] 9-2. 모든 5 YAML 에 `review_cycle_cap: 3` 명시 + 출처 인용 (`aws-samples/sample-claude-code-agent-team` HEAD `67840be3` `skills/spec-workflow/SKILL.md:65`)

### Step 10: history + .todo + commit
- [ ] 10-1. `docs/history/2026-05-04.md` §15 신설 (~50줄 예상) — turn 12 #009-B PASS 요약
- [ ] 10-2. `docs/history/index.md` Day 19 행에 turn 12 결과 추가
- [ ] 10-3. `.todo.md` #009 진행 갱신 (Step B 完 표기)
- [ ] 10-4. **commit + push** = `feat+docs: Day 19 turn 12 — #009-B PASS (presets/ 5 YAML 신설, Step A 12 agent 호출 박음 + 4-step 프로토콜 + review cycle cap 3)`

## 수정 대상 파일

### 신설 (5 + 1 디렉토리)
- `presets/` (디렉토리 신설)
- `presets/review.yaml`
- `presets/debug.yaml`
- `presets/research.yaml`
- `presets/docs-research.yaml`
- `presets/harness-design.yaml`

### 운영 sync (5쌍, git 외부)
- `~/.claude/presets/*.yaml` ← `presets/*.yaml` × 5

### 기존 수정
- `docs/history/2026-05-04.md` — §15 신설
- `docs/history/index.md` — Day 19 행 turn 12 결과 추가
- `.todo.md` — #009 Step B 완료 표기

### 마스터플랜 (정합 검증 결과만, 본문 변경 없을 시 무수정)
- `docs/research/agent-office-masterplan/04_masterplan.md` — Step 8 검증으로 §2.4 표 1:1 정합 확인 시 무수정. 불일치 발견 시 정정.

## 검증 항목

- [ ] **mode: mixed 필수 검증** = 코드 측면 (YAML 양식):
  - [ ] 5 YAML 모두 공통 키 (`name`/`description`/`source`/`pm_lead`/`team_size`/`display_mode`/`members`/`task_template`/`protocol`/`review_cycle_cap`/`escalation_after_cap`) 존재
  - [ ] members 의 모든 `name` = Step A 12 agent (turn 11 신설) 中 1개와 1:1 매핑 (오타·존재하지 않는 agent 0건)
  - [ ] members 의 모든 `model` = pm·architect=opus / 나머지=sonnet (Haiku 0건)
  - [ ] task_template 의 `output_format_required` = 4 요소 (결론·출처·추측 금지·자기비판) 명시
  - [ ] YAML 문법 valid (Python `yaml` 모듈 또는 PowerShell `ConvertFrom-Yaml` 검증)
- [ ] **mode: mixed 필수 검증** = 문서 측면 (마스터플랜 정합):
  - [ ] §2.4 ② 회의실 preset 표 5/7 preset (review·debug·research·docs-research·harness-design) 멤버 명세 = YAML members 와 1:1 매핑 PASS
  - [ ] §10.2 v2 P0 항목 中 O1 4-step 프로토콜 = YAML `protocol` 섹션 명시 PASS
  - [ ] §9.1 review cycle cap 3 = YAML `review_cycle_cap: 3` 명시 PASS
  - [ ] 02_external-deep §6 (wshobson HEAD `ece811f2`) preset-teams.md 양식 차용 정합 (변수: name·team_size·members·task_template·variations 키 존재)
- [ ] **운영 sync 검증**: `presets/*.yaml` ↔ `~/.claude/presets/*.yaml` SHA256 MATCH 5쌍
- [ ] **작업 후 `git status --short` / `git diff --stat`**: 변경 = 5 신설 (presets/) + history 2건 + .todo 1건 = **8 파일** (마스터플랜 무변경 시) 또는 9 (정합 정정 시)
- [ ] **사이드 이펙트 검증**: Step A 12 agent (`agents/*.md`) 무영향 확인 (본 turn 은 호출 박는 자산 신설 = agent 본체 변경 0건)

## 롤백 계획 (실패 시 복구 방법)

- **실패 판정 기준**:
  - YAML 문법 invalid (parser 실패)
  - members `name` 中 Step A 12 agent 와 일치하지 않는 항목 발견
  - 마스터플랜 §2.4 표 멤버 명세 와 1:1 정합 실패
  - 운영 sync 後 SHA256 MISMATCH 발견
- **복구 절차**:
  - `git restore docs/history/2026-05-04.md docs/history/index.md .todo.md`
  - 신설 5 YAML 삭제 = `Remove-Item presets/*.yaml`
  - 운영 5 YAML 삭제 = `Remove-Item ~/.claude/presets/*.yaml`
  - 운영 디렉토리 신설했다면 삭제 = `Remove-Item ~/.claude/presets/ -Recurse`
  - `.checklist.md` → `.backups/.checklist.md.실패_009-B_2026-05-04.md` 이동
- **부분 실패 시**: 5 YAML 中 일부만 PASS 시 부분 결과 commit + 나머지 별도 turn (`.todo.md` #009 갱신, Step B 진행 갱신)
- **보존 정책**: 체크리스트 삭제 금지, `.backups/` 이동만

## 더블 체크

- [ ] 체크리스트에 빠진 항목 없는가?
  - 5 YAML + 운영 sync + 마스터플랜 정합 검증 + 4-step 프로토콜 + review cycle cap + history + .todo + commit 全 망라
- [ ] 실제 반영 안 된 것은 없는가?
  - 각 YAML 신설 후 Read 증거 (members + task_template + protocol)
  - 운영 sync SHA256 MATCH 5쌍 응답 인용
  - 마스터플랜 §2.4 표 grep 결과 응답 인용
  - YAML 문법 valid 검증 결과 인용
- [ ] 다른 코드/문서와 일관성 유지되는가?
  - Step A 12 agent (turn 11) 와 1:1 매핑 (members `name` 정합)
  - 글로벌 `rules/agent-spawn-model.md` (model 명시) = 모든 members `model` 명시
  - 메모리 `feedback_no_haiku.md` (Haiku 0건)
  - turn 9 D-11 (스테이징 ↔ 운영 단방향 sync) + turn 11 D-14·D-15·R-10 정책 정합
  - 마스터플랜 §10.2 v2 P0 항목 (O1 4-step) 흡수 = 본 turn 의 `protocol` 섹션

## 승인 규칙

- 이 체크리스트 작성 후 **구현 파일을 수정하지 않는다**.
- 주인님 승인 메시지를 받은 다음 턴에서만 `approved: true` + `status: approved`로 변경.
- **치명/높음 항목 변경 (재승인 필요)**:
  - YAML members `name` 中 Step A 12 agent 와 불일치 발견 → 재승인 (Step A 신설 누락 발견 = #009-A 재진입)
  - 마스터플랜 §2.4 와 결정적 불일치 발견 → 재승인 (마스터플랜 정정 동시 필요 = 범위 초과)
  - 운영 `~/.claude/presets/` 디렉토리 신설 시 다른 자산과 충돌 발견 → 재승인 (네이밍 정책 결정)
  - YAML 문법 invalid → 재승인 (양식 재설계)

## 작업 시간 추정

- Step 1 (디렉토리 + 양식 확정) = 5분
- Step 2·3·4·5·6 (5 YAML 신설, 양식 차용) = 5 × 5분 = 25분
- Step 7 (운영 sync + SHA256) = 5분
- Step 8 (정합 검증) = 5분
- Step 9 (프로토콜 + cap 명시 = Step 1·2~6 와 동시 진행)
- Step 10 (history + .todo + commit) = 10분
- **Total ≈ 50분** (1 turn 가능)
