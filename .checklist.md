---
approved: true
status: in_progress
mode: mixed
작성일: 2026-04-23
version: v3
승인일: 2026-04-23
---

# 체크리스트: /checklist 스킬 혼동 제거 + 5차 피드백 Q1~Q3 종결 (v3, /feedback 반영)

근거:
- 주인님 오늘 지적 (글로벌에 스킬 지시는 있는데 실제 파일이 없어서 혼동)
- `.backups/.checklist.md.승인대기_checklist5차_2026-04-17.md` 결정 대기 종결
- 자체 비판 검토 6건 (SSOT 부재, 이월 장치 부재, 롤백 git 미명시, post-edit-verify 정합 미점검, Read/grep 증거 리포트 규칙 부재, 프로젝트 CLAUDE.md 프리플라이트 애매)
- `/feedback` 3 CLI 리뷰 반영 (`docs/feedback/2026-04-23_claude_.checklist_20260423-084406-종합.md`)
  - 치명급 3건: ①`.checklist.md` stash 위험, ②글로벌 파일 사전 백업 누락, ③venv-guard.sh 선제 실측
  - 높음 2건: frontmatter YAML 구분자, 기존 변경 3건과 최종 1:1 검증 충돌
  - 중간 2건: workflows 갱신 누락, 작업 4번 혼재
  - 낮음 2건: "선택" 기준, 변경 표 번호 정합

## 배경 / 목적

1. **혼동 원인 제거**: 글로벌 CLAUDE.md/rules는 `/checklist`를 참조하지만 `~/.claude/skills/checklist/`가 비어있음. Claude Code는 글로벌 스킬만 자동 로드.
2. **주인님 결정 반영**: Q1 → **A** (훅 제거, 스킬만 유지) / Q2 → **A** (프로젝트 = 스테이징, 글로벌 = 운영) / Q3 → **보수 유지** (tiny 3줄/240자).
3. **5차 피드백 승인대기 종결**: 2026-04-17 보류본 이번 결정으로 종결. 나머지 치명/높음/중간(C1·H1~3·M1~3)은 **별도 세션 이월** — 이번 범위는 혼동 제거 한정.

## 작업 전 기준선

### git status --short
```
 M docs/history/2026-04-22.md
?? docs/research/
?? .checklist.md
?? docs/feedback/2026-04-23_*(4개, /feedback 결과물)
```
→ `docs/history/2026-04-22.md` + `docs/research/` 는 이번 작업과 **무관한 기존 변경** (최종 검증에서 명시적 제외).
→ `.checklist.md` + `docs/feedback/2026-04-23_*` 는 이번 작업으로 신규 생성된 파일 (최종 검증 대상).

### 실측 상태 (v2 + /feedback 반영)
| 위치 | 상태 | 비고 |
|------|------|------|
| `~/.claude/settings.json` | **이미 깨끗** | checklist-guard/doc-doublecheck/auto-backup/check-streamlit/venv-guard 훅 등록 0건 (확인됨) |
| `~/.claude/hooks/` | **이미 정리됨** | 10개만 유효 — dev/doc-checklist-guard·venv-guard 포함 dead 훅 **없음** |
| `~/.claude/skills/checklist/` | **비어있음** | 혼동의 유일한 실제 원인 |
| `~/.claude/skills/.backups/checklist-SKILL-2026-04-17.md` | **존재** (6396 bytes, 04-17) | 글로벌 스킬 롤백 경로 실측 완료 |
| `~/.claude/rules/dev-checklist.md` | **존재** (현재 운영 파일) | 04-16 수정 |
| `~/.claude/rules/.backups/dev-checklist.bak.md` | **존재 but drift** (3685 bytes, 04-11) | 04-11 백업 → 현재 04-16 파일과 내용 불일치 가능. **이번 작업용 새 타임스탬프 백업 필수** (Codex #4) |
| `Harness-engineering/settings/settings.json` | **drift** | dead hook 4개 잔존 (실제 로드 영향 없음) |
| `Harness-engineering/hooks/` | **drift 후보 6개** | `venv-guard.sh` 는 **3-a 에서 dead 여부 실측 확정 후 결정** |
| `Harness-engineering/docs/workflows/dev-checklist.md` | **존재** | 3-a 에서 스킬 경로·명칭 drift 확인 |
| `~/.claude/hooks/post-edit-verify.sh` | **정상** | `.checklist.md` 인식 완료 |

## 작업 항목

### 0. 작업 전 체크포인트 + 글로벌 사전 백업 (v3 재설계 — 치명급 ①②)
- [ ] 0-a: `.checklist.md`, `docs/feedback/2026-04-23_*` 를 `git add` 로 **tracked 전환** → stash에 쓸려가지 않도록 고정
- [ ] 0-b: 체크포인트 커밋 생성 — `git commit -m "chore: checkpoint before /checklist 혼동 제거 작업"` (stash 대신 커밋 기반 롤백)
  - **근거**: Codex #1 (`git stash --include-untracked` 가 `.checklist.md` 자체를 숨김). 커밋 기반 롤백은 `git reset --hard HEAD` 로 돌아오면 됨.
- [ ] 0-c: 글로벌 `~/.claude/rules/dev-checklist.md` 를 `~/.claude/rules/.backups/dev-checklist.md.20260423_HHMMSS.bak` 로 **타임스탬프 복사**
  - **근거**: Codex #2 / Claude Sub M-1. 기존 04-11 백업은 현재 04-16 파일과 drift. 새 작업용 정확 백업 필요.
- [ ] 0-d: 글로벌 `~/.claude/skills/checklist/` 는 **신규 생성**이라 백업 불필요. 롤백은 "디렉토리 삭제"로 충분.

### 1. 글로벌 스킬 복원 (Q2.A) — 이번 작업의 핵심
- [ ] `New-Item -ItemType Directory "$HOME\.claude\skills\checklist"` 로 디렉토리 생성
- [ ] `Harness-engineering/skills/checklist/SKILL.md` → `~/.claude/skills/checklist/SKILL.md` 로 복사 (PowerShell `Copy-Item`)
- [ ] 복사 후 글로벌 파일을 Read로 확인 (내용 일치 + 93줄 유지 + frontmatter의 `trigger: /checklist` 존재)
- [ ] Phase 5 리포트에 Read 결과 인용

### 2. SSOT 규칙 명문화
- [ ] `~/.claude/rules/dev-checklist.md` 에 SSOT 규칙 추가:
  > "**스테이징/운영 분리**: 프로젝트 `Harness-engineering/skills/checklist/SKILL.md` = 스테이징(편집 대상). `~/.claude/skills/checklist/SKILL.md` = 운영(Claude Code 자동 로드). 스테이징 수정 후 반드시 운영으로 복사. drift 발견 시 **프로젝트가 정답**, 글로벌 덮어쓰기."
- [ ] 프로젝트 `CLAUDE.md` 의 "프로젝트 범위" 섹션에 한 줄 추가:
  > "스킬·훅·rules는 프로젝트 = 스테이징 / `~/.claude/` = 운영. 편집 후 동기화 책임은 이 프로젝트에 있음."

### 3-a. `venv-guard.sh` 및 프로젝트 drift dead 여부 실측 (v3 신규 — 치명급 ③)
- [ ] `grep -rn "venv-guard" Harness-engineering/settings/` 실행 → 참조 확인
- [ ] `grep -rn "venv-guard" ~/.claude/settings.json` 실행 → 참조 없음 확정 (실측 이미 없음)
- [ ] `cat Harness-engineering/hooks/venv-guard.sh` Read → 현재 동작 의도 확인
- [ ] `docs/harness_refactor_plan.md:104` 의 "기본 비활성화, Python 프로젝트 audit 항목으로 이동" 계획과 대조
- [ ] **판정 기록**: v3 체크리스트 본문 하단 "실측 로그" 섹션에 결과 기입. 판정 결과 dead → 3-b 포함 / 운영 → 3-b 제외.
- [ ] 같은 방식으로 `doc-doublecheck-guard.sh`, `auto-backup.sh`, `check-streamlit.sh` 운영 여부 간단 확인 (`grep -n` 으로 settings 참조)

### 3-b. 프로젝트 drift 정리 (3-a 결과 기반)
- [ ] `Harness-engineering/settings/settings.json` 에서 dead hook 4개 (`dev-checklist-guard` × 2, `doc-checklist-guard` × 2) 제거 — 이건 실측 완료로 확정
- [ ] 3-a에서 dead 확정된 .sh 파일만 `Harness-engineering/hooks/.backups/` 로 이동 (**삭제 금지, 이동**)
  - 확정 대상 (최소): `dev-checklist-guard.sh`, `doc-checklist-guard.sh`
  - 3-a 결과에 따라 추가: `doc-doublecheck-guard.sh`, `auto-backup.sh`, `check-streamlit.sh`, `venv-guard.sh` (하나씩 판정)
- [ ] `python -m json.tool Harness-engineering/settings/settings.json` 로 JSON 유효성 재확인

### 4-a. 프로젝트 CLAUDE.md 프리플라이트 중복 금지 확인 (v3 분리 — M-3)
- [ ] 프로젝트 `CLAUDE.md` 를 Read 해서 **프리플라이트 블록이 이미 있는지** 확인
- [ ] 있으면 제거 (글로벌 상속이 SSOT). 없으면 유지.
- [ ] 판정 결과를 실측 로그에 기록

### 4-b. 프로젝트 CLAUDE.md 스테이징/운영 메모 추가 (v3 분리)
- [ ] 프로젝트 `CLAUDE.md` 의 "프로젝트 범위" 섹션에 한 줄 추가 (2번 작업과 동일 문구) — **이미 2번에서 수행하면 중복 작업. 2번을 4-b로 통합 고려** → v3에서는 2번 bullet 2 를 이 항목으로 이동 (실제 편집은 1회만)
- [ ] 프로젝트 `CLAUDE.md` 에 명시 메모 1줄 추가: "프리플라이트(`/checklist`, 훅 차단 시 참조)는 글로벌 `~/.claude/CLAUDE.md` 가 SSOT. 이 파일에 중복 금지."

### 5. post-edit-verify.sh 정합 점검
- [ ] `~/.claude/hooks/post-edit-verify.sh` 의 `.checklist.md` 경로 기대값이 **프로젝트 루트 기준**인지 Read로 확인 (이미 실측: line 98, 252 인식 완료 — 재확인)
- [ ] 현재 `.checklist.md` 가 post-edit-verify 에 의해 정상 처리되는지 샘플 관찰 (Phase 3)

### 6. docs/workflows/dev-checklist.md 갱신 판단 (v3 신규 — M-2)
- [ ] `Harness-engineering/docs/workflows/dev-checklist.md` Read
- [ ] 스킬 경로(`~/.claude/skills/checklist/`) 및 명칭(`/checklist`) 언급이 현 구조와 일치하는지 확인
- [ ] 불일치 있으면 수정, 없으면 "검증 완료, 수정 불필요" 로 실측 로그에 기록

### 7. 5차 피드백 승인대기본 종결 처리
- [ ] `.backups/.checklist.md.승인대기_checklist5차_2026-04-17.md` 맨 위에 "종결 메모" 추가:
  > "결정: Q1.A (훅 제거, 스킬 유지) / Q2.A (프로젝트 스테이징, 글로벌 운영) / Q3 보수 유지. 나머지 C1·H1~3·M1~3은 별도 세션 이월 — `docs/history/index.md` 진행 중 섹션에 등록."
- [ ] 파일명을 `.backups/.checklist.md.종결_Q1~3결정반영_2026-04-23.md` 로 변경

### 8. 범위 제외 항목의 이월 장치
- [ ] `docs/history/index.md` 의 "진행 중" 섹션에 신규 엔트리 등록:
  > `[2026-04-23] 진행 중 | /checklist 5차 피드백 나머지 (C1·H1~3·M1~3) | 근거: .backups/.checklist.md.종결_Q1~3결정반영_2026-04-23.md | 다음: 별도 체크리스트로 작업 재개`
- [ ] 이월 엔트리는 **주인님이 닫기 전까지 유지** (자의적 종결 금지)

### 9. 히스토리 기록
- [ ] `docs/history/2026-04-23.md` 신규 작성 (Day 10) — 주인님 지적, 원인 분석 (실측 포함), Q1~Q3 결정, 작업 내역, /feedback 적용 결과, 남은 항목(C1·H1~3·M1~3 → 진행 중 이월), SSOT 규칙 신설
- [ ] `docs/history/index.md` 업데이트 (Day 10 종결 엔트리 + 진행 중 이월 엔트리)

## 범위 제외 (다음 세션 — 명시적 이월)
- **C1** (SKILL.md 승인 허점 차단 — `approved: false` 기본값 + Phase 1 턴 종료 명령형)
- **H1** (mode 분기 도입), **H2** (tiny edit 의미 조건 보강), **H3** (Phase 3 도구·로그 증명)
- **M1~M3** (git status 기준선 / 롤백·보존 / HISTORY 경로 정정)

→ 이월 장치: 작업 항목 8번이 `docs/history/index.md` 진행 중 섹션 등록. 닫을 때까지 유지.

## 수정 대상 파일

**신규 생성**
- `~/.claude/skills/checklist/SKILL.md` — 프로젝트 파일에서 복사
- `docs/history/2026-04-23.md` — Day 10 기록

**편집**
- `~/.claude/rules/dev-checklist.md` — SSOT 규칙 추가
- `Harness-engineering/CLAUDE.md` — 스테이징/운영 메모 + 프리플라이트 중복 금지 메모
- `Harness-engineering/settings/settings.json` — dead hook 4개 제거
- `docs/history/index.md` — Day 10 종결 + 진행 중 이월

**이동 (삭제 금지)**
- `Harness-engineering/hooks/{3-a에서 dead 확정된 .sh들}` → `Harness-engineering/hooks/.backups/`
- `.backups/.checklist.md.승인대기_checklist5차_2026-04-17.md` → `.backups/.checklist.md.종결_Q1~3결정반영_2026-04-23.md`

**백업 (0-c)**
- `~/.claude/rules/dev-checklist.md` → `~/.claude/rules/.backups/dev-checklist.md.20260423_HHMMSS.bak`

**조건부**
- `Harness-engineering/docs/workflows/dev-checklist.md` — 6번에서 drift 발견 시만 수정

**건드리지 않음 (실측 확인)**
- `~/.claude/settings.json`, `~/.claude/hooks/*`, `~/.claude/hooks/post-edit-verify.sh`

## 연관 문서 (교차 검증 대상)
- `~/.claude/CLAUDE.md` 프리플라이트 블록
- `~/.claude/rules/dev-checklist.md`
- 프로젝트 `CLAUDE.md`
- `docs/history/index.md` (SSOT)
- `docs/workflows/dev-checklist.md`

## 검증 항목 (Phase 5 리포트에 Read/grep/ls 결과 직접 인용 의무)
- [ ] 글로벌 `~/.claude/skills/checklist/SKILL.md` 존재 + 93줄 + 트리거 `/checklist` (Read 인용)
- [ ] 글로벌 `~/.claude/rules/dev-checklist.md` 에 SSOT 규칙 문자열 존재 (grep 인용)
- [ ] 프로젝트 `CLAUDE.md` 스테이징/운영 메모 존재 (grep 인용)
- [ ] 프로젝트 `settings/settings.json` JSON 유효 + `dev-checklist-guard`/`doc-checklist-guard` 0건 (grep 인용)
- [ ] 프로젝트 `hooks/` 의 3-a 확정 dead 스크립트가 `hooks/.backups/` 로 이동 (ls 인용)
- [ ] `doc-protection.sh`·`post-edit-verify.sh`·`pre-commit-guard.sh` 생존 (ls 인용)
- [ ] 승인대기본 파일이 `.종결_*.md` 로 이름 변경 + 원본 파일명 부재 (ls 인용)
- [ ] `docs/history/2026-04-23.md` 내용에 주인님 지적·원인·Q1~Q3 결정·범위 제외·SSOT·/feedback 적용 6요소 명시
- [ ] `docs/history/index.md` 에 Day 10 종결 + 진행 중 이월 양쪽 존재 (grep 인용)
- [ ] **최종 검증 (Codex #5 반영)**: `git status --short` + `git log --oneline -3` 기준으로, **체크포인트 커밋 (0-b) 이후 추가된 변경분** 만 "수정 대상 파일" 과 1:1 일치. 기준선의 `docs/history/2026-04-22.md` 와 `docs/research/` 는 명시적 제외.
- [ ] `~/.claude/rules/.backups/dev-checklist.md.20260423_*.bak` 타임스탬프 백업 존재 확인 (ls 인용)

## 롤백 계획 (v3 — Codex #1·#2 반영)

### 기본 복구 수단
- **작업 전**: 0-b 의 체크포인트 커밋이 정답 상태. 실패 시 `git reset --hard <커밋해시>` 로 프로젝트 트리 완전 복원.
- **프로젝트 개별 파일**: `git checkout -- <파일>`

### 글로벌 파일 복구 (git 관리 밖 — 사전 백업 필수)
- `~/.claude/skills/checklist/SKILL.md` 실수 시: 디렉토리 삭제 (`Remove-Item -Recurse`) 로 "작업 전" 복원. 필요 시 `~/.claude/skills/.backups/checklist-SKILL-2026-04-17.md` 에서 161줄 구버전 복원.
- `~/.claude/rules/dev-checklist.md` 실수 시: 0-c의 `~/.claude/rules/.backups/dev-checklist.md.20260423_HHMMSS.bak` 로 복사 복원 (**기존 04-11 .bak 는 사용 금지, drift 상태**)
- **글로벌 settings.json·hooks 는 이번 작업에서 건드리지 않음** → 복구 불필요

### 이동 롤백
- 승인대기본 이름 변경 실패 시: 역방향 이동
- hooks 이동 실패 시: `.backups/` 에서 원위치 복귀

### 체크리스트 자체
- `.checklist.md` 는 삭제 금지 → 작업 완료 후 `.backups/.checklist.md.완료_checklist혼동해소_2026-04-23.md` 로 이동

## 더블 체크
- [ ] 체크리스트에 빠진 항목 없는가? (주인님 지적·Q1·Q2·Q3 종결·히스토리·SSOT·이월장치·롤백·/feedback 반영 9축)
- [ ] 체크만 하고 실제 반영 안 된 것은 없는가? (각 항목 Read/grep/ls 증거 남기기)
- [ ] 다른 코드/문서와 일관성 유지되는가? (글로벌 CLAUDE.md·rules·프로젝트 CLAUDE.md·스킬 트리거 4자 정합)
- [ ] 범위 제외 항목이 이월 장치(진행 중 섹션)로 실제 등록되었는가?
- [ ] Phase 5 리포트에 각 검증 항목별 Read/grep/ls 결과 직접 인용 ("확인했다" 선언 금지)
- [ ] 최종 검증의 "새로 추가된 변경분" 기준이 실측 가능한가? (체크포인트 커밋 해시 기록 필수)

## 승인 규칙
- 이 체크리스트 작성 후 **구현 파일을 수정하지 않는다** (승인 턴 대기).
- 주인님이 "진행" 또는 "approved" 주시면 다음 턴에서 `approved: true`로 바꾸고 Phase 2 진입.
- 범위 변경 시 재승인.

## 실측 로그 (Phase 2~3 실행 중 채워짐)

### 3-a. venv-guard.sh 등 dead 판정
- `venv-guard.sh`: (실측 예정)
- `doc-doublecheck-guard.sh`: (실측 예정)
- `auto-backup.sh`: (실측 예정)
- `check-streamlit.sh`: (실측 예정)

### 4-a. 프로젝트 CLAUDE.md 프리플라이트 블록 유무
- (실측 예정)

### 6. docs/workflows/dev-checklist.md drift 유무
- (실측 예정)

### 0-b. 체크포인트 커밋 해시
- (Phase 2 진입 시 기록)

## v1 → v2 → v3 변경 요약
| # | v2에서 반영 (v1→v2) | v3에서 반영 (v2→v3, /feedback) | 대응 작업 항목 |
|---|---|---|---|
| 1 | 수정 대상을 글로벌→프로젝트 drift 로 교정 | frontmatter `---` 구분자 정규화 | 파일 최상단 |
| 2 | "글로벌 settings 건드리지 않음" 명시 | 0번을 stash → 체크포인트 커밋 + 글로벌 타임스탬프 백업 으로 재설계 | 0-a/b/c/d |
| 3 | git stash 체크포인트 추가 | 기준선 표에 롤백 경로 존재 확인 추가 | 기준선 표 |
| 4 | SSOT 규칙 명문화 신규 | 3번을 3-a (실측) + 3-b (이동) 로 분리 | 3-a / 3-b |
| 5 | post-edit-verify 정합 점검 신규 | 4번을 4-a (중복 금지) + 4-b (스테이징 메모) 로 분리 | 4-a / 4-b |
| 6 | 진행 중 섹션 이월 장치 신규 | docs/workflows/dev-checklist.md 갱신 판단 신규 | 6번 |
| 7 | Phase 5 증거 인용 의무 | 최종 검증을 "체크포인트 이후 변경분"으로 재정의 | 검증 항목 |
| 8 | 프리플라이트 처리 "중복 금지" 명확화 | 변경 표에 작업 항목 번호 직접 기재 | 이 표 |
| 9 | — | 3번 "선택" 제거 (실제 필수) | 3-b |
