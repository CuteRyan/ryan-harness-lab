# 체크리스트: 하네스 피드백 반영 (우선순위 1~6)

approved: true

## 작업 항목

### 우선순위 1: rules ↔ settings 배선 정합성
- [x] `rules/dev-checklist.md` — 훅 강제 아닌 스킬 권장으로 정확히 기술
- [x] `rules/document-safety.md` — 훅 강제 아닌 스킬 권장으로 정확히 기술

### 우선순위 2: post-edit-verify가 `.checklist.md` 인식
- [x] `post-edit-verify.sh` — `.dev-checklist.md`/`.doc-checklist.md` → `.checklist.md`도 인식하도록 업데이트

### 우선순위 3: doc-protection.sh 우회 경로 강화
- [x] `.backups` blanket allow → 대상 경로가 .backups/ 아래인지 확인으로 변경
- [x] `git` 전체 면제 → 읽기 전용 명령(status/diff/log/show/branch/blame)만 허용
- [x] rm/truncate, python/node 스크립트 쓰기, git 파괴적 명령 추가 차단

### 우선순위 4: MultiEdit 배선 추가
- [x] `settings.template.json`에 MultiEdit matcher 추가 (Pre: doc-protection, Post: post-edit-verify)
- [x] 실제 `~/.claude/settings.json`에도 동일 반영

### 우선순위 5: 배포 가드 fail-closed 전환
- [x] `deploy-version-guard.sh` — CI unknown/none 시 차단으로 변경 (passed만 통과)
- [x] cancelled/timed_out 등 비성공 conclusion도 실패 처리 (success만 passed)

### 우선순위 6: skipDangerousModePermissionPrompt 제거
- [x] `~/.claude/settings.json`에서 `skipDangerousModePermissionPrompt: true` 제거

## 수정 대상 파일
- `rules/dev-checklist.md` — 스킬 권장으로 정확한 기술
- `rules/document-safety.md` — 스킬 권장으로 정확한 기술
- `hooks/post-edit-verify.sh` — .checklist.md 인식 추가
- `hooks/doc-protection.sh` — .backups/git 우회 강화
- `settings/settings.template.json` — MultiEdit 배선
- `~/.claude/settings.json` — MultiEdit 배선 + skipDangerousMode 제거
- `hooks/deploy-version-guard.sh` — fail-closed 전환

## 검증 항목
- [x] rules 내용이 실제 배선과 일치하는가 — 글로벌+프로젝트 rules 모두 수정 확인
- [x] post-edit-verify가 .checklist.md 파일 편집 시 검증 동작하는가 — 97행, 251행 확인
- [x] doc-protection.sh 우회 경로가 막혔는가 — .backups/git 화이트리스트+추가 패턴 확인
- [x] MultiEdit 배선이 settings에 반영되었는가 — Pre(67행)+Post(96행) 확인
- [x] 배포 가드가 CI unknown 시 차단하는가 — fail-closed(74행) 확인
- [x] skipDangerousModePermissionPrompt 제거 확인 — grep 매치 없음
- [x] 글로벌 hooks 디렉토리에 수정 파일 동기화 완료

## 더블 체크
- [x] 체크리스트에 빠진 항목 없는가? — 우선순위 6(skipDangerous) 소급 추가함
- [x] 실제 반영 안 된 것은 없는가? — 전체 7개 파일 grep으로 확인 완료
- [x] 다른 코드/문서와 일관성 유지되는가? — rules↔settings↔hooks 3자 일치 확인
