# 체크리스트: Day 10 이월 ①③⑤⑥ + Day 12 부산물 #8#9 (4인 팀 병렬)

approved: true  <!-- 2026-04-25 승인 — 4인 팀 병렬 진행 -->
status: draft
mode: code+doc+mixed
작성일: 2026-04-25

## 배경 / 목적
2026-04-25 5개 CLAUDE.md 보강 종결 후, 즉시 처리 가능한 기술 부채 4영역(6건)을 4인 팀 병렬 처리.
- A1+A2 는 `/feedback` 스킬 내 다른 파일 → 분리 가능
- B 는 `/checklist` 스킬 + 워크플로 문서 → 같은 사람
- C 는 Agent-office (별도 repo) → 같은 사람

## 작업 전 기준선 (git status --short)
```
?? .checklist.md  (이 파일, 신규 작성 직후)
```

## 팀 구성 (4인)
| Teammate | 영역 | 근거 파일 |
|---|---|---|
| A1 — `cli-stateful` | 이월 ⑤ CLI 연속 호출 누적 실패 | `docs/feedback/2026-04-23_*_2026-04-22_20260423-{173731,174318,174824}.md` 6건 |
| A2 — `bom-recovery` | 이월 ⑥ prepare-isolation.ps1 BOM 회복 | `docs/research/feedback-encoding-fix/03_fix-plan.md` L4 섹션 추가 |
| B — `checklist-skill` | 이월 ① 5차 피드백 7건 + 이월 ③ dev-checklist.md 재작성 | `.backups/.checklist.md.종결_Q1~3결정반영_2026-04-23.md` |
| C — `agent-office` | Day 12 #8 venv→.venv + #9 CLAUDE.md 통합 | `~/.claude/rules/coding.md` Phase 1~4 |

## 작업 항목

### A1. CLI 연속 호출 stateful 누적 실패 (이월 ⑤)
- [ ] A1-1. `orchestrate.ps1` Start-Sleep 간격 2s → 10s 증가 + 변수화 (`$WaitSec`)
- [ ] A1-2. `run-codex.ps1` / `run-gemini.ps1` retry 시 지수 백오프 (1s → 2s → 4s, 최대 3회)
- [ ] A1-3. orchestrate Start-Job 모드를 순차 옵션 추가 (`-Sequential` 플래그)
- [ ] A1-4. 운영 동기화 (스테이징 → `~/.claude/skills/feedback/scripts/*` SHA256 MATCH)
- [ ] A1-5. `docs/research/feedback-encoding-fix/03_fix-plan.md` 이월 ⑤ 해결 기록 추가

### A2. prepare-isolation.ps1 BOM 회복 단계 (이월 ⑥)
- [ ] A2-1. `prepare-isolation.ps1` 격리 복사 시 UTF-8 BOM 삽입 (`Set-Content -Encoding UTF8`)
- [ ] A2-2. `docs/research/feedback-encoding-fix/03_fix-plan.md` L4 섹션 신설 (Codex 내부 spawn 회복 단계 skip 근거)
- [ ] A2-3. 운영 동기화 (스테이징 → 운영 SHA256 MATCH)

### B. /checklist 5차 피드백 7건 + dev-checklist.md 재작성 (이월 ① + ③)
- [ ] B-1. `skills/checklist/SKILL.md` C1 — `approved:false` 기본값 + 턴 종료 명령형
- [ ] B-2. SKILL.md H1 — mode 분기 (code|doc|design|mixed) 필수 섹션 가이드
- [ ] B-3. SKILL.md H2 — tiny edit 의미 조건 보강 (blanket 예외 제거)
- [ ] B-4. SKILL.md H3 — Phase 3 Read 증거 의무 (선언 금지)
- [ ] B-5. SKILL.md M1 — git status 기준선 (Phase 1 + Phase 3)
- [ ] B-6. SKILL.md M2 — `.backups/` 이동 + 삭제 금지 명문화
- [ ] B-7. SKILL.md M3 — `HISTORY.md` → `docs/history/` 경로 정정
- [ ] B-8. `docs/workflows/dev-checklist.md` 전면 재작성 (스킬 기반)
- [ ] B-9. 운영 동기화 (`~/.claude/skills/checklist/SKILL.md` SHA256 MATCH)

### C. Agent-office venv→.venv 마이그레이션 + CLAUDE.md (Day 12 #8 + #9)
- [ ] C-1. Phase 1 사전 확인 (.gitignore `.venv/`, 하드코딩 grep, requirements.txt 최신)
- [ ] C-2. Phase 2 재생성 (`.venv` 생성 + `requirements.txt` 복원)
- [ ] C-3. Phase 3 동작 확인 (uvicorn 기동 또는 핵심 모듈 import)
- [ ] C-4. Phase 4 정리 (구 `venv/` → `venv.old/` 또는 삭제)
- [ ] C-5. `CLAUDE.md` `## 개발 명령` `venv\Scripts\activate` → `.venv\Scripts\activate` 갱신
- [ ] C-6. `## 참조` + `## 참고 (@import)` 통합 — 중복 docs/research 정리
- [ ] C-7. 별도 git repo commit (Agent-office, master)

## 검증 항목
- [ ] V-1. `/feedback` 단발 스모크 (한글 정상 + mojibake 0) — A1+A2 종합
- [ ] V-2. `/checklist` SKILL.md 7건 grep 확인 (각 항목 키워드)
- [ ] V-3. `dev-checklist.md` 글로벌 `rules/dev-checklist.md` 와 SSOT 충돌 없음
- [ ] V-4. Agent-office `.venv/` 동작 확인 (uvicorn 또는 import 1건)
- [ ] V-5. Agent-office CLAUDE.md venv 표기 + 참조 통합 둘 다 반영
- [ ] V-6. 사이드 이펙트 — 다른 스킬·hook·rules 미수정 (`git diff --stat`)

## 더블 체크
- [ ] D-1. 4 teammate 모두 자기 영역 외 파일 미수정
- [ ] D-2. 운영 동기화 (스테이징→운영) 누락 없음 — 4명 모두 `Get-FileHash` SHA256 MATCH 보고
- [ ] D-3. Agent-office commit 은 별도 repo 에 (Harness-engineering 과 분리)

## 롤백 계획
- A1/A2: 스테이징·운영 모두 git checkpoint 커밋 후 진행. 실패 시 `git revert`
- B: `~/.claude/rules/.backups/` 에 SKILL.md 타임스탬프 백업 선행
- C: `venv/` 즉시 삭제 안 함 → `venv.old/` 보존 (1주 후 정리)

## Non-goals
- 이월 ④ Gemini rubber-stamp 근본 해결 (주인님 결정 대기)
- Knowledge-platform 141→80 축약 (주인님 직접 처리)
- agent-team-manager v2 §9 (주인님 결정 대기)
- candidate_orchestra 처리 방침
- Day 4 이월 B5 / B7 (별도 세션)
- /todo 스킬 제작

## 승인 후 진행 순서
1. `approved: true` 변경 → 4인 팀 spawning (체크포인트 커밋 선행)
2. 각 teammate 자기 영역 정찰 → 작업 → 검증 → 보고
3. 메인이 Read 로 cross-check + V-1~V-6 + D-1~D-3
4. 4영역 commit 분리 (Harness 1~2개 + Agent-office 1개)
5. teammate shutdown
