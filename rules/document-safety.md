# 문서 안전 규칙 (훅 + 스킬)

> `doc-protection.sh` 훅이 문서 보호(Write 차단 + 자동 백업)를 **강제**. 체크리스트는 `/checklist` 스킬(**권장**, 훅 강제 아님). 상세: `docs/workflows/document-work.md`

## 발표자료/논문
- md 먼저 → HTML 반영 (역순 금지)
- generate 스크립트로 재생성 금지
