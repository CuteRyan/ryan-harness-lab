---

리뷰 결과입니다. 총 8건, 치명 1건 포함입니다.

---

[치명] **SKILL.md:91 — Validation Gate 조건 #4, `근거` 키워드 단독 통과 허용**
서브 CLI가 "근거: 해당 없음" 한 줄만 써도 `근거` 정규식에 매칭되어 VALID 판정. 실질 증거 없이 통과 가능 — Gate의 핵심 목적(rubber-stamp 차단)이 무력화됨.
근거: SKILL.md:91 — `"근거" 키워드 / URL / "파일.확장자:줄번호" 중 하나` (키워드 존재 여부만 검사, 내용 검증 없음)

[높음] **SKILL.md:37 — Step 2 FilePaths, Step 1 JSON 연결 지침 없음**
`-FilePaths @("<claude_sub 경로>", ...)` 가 Step 1 JSON의 어느 키에서 오는지 명시 없음. Step 1(SKILL.md:26)과 Step 2(SKILL.md:37)가 인접하지 않아 LLM이 매핑을 놓칠 수 있음.
근거: SKILL.md:26 `{claude_sub, codex, gemini, ...}` vs SKILL.md:37 `<claude_sub 경로>` — 키 이름 일치하나 연결 문장 없음.

[높음] **SKILL.md:123-126 — 미구현 기능("이전 피드백 참조")이 실행 명세에 혼재**
동작 기술 후 "MVP에서는 미구현 — Phase 2"로 끝냄. LLM이 현재 구현된 동작으로 오해하고 수동 구현을 시도할 위험.
근거: SKILL.md:125 `"MVP에서는 미구현 — Phase 2"` — 미구현 동작이 운영 명세에 공존.

[높음] **SKILL.md:30 — Gemini sycophancy 근거 출처, 이슈 번호만 기재 (URL 없음)**
`"GitHub gemini-cli #13801"` — 번호만 있고 `https://` URL 없음. 이 파일이 Validation Gate 근거 조건의 모범 케이스를 제시해야 하는 문서임을 감안하면 일관성 문제.
근거: SKILL.md:30 인용 — URL 형식 미충족.

[중간] **SKILL.md:41 vs 87-91 — 유효성 판정 규칙 2곳 중복 (SSOT 위반)**
Step 2 설명과 "유효성 판정" 섹션이 동일 판정 조건을 반복. SKILL.md:85에서 스크립트를 SSOT로 선언했음에도 SKILL.md 내부에서 같은 규칙을 복사 — 한쪽만 수정 시 drift 발생.
근거: SKILL.md:41 ≡ SKILL.md:90-91 — 동일 내용 두 군데.

[중간] **SKILL.md:79 — 슬러그 포맷 정의를 SKILL.md에 복사 (orchestrate.ps1과 이중 관리)**
`<대상파일basename>_<yyyyMMdd-HHmmss>` 는 orchestrate.ps1 내부 구현 상세. 스크립트 포맷 변경 시 SKILL.md가 stale 상태로 남음.
근거: SKILL.md:79 인용 — scripts/orchestrate.ps1에도 동일 정의가 있어야 하므로 복사 상태.

[중간] **SKILL.md:109-110 — 격리 디렉토리 사후 정리 절차 없음**
Codex 격리 디렉토리(`$HOME\codex-cwd\<slug>`) 생성은 있으나, 스크립트 목록(SKILL.md:130-137) 어디에도 cleanup 스크립트 없음 — 파일 누적 및 민감 정보 잔류 가능.
근거: SKILL.md:132 `scripts/prepare-isolation.ps1 — 격리 디렉토리 생성` 만 있고 삭제 항목 없음.

[낮음] **SKILL.md:141-147 — Phase 2 로드맵이 운영 명세에 혼재**
미구현 항목 5개가 운영 명세 말미에 직접 기술되어 현재 스펙과 미래 계획 경계 불명확.
근거: SKILL.md:141-147 — `ROADMAP.md` 또는 별도 Issue 링크로 분리 권장.
