파일을 검토했습니다. 리뷰 결과입니다.

---

## 리뷰: `2026-04-22.md`

[치명] **62행 — Fix #1이 Issue #1을 전혀 해결하지 않음**
표 "메타 리뷰 반영" 1번 수정(`[Console]::OutputEncoding = UTF-8` 추가, `run-claude-sub.ps1`)은 54행 Issue #1 "격리 디렉토리에 `scripts/` 없어 리뷰어들이 '스크립트 없다'로 혼란"과 무관한 인코딩 조치임. Fix #2~#4는 Issue #2~#4와 1:1 대응되므로 번호 매핑이 의도적이었음을 확인할 수 있음. "[치명]"으로 분류된 격리 디렉토리 구조 문제가 실제로는 미반영되었거나, 표 자체가 잘못 작성됨. 두 경우 모두 문서의 신뢰도를 훼손.
근거: 54행 "격리 디렉토리에 `scripts/` 없어" vs 62행 "`[Console]::OutputEncoding = UTF-8` 추가" — 두 항목 사이에 논리적 연결 없음.

---

[높음] **65행 — `validate-outputs.ps1` 정규식 `\.ext:\d+` 오류**
`근거|https?://|\.ext:\d+` 에서 세 번째 패턴은 문자열 `.ext:숫자` (예: `.ext:42`)만 매치함. 파일:줄 형태(`run-claude-sub.ps1:33`, `validate-outputs.ps1:10`)를 감지하려면 `\.\w+:\d+` 또는 `[\w.-]+:\d+`가 필요. 현재 패턴으로는 실제 파일 참조가 대부분 INVALID 판정되어 Validation Gate 자체가 False-Negative를 과다 생성함.
근거: 65행 인용 — `근거|https?://|\.ext:\d+`; `.ext`는 리터럴 "ext" 4글자를 매치하는 정규식.

---

[중간] **54–57행 — 본문 내 `[치명]`·`[높음]`·`[중간]` 인용 형태 삽입**
리뷰 포맷 요구사항: "본문 설명·예시·코드블록 안에 `[태그]` 문자열을 인용 형태로 쓰지 말 것 — Validation Gate가 우회로 판정." 54~57행은 서술 문단 안에서 해당 태그를 사용하고 있어, 이 문서를 `/feedback` 스킬로 재처리할 경우 Validator가 오탐할 수 있음.
근거: 54행 인용 — `1. **[치명]** 격리 디렉토리에...`

---

[중간] **49행 — "재현성 낮음" 버그를 실증 없이 완료 처리**
"Claude Sub 한글 깨짐 (CP949 mojibake) — **재현성 낮음**"이라 서술하면서 같은 문장에서 "한 줄로 예방"으로 완료 처리. 재현성이 낮으면 수정 효과도 확인 불가. `~/.claude/rules/bot-deploy.md` Phase 4 기준("실사용 검증 또는 주인님 확인 응답 전엔 '배포 반영됨, 실조회 부탁' 으로 명시")을 준수하지 않은 서술.
근거: 49행 인용 — `재현성 낮음, [Console]::OutputEncoding = UTF-8 한 줄로 예방`

---

[중간] **110행 — "Shipyard '95% 부적합'" 미정의 참조**
"U1 충돌(`CLAUDE.md '무조건 팀'` vs Shipyard '95% 부적합')" — "Shipyard"가 무엇인지 이 문서 어디에도 정의되어 있지 않음. 내일 재개 시 핸드오프 문서로 기능해야 하는데 독자(또는 미래 세션)가 이 판단의 근거를 추적할 수 없음. `HANDOFF.md`에 있다면 이 문서에서 명시적으로 참조해야 함.
근거: 110행 인용 — `U1 충돌(CLAUDE.md "무조건 팀" vs Shipyard "95% 부적합")`

---

[낮음] **83행 — API 비용 "$0.15 추산" 근거 없음**
"스모크 테스트 2회 × 3 CLI = 대략 $0.15 추산" — 모델별 단가, 입력/출력 토큰 수 없이 단순 추산. 재현·검증 불가. 히스토리 기록이 미래 비용 예측에 쓰인다면 최소한 총 토큰 수라도 기재 필요.
근거: 83행 인용 — `API 비용: 스모크 테스트 2회 × 3 CLI = 대략 $0.15 추산`

---

[낮음] **89–91행 — 레퍼런스에 URL 없음**
`nyldn/claude-octopus`, `johannesjo/parallel-code`, Claude Code Task 도구 모두 URL 없이 설명만 기술. 12행에서 "gh API 기반 검증"이라 했으나 검증 시점 커밋 SHA나 URL이 없어 스타 수 포함 사후 검증 불가.
근거: 12~14행 vs 89–91행 — 별도 URL 기재 없음.

---

[낮음] **112행 — "teammate 복원 불가 공식 제약" 출처 없음**
4명 전원 shutdown 결정의 근거로 "세션 재개 시 teammate 복원 불가 공식 제약"을 제시했으나 Claude Code 공식 문서 링크·릴리즈 노트·실측 재현 기록 중 어느 것도 없음. 이 제약이 향후 변경되면 이 기록은 잘못된 전제 위에 서게 됨.
근거: 112행 인용 — `4명 전원 shutdown (세션 재개 시 teammate 복원 불가 공식 제약)`
