파일을 읽었습니다. 분석하겠습니다.

---

## `/feedback` SKILL.md 리뷰

---

### [높음] 유효성 판정 규칙 이중 관리 — Lines 40–41, 84–91

`validate-outputs.ps1`을 SSOT로 선언(84줄)했지만 SKILL.md 40–41줄과 86–91줄에 판정 규칙을 prose로 재서술. 스크립트와 prose가 어긋날 때 LLM은 어느 쪽을 따를지 판단 근거가 없음.

> 인용(Line 84): *"SSOT: scripts/validate-outputs.ps1 의 판정이 최종. LLM은 이 판정을 그대로 수용."*  
> → 바로 아래 86–91줄에서 규칙을 직접 나열. SSOT와 prose 두 곳이 공존하므로 drift 발생 시 충돌.

---

### [높음] 프롬프트와 Validation Gate 정렬 검증 불가 — Lines 40, 62–64

Validation Gate는 "중요도 태그 접두사 1개 이상"을 VALID 조건으로 요구하지만(Line 40), 실제 CLI 프롬프트(`orchestrate.ps1` 내 `$promptLines`)가 태그 형식을 지시하는지 SKILL.md로는 확인 불가. 프롬프트가 태그 형식을 누락하면 3개 CLI 출력 모두 INVALID → `valid_count = 0` → 종합 불가로 시스템이 항상 실패.

> 인용(Line 62–64): *"SSOT: scripts/orchestrate.ps1 Step 3 의 $promptLines 배열이 유일한 원본. 여기에 템플릿 전문 복사 금지."*  
> → SKILL.md에서 프롬프트 내용을 볼 수 없으므로 Validation Gate 조건과 정렬 여부를 독립 검증할 방법 없음.

---

### [높음] `--approval-mode plan` (Gemini) 플래그 실재 확인 필요 — Line 109

```
Gemini: --approval-mode plan — run-gemini.ps1에 고정
```

Gemini CLI(`gemini-cli`)의 공개 플래그 목록에 `--approval-mode` 가 없을 가능성이 높음. 플래그가 무효이면 Gemini는 write 권한을 그대로 갖게 되어 "서브 리뷰 CLI는 읽기 전용" 권한 분리 원칙(104–110줄)이 붕괴됨.

> 근거: Gemini CLI 공식 문서에는 `--yolo`, `--sandbox`, `--debug` 등이 있으나 `--approval-mode`는 확인되지 않음 — 실행 전 `gemini --help | grep approval` 로 검증 필요.

---

### [중간] Step 1 JSON 필드 타입 미명시 — Line 26

```json
{claude_sub, codex, gemini, slug, isolated_dir, source_file}
```

`claude_sub`, `codex`, `gemini` 필드가 **파일 경로**인지 status string인지 명시 없음. Step 2 플레이스홀더("`<claude_sub 경로>`")로만 추론 가능 — LLM이 JSON을 파싱해 `-FilePaths`에 주입할 때 오역 위험.

> 인용(Line 36): *`-FilePaths @("<claude_sub 경로>", "<codex 경로>", "<gemini 경로>")`* — 꺾쇠 플레이스홀더가 JSON 필드명과 다른 표현이어서 대응 관계가 암묵적.

---

### [중간] `근거` 키워드 validation 취약 — Line 91

```
근거 1개 이상 — `근거` 키워드 / URL / `파일.확장자:줄번호` 중 하나
```

`근거` 단순 포함 검사는 "이 지적의 배경과 근거를", "근거리 통신" 같은 비관련 문자열에서도 통과. 실제 citation이 없는 rubber-stamp 리뷰도 VALID 판정을 받을 수 있어 Gate 의미가 희석됨.

> 인용(Line 90–91): `validate-outputs.ps1` 판정 규칙 재서술 부분 — 키워드 매칭 방식의 한계가 스크립트 구현에도 그대로 전파될 가능성.

---

### [중간] Step 2 강제 실행 hard gate 없음 — Lines 31, 44

Step 2 섹션 제목은 "Validation Gate"이지만 "Step 2를 건너뛰면 Step 3 진입 불가"라는 명시적 차단이 없음. LLM이 Step 1 직후 바로 종합을 작성해도 절차상 위반임을 알 수 없음.

> 인용(Line 44): *"valid_count ≥ 1 이면 종합 작성"* — `valid_count`는 Step 2 실행 결과값인데, Step 2 실행 의무 명시가 없으면 전제가 빠진 조건문.

---

### [낮음] 종합 파일명 날짜 이중 삽입 — Lines 46, 78

파일명 패턴: `{YYYY-MM-DD}_claude_{basename}_{yyyyMMdd-HHmmss}-종합.md`
→ 슬러그(`basename_yyyyMMdd-HHmmss`)에 이미 날짜가 포함되어 앞 `{YYYY-MM-DD}`와 중복.

> 인용(Line 78): *"슬러그 = `<대상파일basename>_<yyyyMMdd-HHmmss>` (격리 디렉토리명과 동일)"*

---

### [낮음] 환경 환각 감지 조작적 기준 없음 — Line 92

> *"CLI가 'mojibake/깨짐' 류 지적 → 원본 파일 UTF-8 정상이면 [반박]"*

"mojibake/깨짐 류"가 어떤 문자열 패턴인지 정의 없음. LLM의 판단에 전적으로 의존해 동일 케이스에서 다른 결과가 나올 수 있음.

---

### [낮음] 스크립트 설치 확인 단계 없음 — Line 22

Step 1에서 `orchestrate.ps1`을 바로 호출하지만, 스크립트가 `$HOME\.claude\skills\feedback\scripts\`에 존재하는지 사전 확인하는 절차 없음. 미설치 시 PowerShell 에러만 반환되며 사용자에게 맥락 있는 안내 없음.
