# 종합: `.checklist.md` v2 리뷰 (2026-04-23)

**대상**: `C:\Users\rlgns\OneDrive\문서\Harness-engineering\.checklist.md` (v2, 혼동 제거 + 5차 피드백 Q1~Q3 종결)
**슬러그**: `.checklist_20260423-084406`
**Validation 결과**: 1/3 VALID (Claude Sub만 중요도 태깅 완비, Codex·Gemini는 태깅 누락으로 rubber-stamp 의심 판정 — 단 Codex 내용은 실질적)

---

## 3개 원문 비교 요약

| CLI | 판정 | 치명 | 높음 | 중간 | 낮음 | 특징 |
|-----|------|------|------|------|------|------|
| Claude Sub | VALID | 0 | 2 (H-1·H-2) | 3 (M-1·M-2·M-3) | 2 (L-1·L-2) | 태깅 완비, 파일 내부 일관성 중심 |
| Codex | INVALID(태깅 누락) | - | - | - | - | 태깅 없지만 5건 Findings 실질적 — git stash 위험·롤백 불완전 등 작업 실행 리스크 중심 |
| Gemini | INVALID(태깅 누락) | - | - | - | - | 장점 나열 위주 + 환경 호환 원론. rubber-stamp 성격 |

---

## 판정 (반영 / 유보 / 반박)

### [반영 — 치명급] Codex #1: `.checklist.md` 자체가 git stash로 사라질 위험
- **근거**: 기준선의 `git status --short` 에 `?? .checklist.md` 가 untracked로 잡힘. 작업 항목 0번의 `git stash --include-untracked` 가 체크리스트 자체를 숨김.
- **영향**: Phase 2 진입 직후 작업 근거 파일이 워크트리에서 사라짐. 실행 중 참조 불가.
- **조치**: 0번을 교체 — `.checklist.md` 먼저 `git add` (혹은 tracked 상태로 고정) 후 stash, 또는 stash 대신 **체크포인트 커밋** 사용.

### [반영 — 치명급] Codex #2 + Claude Sub M-1: 글로벌 파일 롤백 백업 누락
- **근거**:
  - Codex: "글로벌 `~/.claude/*` 변경은 `git checkout` 으로 복구 불가. 파일별 사전 백업 필요."
  - Claude Sub M-1: "`~/.claude/skills/.backups/checklist-SKILL-2026-04-17.md` 존재 미실측. 롤백 경로가 실제 없으면 복구 수단 없음."
- **조치**: 작업 항목 0번에 **글로벌 파일 사전 백업** 단계 추가 — `~/.claude/skills/checklist/` (신규라 백업 불필요 대신 삭제로 롤백), `~/.claude/rules/dev-checklist.md` (기존 파일 → `.backups/dev-checklist.md.20260423_HHMMSS.bak` 로 복사). 기준선 표에도 `~/.claude/skills/.backups/checklist-SKILL-2026-04-17.md` 존재 확인 추가.

### [반영 — 치명급] Codex #3 + Claude Sub H-1: venv-guard.sh 사전 확인 순서
- **근거**: 현재 체크리스트는 이동 대상 6개에 `venv-guard.sh` 포함 + 하단에 "살아있을 가능성" 주의 → 실행자가 주의 놓치면 운영 훅을 백업으로 옮김.
- **조치**: 3번 작업을 두 단계로 분리 —
  - 3-a (**선행**): `venv-guard.sh` 참조처 grep (settings.json·다른 스크립트) + 내용 실측으로 dead 여부 확정
  - 3-b: 확정된 dead 5개(또는 6개)만 이동. 실측 결과를 체크리스트에 기록.

### [반영 — 높음] Claude Sub H-2: frontmatter YAML 구분자 누락
- **근거**: `approved: false` 등이 H1 헤더 바로 아래 본문에 혼재. `---` 구분자 없음 → 향후 자동화가 파싱 실패 가능.
- **조치**: 파일 맨 위에 `---` 구분자 블록으로 frontmatter 정규화. 현 SKILL.md 진화본도 이 포맷을 요구하는 흐름과 일치.

### [반영 — 높음] Codex #5: 기존 변경 3건과 최종 1:1 검증 충돌
- **근거**: 기준선에 `docs/history/2026-04-22.md` (수정) + `docs/research/` (untracked) 이 작업과 **무관**하게 이미 있음. 최종 검증 "`git status --short` 가 수정 대상 파일과 1:1 일치" 가 항상 불일치 판정.
- **조치**: 최종 검증을 "**새로 추가된** 변경분만 수정 대상과 1:1 일치" 로 수정. 기준선 파일은 명시적으로 제외.

### [반영 — 중간] Claude Sub M-2: `docs/workflows/dev-checklist.md` 갱신 판단 누락
- **근거**: 연관 문서에 나열됐지만 수정 대상/검증 항목에 없음. 스킬 경로가 바뀌는 작업인데 워크플로 문서 갱신 판단 없음.
- **조치**: 3-a에 "`docs/workflows/dev-checklist.md` Read 후 경로·명칭 불일치 있으면 수정, 없으면 검증 기록 남김" 추가.

### [반영 — 중간] Claude Sub M-3: 작업 항목 4번의 "추가 안 함" + "추가" 혼재
- **근거**: 프로젝트 `CLAUDE.md`에 "프리플라이트 블록 추가 안 함" + "메모 1줄 추가" 가 한 항목에 묶임.
- **조치**: 4번을 두 bullet으로 분리 — (a) 프리플라이트 중복 금지 확인, (b) 스테이징/운영 분리 메모 추가.

### [반영 — 낮음] Claude Sub L-1, L-2: "선택" 기준 명시 + 번호 정합
- **조치**: 3번의 "선택" 제거(실제로는 SSOT 규칙과 의존성 있으므로 필수). v1→v2 표에 작업 항목 번호 명기.

### [유보] Gemini — Win32 경로 호환성 / py vs python 구분
- **근거**: 이미 PowerShell tool 사용 중 + `python -m json.tool` 로컬 검증 완료.
- **사유**: 일반론. 현 체크리스트 v2는 PowerShell 경로로 이미 Windows 기준.

### [유보] Gemini — "글로벌 디렉토리 사전 확인 절차 추가"
- **근거**: 이미 작업 전 기준선에 글로벌 `settings.json`·`hooks/`·`skills/` 실측 포함. 재차 추가 불필요.

### [반박] Gemini — rubber-stamp 성 "매우 완성도 높다"
- **근거**: Codex가 치명급 2건(stash 위험, 롤백 불완전) 지적한 지점을 Gemini는 장점으로 평가. 내부 일관성 위반.
- **사유**: Validation Gate도 rubber-stamp 의심 판정.

---

## Top 3 반영 우선순위

1. **Codex #1 (stash가 .checklist.md 자체를 숨김)** — 치명급. 작업 시작 전 반드시 수정.
2. **Codex #2 + Claude Sub M-1 (글로벌 파일 사전 백업)** — 치명급. 글로벌 rules 파일 손상 시 정확 복구 불가.
3. **Codex #3 + Claude Sub H-1 (venv-guard.sh 선제 실측)** — 치명급. 살아있는 훅을 백업으로 옮길 위험.

---

## 실패 처리
- Codex: **태깅 누락**으로 INVALID. 단 본문 5건 Findings가 실질적이므로 종합에 모두 반영. 향후 개선: `run-codex.ps1` 프롬프트에 "각 지적에 [치명/높음/중간/낮음] 태깅 필수" 명시 강화 필요 (이번 세션에서는 범위 밖).
- Gemini: **태깅 누락 + rubber-stamp 경향**으로 INVALID. 내용은 유보 2건 + 반박 1건으로만 처리.

---

## 체크리스트 v3 작성 필요 사항 (다음 턴)

1. frontmatter `---` 구분자로 정규화
2. 작업 항목 0번 재설계: `git add .checklist.md && git commit -m "chore: checklist start"` 체크포인트 + 글로벌 `~/.claude/rules/dev-checklist.md` 사전 백업
3. 기준선 표에 `~/.claude/skills/.backups/checklist-SKILL-2026-04-17.md` 존재 확인 추가
4. 작업 항목 3번을 3-a (venv-guard.sh 실측) + 3-b (확정 dead만 이동) 로 분리
5. 작업 항목 4번을 4-a (중복 금지 확인) + 4-b (스테이징/운영 메모) 로 분리
6. `docs/workflows/dev-checklist.md` 수정 대상에 조건부 추가
7. 최종 `git status` 검증을 "**새로 추가된** 변경분과 1:1 일치" 로 수정
8. v1→v2→v3 변경 표에 작업 항목 번호 직접 기재
9. 작업 항목 3번의 "선택" 제거 (실제 필수)
