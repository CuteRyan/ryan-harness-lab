리뷰 결과입니다.

---

## 치명

### [C1] 참조 스크립트 전무 — Step 1~2 실행 불가
**SKILL.md:22-23, 34-35, 136-143**

> 근거(실측): 디렉토리 탐색 결과 "SKILL.md 파일만 존재"

6개 스크립트(`orchestrate.ps1`, `validate-outputs.ps1` 등)를 참조하지만 `scripts/` 폴더 자체가 없음. Step 1·Step 2 모두 즉시 실행 불가 — SKILL.md가 단독으로 스킬 역할을 할 수 없는 상태.

---

## 높음

### [H1] 프롬프트 SSOT 분열
**SKILL.md:59-74**

> 인용: "고정 — `orchestrate.ps1`에 하드코딩됨" (L59) / "수정 시 `orchestrate.ps1`의 `$prompt` 변수도 함께 갱신 필요" (L61)

스크립트가 SSOT라면 SKILL.md에 템플릿 전문(L63-74)을 기술할 이유가 없음. 두 소스가 diverge하는 순간 실제 동작(스크립트)과 문서화(SKILL.md)가 달라지며 추적 불가.

### [H2] VALID 조건이 스크립트·LLM 두 곳에 달리 정의됨
**SKILL.md:38-39 vs 94-96**

> 인용(L39): "파일 존재 + 크기 > 0 + 실패 마커 없음 + 중요도 태깅 1개 이상"  
> 인용(L95-96): "중요도 태깅 존재 + **최소 1개 지적에 근거 있음**"

"근거 있음" 조건이 스크립트 기준에는 없고 LLM 기준에만 있음. 스크립트 VALID → LLM이 INVALID 처리하는 케이스 발생 가능.

### [H3] Step 2가 Step 1의 중복 — 설계 근거 불명
**SKILL.md:30-39**

> 인용: "LLM 해석이 아닌 스크립트 판정" (L32)

`orchestrate.ps1`이 이미 3개 실행 결과를 가지고 있으므로, validation을 내부에서 처리하고 `valid_count`를 JSON에 포함시키면 Step 2 자체가 불필요해짐. 현재 설계는 LLM이 Step 1 JSON을 파싱해서 경로를 추출한 뒤 Step 2 스크립트에 전달하는 암묵적 단계가 존재하지만, 이 과정이 절차에 명시되지 않음.

---

## 중간

### [M1] Step 4 인덱스 갱신 주체 불명
**SKILL.md:53-55, L10, L118**

> 인용(L10): "실행 로직은 PowerShell 스크립트가 담당"  
> 인용(L118): "메인 /feedback 실행자: `docs/feedback/`에 파일 저장·인덱스 갱신 가능"

인덱스 갱신이 LLM 직접 Write인지 스크립트인지 불명확. L10 원칙과 L118이 충돌처럼 읽힘.

### [M2] 미완성 Phase 2 설계가 두 곳에 중복
**SKILL.md:130-132, 151**

> 인용(L132): "MVP에서는 미구현 — Phase 2"  
> 인용(L151): "재리뷰 시 이전 피드백 자동 첨부" (Phase 2 목록에도 동일)

"이전 피드백 참조" 섹션이 별도로 존재하면서 미완성임. LLM이 섹션 존재만 보고 구현된 기능으로 오인할 위험. 섹션 삭제 후 Phase 2 목록에만 남기거나, 제목에 `(미구현)` 태그 필요.

### [M3] "유효성 판정" 섹션의 이중 참조 혼란
**SKILL.md:92-100**

> 인용: "## 유효성 판정 (LLM이 종합 시 참조)" (L92)

Step 2에서 스크립트가 이미 VALID/INVALID를 판정하는데, LLM이 종합 시 또 한번 독립적인 VALID 조건을 참조하는 구조. 스크립트 판정이 최종이라면 이 섹션은 "스크립트 결과 해석법"을 기술해야 하는데 실제로는 별개 기준을 재정의함.

---

## 낮음

### [L1] `--permission-mode plan` 쓰기 차단 보장 미검증
**SKILL.md:115**

> 인용: "`--permission-mode plan` (쓰기 차단)"

Plan Mode는 완전 차단이 아닌 억제 수준. 공식 문서 URL 없음. 스크립트 없어 실측 불가([C1] 연계).

### [L2] `--approval-mode plan` (Gemini) 공식 근거 없음
**SKILL.md:117**

> 인용: "Gemini: `--approval-mode plan` — `run-gemini.ps1`에 고정"

Gemini CLI 공식 문서 URL 없음. v4 히스토리에서 검증된 플래그지만 이 문서 내 근거 없음.

### [L3] `-FeedbackDir` 인자 검증 불가
**SKILL.md:28**

> 인용: "`-FeedbackDir` 인자로 저장 디렉토리 재정의 가능"

스크립트가 없어 실측 불가. 문서 레벨에서만 존재하는 기능 명세.

---

## Top 3 반영 우선순위

1. **[C1] 스크립트 파일 생성** — 최소 `orchestrate.ps1` + `validate-outputs.ps1` 없이는 스킬 작동 불가
2. **[H2] VALID 조건 단일화** — 스크립트 기준과 LLM 기준을 하나로 통합, 최종 판정권 명시
3. **[H1] 프롬프트 SSOT 정리** — 스크립트가 SSOT이면 SKILL.md에서 템플릿 전문 제거
