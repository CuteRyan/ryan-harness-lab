---
title: Gemini 리뷰 — Phase A/B/C 구현 결과 (venv 규칙 + VS Code Sync 하이브리드)
type: feedback
reviewer: gemini
date: 2026-04-18
target: 2026-04-18 Day 4 구현 전체
---

# Gemini 리뷰 — Phase A/B/C 구현 결과

## 한 줄 평
"로컬 환경의 기술 부채(deprecated 키)를 청산하고 'Settings as Code'로 전환한 결정적 분기점이며, Codex의 현실론과 Gemini의 구조론이 실용적으로 타협된 우수한 아키텍처."

## 관점별 검토

| 관점 | 방향성 | 우선순위 | 요약 |
|------|-------|---------|------|
| 1. 이전 피드백 반영 | 맞음 | 중간 | 중복 제거·Lock 파일·systemd PATH 등 5/6 달성. `pre-commit-settings-check`만 미구현 |
| 2. 아키텍처 일관성 | 맞음 | 낮음 | CLAUDE.md(진입) → rules(논리) → docs(기록) → memory(상태) 4계층 확립 |
| 3. Settings Sync 하이브리드 | 보완 필요 | 높음 | 수작업(B5) 의존도 높음. 유저가 실수로 Settings 재체크하면 Git SSOT 깨짐. **B7 allowlint 훅이 필수 안전장치** |
| 4. install.ps1 운영 리스크 | 맞음 | 낮음 | ASCII-only + 복사 방식으로 Windows/OneDrive 인코딩·심볼릭 링크 충돌 원천 차단. 백업 체계적 |
| 5. 멀티 기기 확장 | 보완 필요 | 중간 | 현재 Windows 전용(`%APPDATA%`). Linux/서버 위해 `install.sh` + `$XDG_CONFIG_HOME` 추상화 장기 필요 |
| 6. 5차 피드백 보존 | 맞음 | 낮음 | `.backups/` 내 명시적 파일명 보존은 훌륭한 'Suspend' 전략. 차기 세션 즉시 복구 가능 |

## 놓친 리스크 및 제언

### Settings Drift(설정 이탈)
`install.ps1`은 배포만 해줌 — **VS Code UI에서 직접 수정한 설정이 `~/.claude/vscode/settings.json`으로 역전파(Pull)되는 자동화 빠짐** → 주기적 `git diff` 확인 필요. 권장 추가:
- `pull-settings.ps1` (역방향 복사)
- 또는 Git hook으로 주기 검사

### Git Repo 파편화
`~/.claude/vscode/`를 별도 repo로 분리(Q5=A)했으므로, 하네스 프로젝트와 별개로 이 repo의 push/pull 관리가 누락될 수 있음.

## 가장 시급한 TODO 3개
1. **[높음] B7. 금지 키 allowlint 훅 구현** — `terminal.integrated.shell.*` 등 제거된 키가 settings.json에 다시 유입되는지 감시 pre-commit 훅
2. **[높음] Settings Sync 수동 검증** — 가이드(B5)에 따라 Settings 동기화 해제 + `Accept Local` + `Sync Now` 정상 작동 주인님 확증
3. **[중간] 5차 피드백 체크리스트 복원** — `.backups/.checklist.md.승인대기_checklist5차_2026-04-17.md` 활성화하여 중단된 Skill 확장 재개
