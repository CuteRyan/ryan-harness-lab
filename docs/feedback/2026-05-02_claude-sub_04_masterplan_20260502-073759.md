두 파일을 모두 읽었습니다. 이제 `review.md` 규칙에 따라 `04_masterplan.md`를 리뷰하겠습니다.

---

### [치명]

[치명] 04_masterplan.md:546 - 근거: 문서 자체가 "Opus main session이 Agent 호출 시 model 파라미터를 자동 추가 → frontmatter를 덮어쓸 수 있음"을 인정. 동시에 줄 448에서 `CLAUDE_CODE_SUBAGENT_MODEL=sonnet`을 전역 설정. PM=Opus가 이 시스템의 D-4 핵심인데, 환경변수가 이긴다면 PM이 Sonnet으로 실행됨. "대부분 해소" 주장의 실증적 근거(실험 로그, 우선순위 문서) 없음. "실험 후 fallback 확인 필요"(줄 547)로 미결 자인.

[치명] 04_masterplan.md:148 - 근거: α 옵션 PM system prompt 내 `[§3 PM heuristic 표 삽입]` 플레이스홀더가 실제 내용 없이 잔존. Phase 1에서 이 system prompt 그대로 사용 시 PM이 heuristic 기반 워커 선택 불가 — 문서 전체의 핵심 기능이 미완성 상태.

---

### [높음]

[높음] 04_masterplan.md:370 - 근거: `bypass_threshold.max_tool_calls: 10` 판단을 PM이 사전에 어떻게 추산하는지 방법 없음. §부록 반박 3(줄 687)에서 "유효한 우려"로 스스로 인정했으나 해결책 없이 "v0" 레이블만 부착. 이 값이 의미 없으면 PM 게이트 생략 기준 자체가 작동 불가.

[높음] 04_masterplan.md:339 - 근거: §4.1 표에서 ③ 외부 CLI PM 경유 = "**선택**". 반면 줄 391에서 "워커 산출물 최종 검수 필수"가 균형점으로 명시. /feedback = ③의 주 메커니즘(줄 225)이므로 "PM 경유 선택"과 "검수 필수"가 충돌하는지 / 아니면 PM 경유와 실행 자체를 구분하는지 문서에서 불분명 — 독자가 /feedback을 생략해도 된다고 오해 가능.

---

### [중간]

[중간] 04_masterplan.md:553 - 근거: "+90.2% 성능 향상"이 D-4 모델 배분 정책의 핵심 정량 근거인데 출처가 "Anthropic 블로그"로만 표기. URL, 날짜, 실험 조건(데이터셋, 태스크 종류) 없어 재검증 불가. 줄 296–297 표의 동일 출처도 동일 문제.

[중간] 04_masterplan.md:322 - 근거: "④ Pipeline 5단계 ~50×" 수치의 계산 근거 없음. Anthropic 블로그 ~15×(줄 319)와 별개로 추산된 것으로 보이나 도출 방식 미기재. 같은 행 "+1시간+" 시간 추산도 출처 없음.

[중간] 04_masterplan.md:566 - 근거: 가드레일 표에서 `validate-team.ps1`(고아 팀 청소) 참조. 이 스크립트가 실제 존재하는지, 어느 경로에 있는지 문서 내 확인 불가. 출처를 "v2 스펙"으로 표시했으나 v2 스펙 파일(`04_redesign-spec.md`) 내에 해당 스크립트 정의가 있는지 미확인.

---

### [낮음]

[낮음] 04_masterplan.md:218 - 근거: "aws-samples에서 검증된 수치 — 리뷰 실패 3회 초과 시 PM에 에스컬레이션" 인용 시 구체 리포지토리 URL·파일명 없음. aws-samples는 다수의 리포를 포함하므로 특정 수치 추적 불가.

[낮음] 04_masterplan.md:187 - 근거: `isolation: worktree` frontmatter 예시(줄 188–193)가 실제 어느 파일에 저장되고 `Agent` tool에서 어떻게 참조하는지 미설명. 공식 지원이라고 하지만 실제 사용 경로(파일명, 호출 방식) 없어 구현 불가.

---

```
Top 3 반영 우선순위:
[치명] 04_masterplan.md CLAUDE_CODE_SUBAGENT_MODEL=sonnet 전역 설정이 PM model: opus frontmatter를 덮어쓸 수 있음 — 문서 자인, 실증 근거 없이 "대부분 해소" 주장
[치명] 04_masterplan.md PM system prompt 핵심 섹션이 [§3 PM heuristic 표 삽입] 플레이스홀더로 미완성 — Phase 1 구현 시 PM 워커 선택 기능 동작 불가
[높음] 04_masterplan.md bypass_threshold max_tool_calls=10 판단 기준에 PM의 사전 추산 방법 없음 — 게이트 생략 로직 운영 불가
```
