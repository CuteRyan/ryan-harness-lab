ExitPlanMode는 코드 구현 계획에만 사용하는 도구이므로, 이 리뷰 태스크에는 해당하지 않습니다. 리뷰 결과를 직접 출력합니다.

---

`feature.yaml` 리뷰 (`review.md` 규칙 적용):

---

[높음] feature.yaml:51 - 근거: `blocked_by: [frontend-developer, backend-developer]` 필드명이 protocol.steps(line 76) 인용 API 파라미터명 `addBlockedBy` 와 상이 — 구현 레이어가 YAML 필드를 `addBlockedBy` 키로 매핑할 경우 tester 의존 체인이 silently 누락됨

[중간] feature.yaml:20 - 근거: `team_size: 3` 이지만 파일 헤더(line 1) "4명: lead + frontend + backend + tester" — 필드명 `team_size` 는 관례상 총 인원이나 값은 lead 제외 수치(3). 소비자가 주석 없이 필드만 읽으면 lead 누락으로 오계산. `worker_count` 또는 `member_count_excl_lead` 로 개명 권장

[낮음] feature.yaml:73 - 근거: `enforcement: "4-step 강제"` 는 선언뿐, 실제 강제는 외부 hooks(`~/.claude/rules/agent-spawn-model.md`)에 전적으로 위임 — YAML 내 자기-검증 메커니즘 없음. hooks 비활성 시 "강제" 미동작, 오도 가능

[확인 불가] 사유: line 21 `display_mode: tmux` — 이 필드를 소비하는 구현 파일이 같은 폴더에 없고 폴더 재귀 탐색 금지로 확인 불가. tmux 세션·pane 레이아웃·fallback 스펙 미명시

[확인 불가] 사유: lines 6·8·83 외부 commit hash (`wshobson/agents ece811f2`, `aws-samples 67840be3 SKILL.md:65`) — 외부 repo 접근 없이 인용 라인 번호 drift 여부 검증 불가

[확인 불가] 사유: line 19 `pm_lead: pm` — pm agent frontmatter 의 `model:` 명시 여부를 폴더 재귀 탐색 없이 확인 불가 (`agent-spawn-model.md §3` 예외 적용 가능 여부 미판정)

---

```
Top 3 반영 우선순위:
[높음] feature.yaml blocked_by/addBlockedBy 명칭 불일치 → tester 의존 체인 누락 위험
[중간] feature.yaml team_size 필드가 lead 포함 여부 오도 → 하위 소비자 오계산 위험
[낮음] feature.yaml enforcement 선언이 외부 hooks 단독 의존 — hooks 비활성 시 4-step 미강제
```
