이 작업은 파일 읽기만 필요한 분석 작업이므로 ExitPlanMode 없이 바로 리뷰를 출력합니다.

---

[치명] g3_sample.py:11 - 근거: `"SELECT id, role FROM users WHERE name = '" + username + "'"` — username 직접 문자열 연결, `'; DROP TABLE users; --` 등 SQLi 가능 (OWASP A03:2021)

[치명] g3_sample.py:6 - 근거: `ADMIN_PASSWORD = "admin1234"` — 소스코드에 평문 관리자 패스워드 하드코딩, git 이력에 영구 잔존

[높음] g3_sample.py:18 - 근거: `if password == ADMIN_PASSWORD:` — 해시 없는 평문 직접 비교, bcrypt/argon2 미사용 (NIST SP 800-63B)

[높음] g3_sample.py:16 - 근거: `def login(username, password, history=[]):` — Python 가변 기본 인자는 함수 정의 시 1회만 생성됨. 모든 호출이 동일 리스트를 공유해 로그인 시도 사용자명 전체가 누적·노출됨

[중간] g3_sample.py:24 - 근거: `except:` 벌거벗은 except — KeyboardInterrupt·SystemExit 포함 모든 예외를 삼키고 `None` 반환, 실패 원인 추적 불가

[중간] g3_sample.py:21 - 근거: `conn = sqlite3.connect(DB_PATH)` 이후 `conn.close()` 또는 `with` 컨텍스트 매니저 없음 — 예외 발생 또는 정상 반환 양 경로 모두 연결 미닫힘, 파일 핸들 누수

---

```
Top 3 반영 우선순위:
[치명] g3_sample.py:11  username 문자열 직접 연결 → SQLi, 파라미터 바인딩으로 즉시 교체
[치명] g3_sample.py:6   ADMIN_PASSWORD 소스코드 하드코딩 → 환경변수·시크릿 매니저로 분리
[높음] g3_sample.py:16  history=[] 가변 기본 인자 → history=None 후 함수 내부에서 초기화
```
