[중간] security.yaml:1 - 근거: `# security preset ? 보안 감사`에 치환 문자 `?`가 포함되어 의미 구분자가 손상됨
[중간] security.yaml:14 - 근거: `description: 보안 감사 ? SAST`가 같은 줄의 `SAST (정적) → DAST` 표기와 불일치하여 화살표/구분자 인코딩 손상 가능
[중간] security.yaml:68 - 근거: 예시 문자열 `결제 모듈 보안 감사 ? PCI-DSS v4.0 + OWASP ASVS L2`에 치환 문자 `?`가 포함되어 태스크 템플릿 출력 품질 저하
[중간] security.yaml:84 - 근거: `Agent spawn (model 명시 의무 ? pm=opus`에 치환 문자 `?`가 포함되어 필수 조건과 예시 사이 관계가 불명확함

Top 3 반영 우선순위:
[중간] security.yaml 인코딩 치환 문자 제거
[중간] security.yaml description 구분자 정합화
[중간] security.yaml task_template 예시 문자열 복구
