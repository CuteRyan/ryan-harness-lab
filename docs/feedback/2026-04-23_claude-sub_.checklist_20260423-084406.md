파일을 읽었습니다. 리뷰 결과입니다.

---

## 파일 리뷰: `.checklist.md` (2026-04-23, v2)

---

### 치명

없음

---

### 높음

**H-1. `venv-guard.sh` 이동 대상 포함과 "살아있을 가능성" 경고가 모순**

- `.checklist.md:66` — 이동 대상 6개 목록에 `venv-guard.sh` 명시
- `.checklist.md:67` — "venv-guard.sh가 진짜 dead인지 실측 확인 필요 (2026-04-16 정리 리스트에 없음 → 살아있을 가능성)"

이동 대상에 포함한 뒤 주의 메모만 달면, 실행자가 확인 없이 이동할 경우 운영 중인 훅을 `.backups/`로 옮기게 됩니다. 확인 전에는 목록에서 제거하고, 확인 후 추가하는 것이 안전합니다.

---

**H-2. frontmatter가 YAML 구분자 없이 본문에 혼재**

- `.checklist.md:3-11` — `approved: false`, `status: draft`, `mode: mixed` 등이 `---` 구분자 없이 H1 헤더 바로 아래에 위치

승인 규칙(line 165-168)은 "다음 턴에서 `approved: true`로 바꾸고 Phase 2 진입"을 명시합니다. `post-edit-verify.sh`나 향후 자동화가 이 필드를 파싱하려 할 경우, 표준 YAML frontmatter 형식(`---` 구분자)이 없어서 인식 실패합니다. 수동 규약으로 쓰겠다면 이를 코멘트나 섹션으로 명시해야 합니다.

---

### 중간

**M-1. 롤백 경로 `~/.claude/skills/.backups/checklist-SKILL-2026-04-17.md` 존재 미실측**

- `.checklist.md:147` — "~/.claude/skills/.backups/checklist-SKILL-2026-04-17.md에서 복원 가능 (161줄 구버전)"
- `.checklist.md:31-41` — 실측 상태 표에 해당 경로 없음

작업 전 기준선 표에 글로벌 skills 하위 항목이 없습니다. 롤백 경로가 실제 존재하지 않으면 복구 수단이 없어집니다.

---

**M-2. 연관 문서 `docs/workflows/dev-checklist.md`가 수정 대상과 검증 항목 모두에서 누락**

- `.checklist.md:125` — 연관 문서에 "docs/workflows/dev-checklist.md (스킬 상세 문서 — 경로·명칭 변동 시 갱신 대상)"로 명시
- `.checklist.md:102-118` — 수정 대상 파일에 없음
- `.checklist.md:128-137` — 검증 항목에 없음

스킬 경로(`~/.claude/skills/checklist/`)가 이번 작업의 핵심인데, 이 경로를 언급하는 워크플로 문서가 갱신 여부 판단 없이 연관 문서로만 나열되어 있습니다.

---

**M-3. 작업 항목 4번에서 "추가하지 않음"과 "추가" 두 동작이 같은 항목 내에 혼재**

- `.checklist.md:71` — "프리플라이트 블록 **추가하지 않음**"
- `.checklist.md:72` — "명시 메모 1줄 추가"

한 항목 내에 반대 동작이 있어서, 실행자가 프로젝트 `CLAUDE.md`에 무엇을 쓰고 쓰지 않는지 한 번에 파악하기 어렵습니다. 두 항목으로 분리하거나 표로 구분 권장합니다.

---

### 낮음

**L-1. 작업 항목 3번 "선택" 기준이 불명확**

- `.checklist.md:62` — "프로젝트 drift 정리 (선택 + 하한 실측)"

"선택"이라고 했지만 언제 선택하지 않아도 되는지 조건이 없습니다. 판단 기준(예: 다음 세션 이월 가능, 또는 SSOT 규칙 항목과 의존성 있음)을 명시하면 모호성이 줄어듭니다.

---

**L-2. v1→v2 변경 요약 번호가 작업 항목 번호와 불일치**

- `.checklist.md:172-182` — 변경 표 인덱스 1~8
- `.checklist.md:43-92` — 작업 항목 번호 0~8

변경 표의 "4번"이 작업 항목의 "2번"(SSOT 규칙)을 가리키는 식으로, 추적 시 번호를 맞춰보기 어렵습니다. 작업 항목 번호를 변경 표에 직접 기재하면 해소됩니다.

---

**요약**: H-1(venv-guard.sh 이동 대상 사전 확인 필요), H-2(frontmatter 형식)가 실행 전 수정 권장 사항입니다. M-1(롤백 경로 실측), M-2(워크플로 문서 갱신 여부 판단)는 이번 작업 범위에서 명시적으로 처리되어야 합니다.
