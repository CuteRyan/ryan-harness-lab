[높음] feature.yaml:73 - 근거: `enforcement: 4-step 강제 (글로벌 ~/.claude/rules/agent-spawn-model.md)`로 경로가 `~/.claude`를 가리키지만, 현재 지시문은 `~/.Codex/rules/agent-spawn-model.md`를 참조하므로 규칙 파일 경로가 불일치
[중간] feature.yaml:20 - 근거: `team_size: 3`와 같은 줄 주석의 `마스터플랜 표 "4명" 은 lead 포함`이 서로 다른 기준을 함께 사용해 소비자가 `team_size`를 총원/워커 수 중 무엇으로 해석해야 하는지 불명확
[중간] feature.yaml:1 - 근거: `# feature preset ? 기능 개발`처럼 UTF-8 읽기 실측 출력에 `?` 문자가 포함되어 원래 구분 기호 또는 문자가 손상된 상태로 보임
[낮음] feature.yaml:89 - 근거: `backend_only` 설명은 `UI 없는 기능`인데 예시가 `API 신설`을 포함하고, 기본 `task_template` line 64는 `frontend ? backend 병렬 진행`을 전제로 해 변형 적용 시 기본 템플릿과 충돌 가능

Top 3 반영 우선순위:
[높음] feature.yaml 규칙 파일 경로 불일치
[중간] feature.yaml team_size 기준 불명확
[중간] feature.yaml 문자 손상 의심 `?` 출력
