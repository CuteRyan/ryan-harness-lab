---

## 리뷰: `2026-04-22.md`

---

[치명] **메타 리뷰 반영 표 #1이 작업 B 이슈 #1을 해결하지 않음** — `2026-04-22.md:54,61`
작업 B 이슈 #1은 "[치명] 격리 디렉토리에 `scripts/` 없어 리뷰어 혼란"이다. 그런데 반영 표 #1에는 `[Console]::OutputEncoding = UTF-8 추가`가 들어 있다. 이것은 작업 A(line 49)에서 발견된 Claude Sub 한글 깨짐 수정이지, 격리 디렉토리 scripts 누락 해결이 아니다. 즉 "[치명] 버그"로 분류한 이슈가 반영 표에서 사라졌고, 실제로 수정됐는지 알 수 없다. 표의 번호와 섹션 "4건 모두 보완"이 모순된다.

---

[높음] **`validate-outputs.ps1` 정규식 `\.ext:\d+`는 파일:줄 패턴을 실제로 매칭하지 못함** — `2026-04-22.md:65`
`\.ext:\d+`는 리터럴 문자열 `.ext:숫자`를 찾는다. 실제 근거 패턴(`run-claude-sub.ps1:42` 등)은 `\.\w+:\d+` 형태여야 한다. 현 정규식으로는 `.py:`, `.ps1:` 등 실제 파일 확장자가 전부 미검출되어 INVALID 오판 가능성이 있다.

---

[높음] **작업 A 0/3 VALID를 "기능 정상"으로만 해석하고 Phase 2 blocker로 미분류** — `2026-04-22.md:72,75`
작업 A 기준 Codex·Gemini·Claude Sub 전부 INVALID다. 즉 현재 상태로 `/feedback`을 실행하면 100% INVALID 판정이 나온다. "버그 아니고 기능"이라는 해석은 validator 자체의 동작을 설명하는 것이지, MVP 실사용 불가 상태를 정당화하지 않는다. Next 섹션(line 117~123)에 Phase 2 작업은 나열됐지만 "프롬프트 강화 없이 MVP 배포 시 전건 INVALID"를 명시적 blocker로 표시하지 않았다.

---

[높음] **수정 #4가 이슈를 해결하지 않고 닫음** — `2026-04-22.md:57,66`
이슈 #4는 "`$HOME\.claude\skills\feedback\...` 하드코딩 — 다른 CLI 환경 이식 시 문제"다. 반영 표의 수정 내용은 "경로 유지 (Claude Code 설치 경로로 올바름)" + "(해당 없음)"이다. 기술 부채를 인정하고 닫은 것이 아니라 "올바르다"고 주장하며 닫았기 때문에, 향후 bash 버전 병행(line 119) 작업 시 이 하드코딩이 다시 문제가 될 수 있다. 최소한 Known Limitation으로 명시해야 한다.

---

[중간] **SSOT 분열 수정이 단방향만 처리됨** — `2026-04-22.md:55,63`
이슈 #2는 "프롬프트 템플릿이 SKILL.md + orchestrate.ps1 양쪽에 중복"이었다. 수정은 SKILL.md에서 제거하고 orchestrate.ps1 참조만 남기는 것이다. 그러나 orchestrate.ps1의 중복 프롬프트 제거 여부는 표에 없다. orchestrate.ps1이 SSOT라면 해당 파일의 템플릿은 그대로 유지되어야 하지만, 그렇다고 SSOT 분열이 해소된 것은 아니다. SSOT가 어느 파일인지를 이 문서에서 명시하지 않으면 추후 수정자가 혼란을 겪는다.

---

[중간] **TaskCreate/TaskList/TaskUpdate `~/.claude/tasks/` 세션 간 영속 주장이 line 112의 공식 제약과 모순** — `2026-04-22.md:90,112`
line 90: "세션 간 영속 (`~/.claude/tasks/`)"을 기정사실로 기술. line 112: "세션 재개 시 teammate 복원 불가 공식 제약"을 인정. 동일 세션 컨텍스트에서 teammate는 복원 불가인데 Tasks만 영속된다는 근거(공식 문서 URL 또는 실측)가 없다. 근거 없는 내부 동작 주장은 다음 세션에서 TaskList로 재개를 시도할 때 혼란을 유발한다.

---

[낮음] **GitHub 별점 수치에 측정 시점 없음** — `2026-04-22.md:12-14`
`nyldn/claude-octopus 2,777★` 등 실측 수치가 기록됐지만 측정 날짜가 없다. 문서 날짜(2026-04-22)로 추정은 가능하나, 별점은 빠르게 변하므로 "(측정: 2026-04-22)" 형태의 타임스탬프를 붙이지 않으면 나중에 참조 시 유효성을 알 수 없다.
