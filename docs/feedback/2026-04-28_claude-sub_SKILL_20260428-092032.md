리뷰를 작성하겠습니다.

---

## `/feedback` SKILL.md 리뷰

---

[치명] **`valid_count ≥ 1` 임계값이 "교차검증" 핵심 목적과 정면 모순** — `SKILL.md:12` "다른 계열 교차검증이 /feedback의 핵심 목적"이라 명시하면서, `SKILL.md:45`에서 `valid_count ≥ 1`이면 종합 작성을 허용한다. 1개만 성공하면 단일 LLM 리뷰와 구조적으로 동일하므로 교차검증이 성립하지 않는다. 최소 임계값은 ≥ 2여야 목적 달성이 가능하다.

---

[높음] **Gemini `--approval-mode plan` 플래그 미존재** — `SKILL.md:110` "Gemini: `--approval-mode plan`" 지정. Gemini CLI 공식 문서(https://github.com/google-gemini/gemini-cli) 기준 해당 플래그는 없다. Claude Code의 `--permission-mode plan`과 혼용된 것으로 보이며, 미인식 플래그는 CLI가 무시하거나 오류를 내 Gemini가 권한 제한 없이 실행될 위험이 있다.

[높음] **동일 파일 내 SSOT 원칙 자기 위반** — `SKILL.md:63` "프롬프트 SSOT는 `orchestrate.ps1`만"이라고 선언하면서, 유효성 판정 규칙이 `SKILL.md:41`과 `SKILL.md:87-91` 두 곳에 중복 기술되어 있다. 두 섹션 간 미묘한 표현 차이(41줄의 "선택적" 불릿 서술 순서 등)가 스크립트 구현과 어긋날 경우 어느 쪽이 권위 있는지 판정 불가다.

---

[중간] **슬러그 내 날짜가 파일명에서 이중 표기됨** — `SKILL.md:73-74` 파일명 형식이 `{YYYY-MM-DD}_claude-sub_{슬러그}`이고, `SKILL.md:79` 슬러그 정의가 `<basename>_<yyyyMMdd-HHmmss>`다. 실제 파일명은 `2026-04-28_claude-sub_SKILL.md_20260428-092032.md` 형태가 되어 날짜가 두 번 들어간다. `index.md` 파싱 스크립트가 추후 추가될 경우 파싱 기준이 모호해진다.

[중간] **미구현 기능(이전 피드백 참조)이 핵심 플로우 문서에 How-To 형식으로 기술됨** — `SKILL.md:125` "재리뷰 시 과거 지적 첨부"를 구체적 방법("orchestrate.ps1 `$prompt`에 조건부 분기 추가")까지 포함해 서술하면서 "MVP 미구현"으로 끝낸다. LLM이 이 명세를 따를 경우 미구현 경로를 호출하려다 실패하고, 실패 원인을 오케스트레이터 버그로 오인할 수 있다. Phase 2 섹션으로 완전 이동하거나 "현재 동작 없음" 경고 배너가 필요하다.

---

[낮음] **GitHub 이슈 번호가 URL이 아닌 형태로 근거 제시** — `SKILL.md:30` "근거: GitHub gemini-cli #13801"은 `SKILL.md:91`이 정의한 유효 근거 형식("URL / 파일.확장자:줄번호")을 스스로 충족하지 못한다. Validation Gate가 이 파일 자체를 검증한다면 이 줄은 통과 증거로 인정되지 않는다.
