# 2026-07-23 — Day 24: 사용하지 않는 전역 커밋 훅 정리

## 결론

호출되지 않는 `pre-commit-guard.sh`를 활성 영역에서 제거했다. 전역 `git commit` 때 모든 프로젝트에 같은 lint·pytest를 강제하는 방식은 되살리지 않는다.

## 변경과 이유

- 프로젝트의 `hooks/pre-commit-guard.sh`는 Git에서 제거했다.
- Claude 운영본은 삭제하지 않고 `~/.claude/.archive/2026-07-23/pre-commit-guard.sh`로 옮겼다.
- 프로젝트별 검사는 각 저장소의 CI나 자체 pre-commit 설정이 맡는다. 전역 검사는 느리고 프로젝트와 맞지 않는 테스트를 실행할 수 있기 때문이다.
- 과거 설계·히스토리 문서의 파일명 언급은 당시 사실을 보존했고, 전역 검사를 호출하지 않는 회귀 테스트도 유지했다.

## 확인

- 활성 훅·설정의 호출 참조 0건
- Bash 문법 검사 통과
- 문서·Git 안전 회귀 테스트 28건 통과
- 프로젝트와 Claude 운영본의 활성 공통 파일 51개: 누락 0건, 해시 불일치 0건

## Slack 데일리 스킬 전환

- `daily-report`를 Telegram 장문 양식에서 `완료·진행·막힘·내일 핵심` 네 줄 Slack 양식으로 바꿨다.
- **왜**: 실제 팀 공유 형식에 맞추고, 근거 없는 고민이나 장황한 설명을 자동으로 붙이지 않기 위해서다.
- 프로젝트 원본과 Claude·Codex 운영본의 해시가 일치하고 스킬 구조 검사를 통과했다.

## Graphify 제거와 규칙 감사

- Codex 공용 `graphify` 스킬 1,755줄을 `~/.agents/.archive/2026-07-23/graphify`로 옮기고 Python 3.10의 `graphifyy` 패키지를 제거했다.
- `project-structure`의 Graphify 필수 점검과 `todo`의 현재 없는 `TaskCreate` 설명을 제거했다. 깨진 Graphify 가이드 링크도 문서 인덱스에서 뺐다.
- **왜**: Obsidian 사용 흐름과 겹치고, 자동 설치·에이전트 강제·Git 훅 기능이 현재의 가벼운 하네스 방향과 맞지 않기 때문이다.
- rules 8개는 프로젝트와 Claude 운영본의 파일명·해시가 모두 일치했다. 팀 도구·고정 모델·삭제 파일·Graphify의 활성 강제 참조는 0건이고 안전 테스트 28건이 통과했다.
- 후속 확인 필요: Codex 공용 `checklist`는 프로젝트 53줄 대비 177줄, `feedback`은 60줄 대비 286줄로 이전 무거운 운영본이 남아 있다. 팀 강제 제거 뒤에도 `agent-team-manager`가 Codex 전용 스킬로 남아 있다.

## Codex checklist 경량화

- Codex 운영본을 177줄에서 51줄로 줄이고 프로젝트의 선택형 절차와 의미를 맞췄다.
- **왜**: 일반 수정에도 체크리스트 생성·별도 승인·턴 종료·전문 재출력을 강제해 작업을 여러 턴으로 늘렸기 때문이다.
- 이제 사용자가 명시적으로 요청할 때만 실행하며, 2~7개 항목으로 진행과 검증을 짧게 관리한다.
- 기존 운영본은 `~/.agents/.archive/2026-07-23/checklist.before-lighten`에 보관했다. 스킬 구조와 핵심 행동 계약 검사를 통과했다.

## Codex feedback 경량화

- Codex 운영 문서를 286줄에서 60줄로 줄이고 프로젝트의 현재 프롬프트·스크립트 9개와 맞췄다.
- **왜**: 모든 검토에 모델 3개, 통계, 반박, 자기비판, 외부 훅 확인을 강제해 검토 자체보다 절차가 더 무거워졌기 때문이다.
- 기본 `Quick`은 리뷰어 하나만 선택하고, `Full`은 사용자가 여러 모델이나 정밀 검토를 명시한 경우에만 세 리뷰어를 선택한다.
- 기존 운영본 전체는 `~/.agents/.archive/2026-07-23/feedback.before-lighten`에 보관했다.
- 프로젝트·Claude·Codex 실행 리소스 9개 해시 일치, 스킬 구조 검사, PowerShell 문법 검사를 확인했다.

## Codex 팀 관리 임시 스킬 보관

- 222줄짜리 Codex 전용 `agent-team-manager`를 활성 위치에서 `~/.agents/.archive/2026-07-23/agent-team-manager`로 옮겼다.
- **왜**: 현재 없는 `TeamCreate·TaskCreate`, Opus/Sonnet 고정 배분, 모든 산출물의 다중 모델 검수를 전제로 해 현재의 선택형 협업 규칙과 충돌했기 때문이다.
- 활성 `AGENTS.md·rules·skills`에서 팀 도구와 고정 모델 강제 참조가 0건임을 확인했다.
- 프로젝트 원본과 Codex 글로벌 `AGENTS.md` 해시도 일치한다.

## Codex 공용 스킬 최종 감사

- Codex `research-knowledge`를 88줄에서 74줄로 줄였다. 문서마다 역참조·`log.md`·`HISTORY.md` 갱신을 강제하던 이전 절차와 삭제된 훅 전제를 제거했다.
- Codex 운영 스킬 7개에서 현재 지원하지 않는 `trigger·argument-hint·user-invocable` 메타데이터를 제거했다.
- 존재하지 않는 `AGENTS.md.template`을 참조하던 `Codex-md`는 `~/.agents/.archive/2026-07-23/codex-md-broken`으로 옮겼다.
- **왜**: 없는 자산을 새로 만들어 기능을 늘리기보다, 실행 불가능하거나 중복된 전역 기능을 빼는 편이 이번 경량화 목적에 맞기 때문이다.
- 활성 Codex 스킬 10개 구조 검사, 프로젝트·Claude 스킬 파일 32개와 rules 8개 해시, Claude·Codex 글로벌 설정 일치를 확인했다.
- 삭제된 훅·팀 도구·Graphify 강제 참조는 0건이고 안전 회귀 테스트 28건과 `git diff --check`가 통과했다.
- `~/.claude/hooks/lib/__pycache__`의 재생성 가능한 `.pyc` 4개도 제거했다.

## Claude 메모리 정리

- 글로벌 `MEMORY.md`의 rules 목록을 실제 활성 8개에 맞추고, `/feedback`을 기본 리뷰어 1개·명시적 정밀 검토 시 여러 모델로 바로잡았다.
- 폐기된 EC2 참고 문서와 운영본 우선 역방향 동기화 문서는 `~/.claude/memory/.archive/2026-07-23/`으로 옮겼다.
- **왜**: 이번에 없앤 규칙과 무거운 검토 절차가 메모리에 남으면 다음 Claude 세션이 오래된 행동을 다시 따를 수 있기 때문이다.
- 글로벌 원본 3개와 하네스 프로젝트 원본 2개는 각각 날짜별 `.backups/memory-clean-20260723/`에 보관했다.
- 하네스 프로젝트 메모리는 3줄 인덱스와 12줄 요약으로 유지하며, 상세는 `docs/history/2026-07-22.md`와 이 문서를 가리킨다.

## 전역 훅 단일화와 운영 반영

- Bash 전역 훅을 `hooks/pretooluse-guard.ps1` 하나로 통합하고 문서 수정 방식 강제, 특정 서버 배포 검사, Git Bash 래퍼와 사용하지 않는 공통 함수를 제거했다.
- 대문자 Git 명령, 명령 체인, `git -C`와 `--git-dir` 우회를 막았다. 일반 강제 push는 차단하고 `force-with-lease`는 경고한다.
- **왜**: 모든 프로젝트에 같은 문서·배포 절차를 강제하지 않고, 실제 데이터 손실 위험만 전역에서 막기 위해서다.
- 운영 `~/.claude`에 반영하고 기존 훅 5개와 설정을 `.claude/.archive/20260723-215508-hook-lightweight`에 보관했다.
- 원본·운영본 SHA-256 일치, 정책 검사 28건, stdin 통합 검사, 설치·백업 검사, 운영 허용·경고·차단 재현을 확인했다.
- 호출 시간은 기존 약 0.85~0.94초에서 원본 측정 0.39~0.44초, 운영 측정 0.59~0.66초로 줄었다.
- 커밋: `f33f9c6 refactor: simplify global Claude hooks`
