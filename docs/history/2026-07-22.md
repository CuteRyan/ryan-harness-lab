# 2026-07-22 — Day 23: 과정 통제 → 결과 검증

## 결론

주인님이 "하네스가 오히려 성능을 떨어뜨린다"고 지적. 과정을 통제하는 장치를 걷어내고, 되돌리기 어려운 작업의 안전장치만 남겼다.

## 바꾼 것

- **훅 등록 8종 → 2종** — Bash용 검사 3개(커밋 전 검사·문서 보호·배포 검사)를 `pre-bash-guards.sh` 하나로 합쳐 호출당 프로세스 4→1. 사후(PostToolUse) 훅은 전부 은퇴. 배포 검사의 감시 IP를 폐기된 EC2에서 VPS(187.127.123.81)로 갱신하고 rsync도 감지.
- **작업 전 확인 축소** — `/checklist` 의무 해제(선택으로). 사전 승인은 ①되돌리기 어려움 ②외부 발송 ③해석이 갈리는 큰 지시, 세 경우만. PAA 사건의 교훈은 work-style.md에 유지.
- **글로벌 에이전트 35 → 6** — humanize 실행용 6종만 유지. 삭제된 선거 프로젝트 잔재 4종, 회의실 preset 18종, PM 위계·model 강제·agent-team-manager 스킬 은퇴.
- **영어 전환** — 규칙 8개 + 글로벌 CLAUDE.md. 정책: 기계가 읽는 파일은 영어, 사람이 읽는 문서(docs/·히스토리·HANDOFF)는 한국어. (Day 22의 "research-mandatory 한국어 유지"를 뒤집음)
- **settings.json 정리** — 훅 등록부 축소, 삭제된 프로젝트 경로 3건 제거.
- **글쓰기 피드백 반영** — 기록은 결론·변경·이유·확인 4가지로 짧게, 만든 용어 대신 평범한 말, 같은 결정은 한 파일에만. 원문: `docs/claude_harness_writing_feedback_2026-07-22.md`, 반영처: 글로벌 CLAUDE.md Documentation 항목.

## 이유

훅이 도구 호출마다 프로세스를 여러 개 띄워 느렸고(파일 10개 수정이면 훅만 수십 초), 작은 수정도 승인 절차로 여러 턴이 걸렸으며, 지워진 프로젝트의 에이전트가 글로벌에 남아 있었고, 옛 모델 약점을 보완하려던 장치(PM=Opus 강제 등)가 새 모델에는 간섭이 됐기 때문.

## 확인

- 정상 명령은 조용히 통과(exit 0), 가짜 배포 명령(scp → VPS)은 차단(exit 2).
- 스테이징·운영 파일 해시 일치.
- 훅 등록 변경은 **새 세션부터 적용** — 이 작업을 한 세션은 옛 등록으로 동작했음.

## 되돌리기

전부 이동 보관(삭제 아님): 스테이징 `.backups/retire-2026-07-22/`, 운영 `~/.claude/.archive/2026-07-22/`, settings.json 이전본은 git 이력 + `settings/.backups/`.

남은 일과 다음 세션 확인 사항은 `HANDOFF.md` 한 곳에만 기록.

## Codex 재검토 — 7단계 경량화 완료

Claude가 먼저 정리한 상태를 기준으로 Codex가 저장소와 글로벌 운영본을 다시 대조하고 다음 작업을 완료했다.

1. `/checklist`를 57줄의 선택형 절차로 줄이고 글로벌 운영본과 동기화했다.
2. `/humanize` 자동 실행을 없애고, 평범한 한국어와 짧은 답변 원칙을 글로벌 지침에 추가했다.
3. `/feedback` 기본 실행을 검토자 1명으로 줄이고, 여러 모델 검토는 명시적으로 요청할 때만 실행하도록 바꿨다.
4. 삭제된 훅 참조, 오래된 테스트 3개, 사용하지 않는 훅 보조 파일 4개를 정리했다.
5. Bash 훅이 매번 하위 검사 3개를 실행하던 구조를 필요한 검사만 실행하도록 변경했다. 측정 평균은 약 4,230ms에서 898ms로 줄었다.
6. 저장소와 글로벌 운영본을 비교해 공통 파일의 차이를 없애고, 역할이 다른 `CLAUDE.md`, 사용자 설정, 상태줄, 테스트 샘플은 동기화 대상에서 제외했다.
7. 프로젝트 지침, rules, humanize 스킬과 에이전트 문서를 다시 썼다. 대상 문서는 약 1,089줄에서 413줄로 줄었다.

### 이유

하네스가 모든 작업에 같은 절차와 검사를 강제하면서 간단한 명령도 느려졌고, 삭제된 기능의 설명과 프로젝트 전용 규칙이 글로벌 문맥에 남아 있었기 때문이다. 되돌리기 어려운 작업의 안전장치는 유지하고, 나머지는 결과 검증으로 전환했다.

### 확인

- 활성 Bash 훅 문법과 문서 보호 테스트 13건 통과
- 글로벌 운영본 대상 해시 일치, 공통 영역 불일치 0건
- humanize frontmatter 9개와 출력 파일 계약 7종 확인
- 오래된 강제 용어와 참조 검색 결과 0건
- `git diff --check` 통과
- 기존 백업과 사용자별 `settings.json`은 삭제하거나 덮어쓰지 않음

## Claude 독립 검토

Claude Sonnet이 완료 보고를 검토해 4건을 제시했다. 실제 파일 대조 결과는 다음과 같다.

- **반영 1건**: `git push -f` 축약형 차단 누락을 재현하고 수정. 통합 훅 테스트를 추가해 전체 19건 통과, 글로벌 운영본에서도 `exit 1` 확인.
- **반박 1건**: 운영 `settings.json`의 삭제 훅 참조 우려는 재검색 결과 해당 없음.
- **위험 수용 1건**: Git 밖 문서 자동 백업은 지연과 누적 비용 때문에 되살리지 않음.
- **보류 1건**: humanize 실제 모델 실행은 비용 때문에 생략. 다음 명시 실행 때 확인.

상세: `docs/feedback/2026-07-22_claude_global-harness-review.md`

## 한국어 글 다듬기 기본 모드 경량화

- 기본 모드의 `humanize-monolith` 호출을 제거하고 현재 Claude 세션이 `quick-rules.md`만 읽어 직접 처리하도록 변경했다. 정밀 모드의 분리 검토는 유지했다.
- **왜**: 432자 무수정 입력에도 별도 에이전트 호출과 파일 쓰기 충돌로 95초·8턴이 걸렸기 때문이다.
- 1차로 별도 에이전트를 제거하자 67.5초·10턴으로 줄었다. 2차로 기본 모드의 파일 저장 의무를 없애고 채팅에 바로 반환하게 하자 28.9초·4턴으로 줄었고 새 `_workspace` 폴더도 생기지 않았다.
- 최종 결과는 원문의 미래·가능성·목록·기술 용어를 유지하고 `선택해서`를 `선택해`로 한 곳만 다듬었다.
- 프로젝트와 Claude 전역 `SKILL.md`의 SHA-256 일치를 확인했다.

## Codex 전역 지침과 공용 훅 잔여 코드 정리

- Claude 글로벌 정리본과 같은 원칙으로 `settings/AGENTS.global.md`를 만들고 Codex 전역 `AGENTS.md`에 적용했다. 현재 세션에 없는 TeamCreate·TaskCreate와 고정 모델 강제를 제거하고, 사용할 수 있는 협업 도구가 실제로 유리할 때만 쓰도록 바꿨다.
- Codex의 `.codex/rules/default.rules`는 명령 실행 허용 정책이므로 Claude의 행동 지침 `rules/*.md`를 복사하지 않았다. 공통 행동 원칙은 전역 `AGENTS.md`에서만 맞췄다.
- **왜**: PAA의 현재 지침에는 팀 강제가 없는데 Codex 전역 규칙 때문에 모델이 작업을 중단하고 저장소 탓으로 잘못 설명했기 때문이다.
- `_harness_common.sh`에서 활성 호출처가 없는 체크리스트·tiny edit 검사 함수 8개, 168줄을 제거했다.
- Codex 지침과 Claude 공용 훅은 각각 운영본과 SHA-256이 일치했다. Bash 문법 검사와 문서·Git 안전 회귀 테스트 28건이 통과했다.

## 최종 정리

- 완료 백로그 26건 중 최신 10건만 `.todo.md`에 남기고 이전 16건을 `.backups/.todo.done.2026-07-22.md`로 옮겼다. 미완료 9건은 그대로 보존했다.
- 삭제된 `rules/wiki.md`와 `rules/ssot-immediate-sync.md`의 설명 문서에는 현재 적용하지 않는 과거 기록임을 표시했다.
- 활성 공통 파일 52개가 Claude 글로벌 운영본과 일치하고, Claude·Codex 글로벌 지시 원본도 각각 운영본과 일치함을 확인했다.
- 삭제 파일의 활성 참조는 0건, 안전 회귀 테스트는 28건 통과했다. 기존 `HANDOFF.md`는 완료 기록으로 옮겼다.
