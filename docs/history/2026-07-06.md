# 2026-07-06 — Day 22

> **하네스 다이어트.** "하네스가 비대해져 오히려 독이 된다"는 주인님 문제의식 → 현황 파악 후 죽은 것 제거 + 규칙 압축. PM 부장 협의로 계획 검증(반박 우선). 상시 로드 무게 **711 → 240줄 (-66%)**.

---

## §1. 배경

주인님 문제 제기: 하네스 엔지니어링 자체가 자기 강화 메타가 되어 독이 됨. 기존 진단 메모리(`feedback_harness_inflation.md`, "체계화=안전은 잘못된 전제")와 정합. → 새 진단 아닌 **이미 내린 진단대로 빼는 실행**.

## §2. 현황 파악

- Rules 12개 = 517줄 (매 세션 자동 로드) / Skills 16개 = 4,167줄 (graphify 혼자 1,755) / Hooks 파일 19개
- **죽은 훅 4종**: code-doc-sync·doc-template-guard·graphify-reminder·wiki-index-guard (settings.json 참조 0)
- **문서 하네스 서브시스템 유령**: `.harness.yml`/역색인 소비자 전 프로젝트 0건
- **drift**: `ssot-immediate-sync.md`·humanize 3종 운영에만 존재 / `coding.md`·`deployment.md` 스테이징이 옛 버전

## §3. PM 협의 (반박 우선)

`pm` agent(opus) spawn → 계획 검증. 제 최초안(A→C→B, @import 분리)의 **2개 오류 교정**:
1. 순서 A→C→B → **C→A→B** (비-git 운영 삭제 전 git 안전망 먼저)
2. @import 분리는 컨텍스트 절감 안 됨(공식 docs) — 실제 수단은 `paths:` frontmatter or 압축
- PM 신규 발견: `rules/.backups/*.bak.md` 137줄이 매 세션 로드 중

## §4. 실행 (커밋 6개, 전부 git 복구 가능)

| 커밋 | 내용 |
|------|------|
| `72e47b1` (C) | 드리프트 복구 — 운영 전용 ssot·humanize 3종 스테이징 편입 (안전망) |
| `5e6fca7` (A1) | 죽은 훅 4종 삭제 (스테이징→운영) + `.backups` 18파일 rules 밖 이동 |
| `b2a186d` (A2) | `project_harness_architecture.md`(500줄) @import 해제 + ARCHIVED 배너 (삭제 아님) |
| `69f4c6e` | graphify 제거 (주인님 결정: 옵시디언+LLM위키 대체) — 스킬 1,755줄 + 규칙 + 워크플로 문서 |
| `0eb7d2e` (B) | research-mandatory 95→28줄 압축 (표본) |
| `cdb6eb0` (B) | ssot 96→27 · agent-spawn 75→23 · coding 55→10 · deployment 116→17 압축 + coding/deployment 드리프트 해소 |

## §5. 핵심 결정

- **언어**: 규칙 영어 전환 검토 → **취소, 한국어 유지** (주인님 영어 미숙 → 자기 규칙 가독성 우선). 토큰 절감의 본질은 언어 아닌 **길이**.
- **압축 방식**: 규칙 본문 = 명령 알맹이만, 근거·출처·상세 체크리스트 = `docs/rules-appendix/`로 분리 보존 (삭제 아님).
- **graphify_reminder 훅**은 A1에서 이미 제거되어 graphify 제거와 정합.

## §6. 산출물

- 삭제: 죽은 훅 4 / graphify(스킬+규칙+문서) / `rules/graphify.md`
- 아카이브: `project_harness_architecture.md` (@import 해제)
- 압축: 규칙 5개 (437→105줄)
- 신설 부록 4: `docs/rules-appendix/{research-mandatory,ssot-immediate-sync,agent-spawn-model,coding-venv-migration,deployment-checklist}.md`
- 편입: `rules/ssot-immediate-sync.md` + `skills/humanize{,-korean,-redo}` (운영→스테이징)

## §7. 결과 검증

- git clean, 압축 5개 규칙 운영↔스테이징 SHA 전부 MATCH (드리프트 0)
- **상시 로드: 711줄 → 240줄 (-66%)** (CLAUDE.md 57 + 규칙 517 + 백업 137 → 57 + 183)
- 이 프로젝트 추가로 아키텍처 문서 500줄 @import 해제

## §8. 잔여 (안 하기로 했거나 나중)

- 라이브 검증 (Day 21 이월, dast-analyzer prod guard) — 미완, HANDOFF로 인계
- `deployment.md` → 스킬 전환 (실제 배포 작업 시)
- PM 스킬(`agent-team-manager` 417줄) 다이어트 — 주인님 "천천히"

## §9. 마무리 정리 (기록·메모리·인프라)

다이어트 실행 후 세션 종료 작업 (커밋 `de53a9e`·`68d6b81`):
- **히스토리**: 본 파일 신설 + `index.md` Day 22 행
- **메모리 정합·슬림화**:
  - `feedback_harness_inflation.md` 신설 — 기존 끊긴 참조(존재하지 않던 파일) 해소 + 다이어트 원칙 박제 (재부풀림 방지)
  - `MEMORY.md` 옛 Rules 나열(workflow·presentation = 존재X) → 포인터 1줄로 슬림화
  - **인프라 갱신**: `shared-ec2-infra.md`(폐기된 AWS EC2 기준) → `shared-vps-infra.md`(Hostinger VPS, 2026-07-03 단일화). 서버 좌표는 `deployment.md`가 SSOT라 포인터만, 프로젝트별 이전 현황(선창식당 ✅ / 나머지 재구축) 담음
- **HANDOFF**: Day 21 → Day 22 갱신 (다이어트 완료 + 잔여 인계)
- `memory/`는 gitignore → 메모리 파일은 디스크 반영(커밋 외)
