# Day 6 — 2026-04-20 — 하네스 프로젝트 venv/ → .venv/ 실 마이그레이션 + Day 4 체크리스트 종결

## 1. Day 4 체크리스트 실측 종결

**왜**: Day 4(2026-04-18) 시작한 `.checklist.md`(venv 규칙 개정 + VS Code Sync 하이브리드)가 `status: in_progress` 상태로 남아 있었음. 이후 커밋 히스토리(9152f21 등)는 실질 완료를 시사하나 체크박스는 미갱신. Day 6 세션에서 실측 점검으로 종결.

**작업**:
- 실측 기반 체크박스 일괄 `[x]` 처리 — A1~A12, B1~B4, B6, C1~C2, D1~D3 (24개)
- 미완료 3건 명시:
  - **B5 (Settings Sync UI 조작)**: 주인님 수작업 필요 — 이월
  - **B7 (금지 키 allowlint 훅)**: Q4=B 결정대로 후속 세션
  - **C3 (`harness-status.md` 갱신)**: **불필요로 판정 종결** — `~/.claude/memory/`에 해당 파일 자체 부재, 메모리 아키텍처 원칙상 docs 중복 금지
- `.backups/.checklist.md.완료_venv+sync+feedback_2026-04-18.md`로 이관 (삭제 금지 원칙 준수)

## 2. `~/.claude/vscode/` git init (Q5=A 집행)

**왜**: Day 4 결정 Q5=A(단독 repo)였으나 실행 누락. Day 6 세션에서 처리.

**작업**:
- `~/.claude/vscode/.gitignore` 생성 — `_initial_backup/`, `.backups/` 제외
- `git init -b main` + 초기 커밋 `73d8b93`
- 포함: `settings.json`, `extensions.txt`, `install.ps1`, `README.md`, `.gitignore`

## 3. 하네스 프로젝트 `venv/` → `.venv/` 실 마이그레이션

**왜**: Day 4에 글로벌 규칙(`~/.claude/rules/coding.md`)을 `.venv` 표준으로 개정했으나, 이 하네스 프로젝트 자체는 여전히 `venv/` 사용. 규칙 ↔ 실제 불일치 해소.

**사전 확인**:
- `venv/`에 pip + setuptools만 (실제 의존성 없음)
- Lock 파일 일체 부재 (`requirements.txt`, `pyproject.toml`, `poetry.lock` 등)
- 훅 dual-path 이미 지원 (`hooks/post-edit-verify.sh:50-63`, `hooks/pre-commit-guard.sh`)
- 리스크 낮음: 배포 없음, 로컬 전용

**예상 밖 발견 (Phase 1)**:
프로젝트 로컬 `rules/coding.md`·`rules/deployment.md`가 Day 4 개정 이전 버전(구 `./venv/Scripts/python.exe` 참조). 글로벌 `~/.claude/rules/`는 최신, 로컬 미러만 stale. **이번 범위에서 제외** (주인님 Q=B) — 별도 정리 세션으로 분리. 운영 영향 없음 (Claude는 글로벌 읽음).

**실행 (5 Phase)**:
- Phase 1: 실측 재확인 — 의존성/Lock/경로 스캔
- Phase 2: `python -m venv .venv` — Python 3.10.9, 30초
- Phase 3: `.gitignore` 1줄 변경 (`venv/` → `.venv/`), 훅은 dual-path라 변경 불필요
- Phase 4: `.venv` 동작 확인 → 구 `venv/` 삭제 (22MB), 훅이 자연스럽게 `.venv` 선택
- Phase 5: 본 Day 6 히스토리 작성 + index 갱신

## 4. 이월 항목 (Day 7+ 또는 별도 세션)

- **B5** Settings Sync UI 조작 — 주인님 수작업
- **B7** 금지 키 allowlint 훅 — 후속 세션
- **로컬 `rules/` 동기화** — 프로젝트 `rules/coding.md`, `rules/deployment.md` 등이 글로벌 대비 stale. 글로벌 참조 포인터로 축약 또는 최신화 필요
- **VS Code 캐시 refresh** — 주인님 수작업 선택 (`Python Environments: Refresh All Environment Managers`)

## 5. 검증 결과

| 항목 | 결과 |
|------|------|
| `.venv` 생성 | ✅ Python 3.10.9, `sys.prefix` 확인 |
| 훅 dual-path 동작 | ✅ 삭제 후 `.venv`만 남음, find_python 로직 자연 선택 |
| `.gitignore` 반영 | ✅ `venv/` → `.venv/` |
| 구 `venv/` 제거 | ✅ 22MB, `rm -rf` 성공 |
| 체크리스트 1:1 대조 | ✅ P1-1~P5-2 전부 `[x]`, 미완 P4-4(주인님 수작업)만 명시 |

## 6. 교훈

- **프로젝트 로컬 미러가 글로벌과 Sync 안 됨**: `rules/` 같은 디렉토리가 프로젝트에 있으면 글로벌 규칙 변경 시 같이 갱신해야 함. 현재는 수동 — 다음 규칙 개정 때 체크리스트에 "프로젝트 로컬 rules/ 동시 갱신" 항목 추가 고려
- **dual-path 훅 설계의 가치**: `venv/`·`.venv/` 양쪽 지원해둔 덕분에 마이그레이션 중 서비스 단절 없이 자연 전환
- **종결되지 않은 체크리스트 누적**: `in_progress` 상태로 방치된 체크리스트가 다음 작업 시 혼선 유발. 세션 종료 시 실측 종결 루틴 정착 필요

---

# Day 7 — 2026-04-20 (오후) — /feedback 스킬 B옵션 3차 개정 (JSON + 프로필 + Debate Integrity)

## 1. 리서치 (4축 병렬)

**왜**: Day 5 계획대로 기존 오케스트레이션 도구 + MCP 서버 + 한국 커뮤니티 + 공식 플래그 4각도에서 `/feedback` 스킬 개선점 조사. `general-purpose` 서브에이전트 4개 병렬.

### 발견 요약
- **기존 플러그인 5개** (claude-octopus, myclaude, Claude-Code-Workflow, pal-mcp-server, claude_code_bridge): Top 3 차용 후보 = (a) Debate Integrity Rules 프리앰블 (b) JSON 스키마 강제 태깅 (c) JSONL 파싱 실패 fallback
- **MCP 서버 5개** (PAL, MCO, multi_mcp, ai-cli-mcp, codex-mcp-server): **전면 도입 오버엔지니어링** — 한글 경로 CP949 문제는 Codex 바이너리 버그(#14311)라 MCP 래퍼로 해결 불가. 현재 CLI 직접 호출 유지 + 결과 구조화만 차용
- **한국 커뮤니티**: CCG 역할 분담(Codex=백엔드, Gemini=1M 컨텍스트, Claude=종합) 이미 대세. 주인님 구조와 일치. oh-my-claudecode는 WSL2 권장이라 PowerShell 환경 직접 이식 어려움
- **공식 플래그 스캔**: Codex `--json` **실측 없음** (리서치 에이전트 오보 — 공식 문서만 보고 실측 안 함), 대신 `--output-schema <FILE>`로 JSON 스키마 강제 가능. `-p <profile>`, `-c key=value`도 유효. Gemini `-o json` 실측 확인

## 2. 주인님 결정 — B옵션 (중간 패치)

A(최소), B(중간), C(전면) 3안 중 **B 선택**:
- A. 줄글 → JSON (즉시 이득)
- B. A + Debate Integrity 프리앰블 + `[profiles.feedback]` 프로필 이주
- C. B + JSON 스키마 강제 convergence score + JSONL fallback (보류 — 실전 3~5회 후 재판단)

## 3. 실측 검증 3건

| 항목 | 결과 | 관찰 |
|------|------|------|
| V1 Gemini `-o json` | ✅ | 출력 `{session_id, response, stats: {models, tools, files}}`. `response`에 본문, `stats`에 토큰(14,367) 자동. IDE 경고 1줄 prefix |
| V2 Codex `--output-schema` | ✅ (스키마 수정 1회 후) | strict mode로 모든 `object`에 `additionalProperties: false` 필수. 여러 중간 JSON 뱉음 → 마지막 유효 JSON 채택. **스키마 강제가 "채워넣기 환각" 유발** → `minItems: 0` + `error_note` 필드 추가로 완화 |
| V3 Codex `-p feedback` 프로필 | ✅ | `model_reasoning_effort = "high"` 오버라이드 확인. **토큰 21,248 → 2,483 = 약 8.5배 절감** (xhigh → high) |

## 4. 반영 (A + B + C + D)

### A. Gemini JSON 전환
- `~/.claude/commands/feedback.md` Phase 2 Gemini: `-o text` → `-o json`
- Phase 6.1 파싱 절차: 첫 `{`부터 추출, `response` 키 본문, `stats` 메타 기록

### B. Codex 프로필 + output-schema
- `~/.codex/config.toml`에 `[profiles.feedback]` 5줄 **append** (기존 37줄 전부 보존, diff로 확인)
  - `model = "gpt-5.4"`, `model_reasoning_effort = "high"`, `approval_policy = "never"`, `sandbox_mode = "read-only"`
- `~/.codex/schemas/feedback-review.json` 신규 — findings/top3/error_note 구조, strict mode 준수
- Phase 2 Codex 명령: `codex.cmd exec -p feedback --skip-git-repo-check -C <workdir> --output-schema <path> --add-dir <dir> "<프롬프트>" </dev/null`
- `</dev/null` 추가: `codex exec`가 stdin도 대기하는 동작 방지

### C. Debate Integrity 프리앰블 (Phase 3 고정 꼬리)
2줄 → 5규칙으로 확장: no-explore / anti-contrarian / anti-rubber-stamp / evidence-based / proportional. 각 규칙에 실측 근거 주석:
- no-explore ← 2026-04-19 `--add-dir` 재귀 스캔 1.6MB 폭주 직접 차단
- anti-contrarian / anti-rubber-stamp ← Day 5 그룹싱크·동반 환각 리스크
- evidence-based ← Phase 6.3 "공통 지적 자동 반영 금지"의 CLI쪽 거울
- proportional ← 스키마 강제 "채워넣기 환각" 완화

### D. Phase 6 파싱 절차 재구조화
- 6.1 파싱 (JSON 우선, 폴백은 원문)
- 6.2 폴백 체크
- 6.3 Claude 검토 + **"스키마 강제 환각 탐지" 항목 신설** (rationale이 일반 템플릿이면 `[유보]` 우선)
- 6.4 주인님 보고 구조에 **"호출 메타 (토큰 + error_note)" 섹션 추가**

## 5. 검증 결과

| 항목 | 결과 | 메모 |
|------|------|------|
| V4 프리앰블 실 호출 | 부분 ✅ | `reasoning effort: high` 로드 ✓, no-explore 작동 ✓, `findings: []` 빈 배열 허용 ✓. 단 한글 파일 내용 CP949 깨짐 상황에서 `error_note`를 비워둠 → 다음 개정에서 "읽기 실패 시 error_note 필수" 보강 필요 |
| V5 config.toml 보존 | ✅ | diff로 5줄 append만 확인. 기존 37줄 전부 그대로 |
| V6 스킬 줄 수 | ⚠️ 초과 | 목표 200줄, 실제 **217줄** (+17). 추가분은 전부 실측 근거 주석·JSON 구조 설명·환각 탐지 절차 — 품질 가드 성격이라 실용적으로 허용 가능하나 MVP 원칙에서 17줄 초과 |

## 6. 보류 항목 (C안으로 이월)

- JSONL 파싱 실패 fallback (pal-mcp-server `_recover_from_error` 차용) — Gemini JSON 도입만으로 1차 안정화 충분, 이슈 발생 시 추가
- convergence score (Claude-Code-Workflow) — 수치형 합의 판정, 실전 누적 후 필요성 재평가
- oh-my-claudecode 아티팩트 저장 패턴 — 현재 `docs/feedback/` 구조로 충분, 향후 리뷰 비교 필요 시 도입

## 7. 남은 리스크 (다음 세션)

- **Codex 한글 파일 내용 CP949 깨짐** — 영문 workdir로 경로 해결했으나 파일 내용 자체는 여전히 깨짐 (Constrained Language 환경에서 `Get-Content` CP949 fallback). `--output-schema`의 `error_note` 필드가 이 상황을 기록해야 하는데 실측상 Codex가 비워둠 → 프리앰블에 "파일 읽기 실패 시 error_note 필수 기입" 1줄 추가 검토
- 토큰 경제성: V4에서 `high` + `--output-schema` 조합이 20,330토큰 — V3(스키마 없음, 짧은 요약) 2,483 대비 8배. 스키마 파싱 오버헤드 실측 필요
- 스킬 줄 수 217 — 다음 개정에서 Phase 2 "왜 5규칙인가" 블록(9줄) 또는 config.toml 설명 압축 검토

## 8. 변경 파일

- `~/.claude/commands/feedback.md` — Phase 2 Codex/Gemini/공통, Phase 3 고정 꼬리, Phase 6 전반 (189줄 → 217줄, +28)
- `~/.codex/config.toml` — `[profiles.feedback]` 섹션 append (37줄 → 42줄, +5)
- `~/.codex/schemas/feedback-review.json` (신규) — JSON Schema, findings/top3/error_note
- 백업: `feedback.md.bak-2026-04-20`, `config.toml.bak-2026-04-20`
- `docs/history/2026-04-20.md` (본 파일, Day 7 섹션 append)
- `.checklist.md` — 상태 `in_progress` → `completed`로 종결 예정

---

# Day 7 (오후 연장) — 2026-04-20 — dogfood 실패 → Claude Sub 도입 → 제로베이스 재설계 제안

## 1. dogfood 6회 시도 (Codex/Gemini)
**왜**: B옵션 3차 개정 검증을 자기 스킬로 dogfood.

**실패 계보** (6회):
1. `--output-schema` + 5규칙 + 3파일 — sandbox `Get-ChildItem -Recurse` 차단
2. 프롬프트 간소화, approval-mode 제거 — `--output-schema` early return
3. Day 5 방식 줄글 회귀 — "파일 찾겠습니다"만 말하고 tool use 0회
4. 절대경로 명시 — bash→.cmd 멀티라인 인자 잘림
5. stdin 파이프 — reasoning만 하고 종료
6. **단일 파일 + 짧은 프롬프트**: Codex는 유효 지적 5개 성공(환각 1 포함), Gemini는 rubber-stamp

**교훈**: 오늘 개정이 이론상 완벽하지만 실측에서 fragile. Windows 환경 + 복합 리뷰에서 두 CLI 모두 fragile.

## 2. Claude Code 서브인스턴스 도입 실측
**왜**: 주인님 제안 — 새 터미널에서 `claude` CLI 호출. Codex/Gemini 대안.

**실측** (`claude -p --output-format json --model sonnet --permission-mode plan --allowed-tools "Read,Grep,Glob" --no-session-persistence`):
- **1회 성공** (재시도 1회는 `--bare` 로그인 이슈로 폐기)
- duration 117초, cost $0.20 (환산치 — Max 구독은 한도 내 커버)
- **유효 지적 7건** (치명 1, 높음 3, 중간 3, 낮음 2)
- 환경 이슈 **0건**, 환각 **0건**, 파일:줄 정확도 100%

**Claude Sub 단독 발견** (Codex/Gemini 미발견 4건):
- **[치명]** Phase 6.1 "마지막 유효 JSON" 유효 JSON 0개 케이스 미처리 (feedback.md:164)
- **[높음]** `findings: []` + `error_note: ""` 조합이 빈 결과로 정상 통과 (환각 탐지 구멍)
- **[높음]** Phase 5 / Codex 스키마 / Gemini JSON 삼중 포맷 불일치 (SSOT 없음)
- **[높음]** `--output-schema <FILE>` 동작 모호 (읽기/쓰기 미명시)

## 3. Windows vs 리눅스/맥 분석 (주인님 질문)

| 원인 | Windows 고유 | 리눅스/맥 |
|------|:-:|:-:|
| Constrained Language Mode | ✅ | ❌ |
| CP949 locale | ✅ | ❌ |
| Bash → .cmd shim 잘림 | ✅ | ❌ |
| Codex `--output-schema` early return | ❌ | ✅ 동일 |
| Gemini `--approval-mode plan` 대체 도구 미사용 | ❌ | ✅ 동일 |

**결론**: 오늘 실패의 약 60%는 Windows 고유. 리눅스/맥 이주 시 ~3회로 감소 예상. 단 Codex/Gemini 고유 제약은 OS 무관.

## 4. Claude Max 비용 확인 (주인님 질문)

- `total_cost_usd: 0.20`은 "API로 썼으면 들었을 환산치"
- `ANTHROPIC_API_KEY` UNSET 확인 → Max 구독 OAuth로 동작 중
- 실제 과금은 Max 한도 내 커버 (초과하지 않는 한 0원)

## 5. 제로베이스 재설계 제안서 작성

`docs/feedback/drafts/2026-04-20_proposal_feedback-skill-rewrite.md` (v4 draft):
- **아키텍처**: Tier 1 (Claude Sub 기본) + Tier 2 (Codex/Gemini 설계 결정 시만)
- **원칙**: 안정성 > 다양성, 단일 호출 > 복합 병렬, SSOT 명확화, 실패 폴백 명시
- **Phase 구조**: Phase 0(프리플라이트) ~ Phase 7(저장) 총 7단계
- **6규칙** (5규칙에서 Rule 6 "explicit-limits" 분리, Claude Sub 낮음 지적 반영)
- **이관 계획**: Phase A(실험 1주) → Phase B(교체 1주) → Phase C(안정화)
- **미결정 5건** + **리스크 4건** 명시

## 6. Day 7 오후 연장 변경 파일

- `docs/feedback/2026-04-20_codex_feedback스킬-B옵션-3차개정.md` (원문)
- `docs/feedback/2026-04-20_gemini_feedback스킬-B옵션-3차개정.md` (rubber-stamp)
- `docs/feedback/2026-04-20_claude-sub_feedback스킬-B옵션-3차개정.md` ($0.20, 7지적)
- `docs/feedback/2026-04-20_claude_feedback스킬-B옵션-3차개정-종합.md` (Claude 판정)
- `docs/feedback/index.md` (Day 7 항목 + Claude Sub 포함)
- `docs/feedback/drafts/2026-04-20_proposal_feedback-skill-rewrite.md` (v4 재설계 제안)
- `docs/history/2026-04-20.md` (본 파일, Day 7 오후 연장 섹션)
- `docs/history/index.md` (진행 중 섹션 갱신)

## 7. 다음 세션 진입점

**주인님 결정 대기**:
1. `proposal_feedback-skill-rewrite.md` 제안서 검토 → 승인/수정/기각
2. 승인 시 Phase A 실험 시작 (1주) — `feedback-v4.md` draft 작성
3. 일단 현재 `feedback.md`는 유지 (롤백 안전망)
