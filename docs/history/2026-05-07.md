# 2026-05-07 — Day 21

> **Day 21 turn 1**: #028 (a) 라이브 검증 PASS — 옵션 E v2 helper (subagent_lookup) 신설로 stdin agent_type 의미 결함 결정적 해결.

---

## §1. 사전 확인 (메인 재시작 후 새 세션)

| # | 항목 | 결과 |
|---|------|------|
| 1 | env `CLAUDE_CODE_SUBAGENT_MODEL` 부재 | ✅ ABSENT (메인 재시작 효과 발효) |
| 2 | git status clean + 마지막 commit | ✅ `524590f` (Day 20 turn 12 후속) |
| 3 | settings.json PreToolUse 7 matcher 등록 | ✅ Bash·Edit·Write·MultiEdit·Task\|Agent·WebSearch\|WebFetch·WebFetch |
| 4 | HANDOFF Read + `/handoff done` (소멸 정책 18회차 검증) | ✅ `.backups/HANDOFF.done.2026-05-07.md` |

## §2. 사용자 결정 (HANDOFF §🚨 3건)

| # | 결정 | 선택 | 추천 |
|---|------|------|------|
| 1 | R-21 등록 시점 | **B (1회 더 dogfood)** | A 즉시 |
| 2 | #028 진입 시점 | **A 즉시** | A 즉시 |
| 3 | #028 잔여 GAP 처리 순서 | **PM 권장 (a→b→d→c·e·f)** | 동일 |

## §3. PM 협의 (Agent Teams dogfood 3회차)

- TeamCreate `phase2-live-verify-day21-turn1` + Agent spawn `pm-부장` (model=opus) + 4-step 프로토콜
- PM 추천 (R-19 외부 사실 인용 영역 = 5 출처 직접 인용):
  - (a) 라이브 검증 양식 = **PASS with REVISE 1건** (PM hook 트리거 = `tool_input.query/url/prompt` 합산 검사, spawn prompt 자체 X)
  - (b) 키워드 외부 파일화 = **PASS** (sycophancy-keywords.txt 양식 차용 + fallback)
  - (d) Bash matcher 확장 = **옵션 B 추천** (D-29 정합) + R-22 잠정 (`hooks/lib/dast_url_check.py` helper 추출 = DRY 동시 만족)
- 5 출처: Anthropic Hooks docs + bash_command_validator example + Issue #4669 + SIG Maintainable Security + OWASP DAST/TurboGeek
- R-20 자기비판 PASS (2 sub-bullet)

**사용자 컨펌**: (d) 옵션 B + R-22 helper 추출 / (a) PM·DAST 직접 SendMessage 패턴 동의

## §4. 라이브 검증 진단 (라운드 5회)

### 라운드 1·2 (옵션 E 적용 전)
- verifier-2 spawn × 2 (PM + DAST) → 4 검증 中 3 FAIL (silent pass)
- 사전 확인 PASS 인데 차단 미발생 = critical 발견

### 라운드 3 (디버그 라인 추가, stdin JSON 결정적 캡처)
- 디버그 라인 = `os.write + fsync` 명시 (with 블록의 buffered write 가 hook subprocess 빠른 종료 시 손실)
- stdin 캡처 결과:
  ```json
  {"agent_id": "ab8f82bb6690ef214", "agent_type": "dast-verifier-3", "tool_name": "WebFetch", ...}
  ```
- **근본 원인 발견**: `agent_type` 필드 = **spawn 시 `name` 파라미터** (임의값) ≠ frontmatter `name` ("dast-analyzer")
- 검사 분기 `agent_type in DAST_AGENTS` 항상 False → silent pass

### 옵션 5종 사용자 결정
- A 컨벤션 강제 / B pattern 매칭 / C 다중 정확 매칭 / D 분기 제거 / **E transcript/team config 파싱**
- **사용자 결정 = 옵션 E** (추천 = B+A 하이브리드 였으나 E 가 robust + false positive 0)

### 라운드 4·5 (옵션 E 구현 + 라이브 검증)
- helper `hooks/lib/subagent_lookup.py` 신설 (~70줄)
- v1 (agent_id 매칭만) → 라이브 FAIL (stdin `agent_id` ≠ team config `agentId`, 별도 식별자)
- v2 (name 매칭 fallback 추가) → 단위 테스트 3/3 PASS → 라이브 4/4 PASS

### 라이브 검증 결과 4/4 PASS

| 검증 | URL/Query | 결과 |
|------|-----------|------|
| 1 | WebSearch query="HANDOFF.md 양식 best practices..." | ✅ 차단 (`matched_keywords=['HANDOFF']`) |
| 2 | WebSearch query="Anthropic Claude Code SDK..." | ✅ 통과 |
| 3 | WebFetch url="https://api.example.com/users" | ✅ 차단 (`matched production pattern: ^https?://api\.[\w-]+\.(com\|io\|net\|co\.kr)/`) |
| 4 | WebFetch url="https://staging.api.example.com/users" | ✅ 통과 via exclude |

## §5. 결정·부수 발견

- **D-30 신설**: 옵션 E (Team config name 매칭) 정책 = `subagent_lookup` helper 신설. stdin agent_type 의 의미는 spawn name (Team 컨텍스트) 이지 frontmatter name 아님 → Hook 의 subagent_type 검사는 반드시 helper 통해.
- **R-23 잠정**: "stdin agent_type 의 의미 = spawn name (Team 컨텍스트 한정). Hook 의 subagent_type 분기 = `subagent_lookup(agent_id, spawn_name=raw_agent_type)` 의무." (3차 helper fallback 패턴 = 1차 agentId 매칭 + 2차 name 매칭 + 3차 stdin agent_type fallback)
- **R-15 정합 부수 발견 (#029 후속)**: 차단 메시지 한글 본문 CP949 깨짐. PYTHONIOENCODING=utf-8 + `sys.stdout = io.TextIOWrapper(buffer, encoding='utf-8')` 이중 보장 필요.
- **PM 협의 dogfood 4회차** = R-21 잠정 누적 (정식 등록 = 1회 더 dogfood 후, 결정 1=B 정합).
- **사용자 retrospective 점검** (turn 中 1회) = 7 결정 중 6 가 합리적, 결정 6 (옵션 E vs 내 추천 B+A) 은 사용자 안이 우월. 진척도 1/6 (Step 1 a 단독) = critical 결함 발견의 자연 결과.

## §6. 산출물

- 신설: `hooks/lib/subagent_lookup.py` (~70줄)
- 수정: `hooks/pretooluse-pm-research-guard.py` + `hooks/pretooluse-dast-prod-guard.py` (subagent_lookup 호출 + 디버그 라인 신설→제거 cycle)
- 운영 sync 3쌍 SHA256 MATCH (helper + 양 hook)
- `.todo.md` #028 (a) 完 + #029 신설 (R-15 후속)
- `.checklist.md` Step 1 完 → archive
- 본 history 신설 + index.md 본 행 신설

## §7. 잔여 (다음 turn)

- **#028 (d) Bash matcher 확장** — 옵션 B + R-22 helper 추출 (`hooks/lib/dast_url_check.py` + `pretooluse-dast-prod-guard-bash.{sh,py}`, 큰 작업)
- **#028 (f) R-21 정식 등록** — dogfood 4회차 누적, 1회 더 후 (결정 1=B 정합)
- **#029 R-15 후속** — 양 hook 한글 메시지 UTF-8 강제
- **R-23 정식 등록** — 옵션 E + name 매칭 fallback 정책, dogfood 1회차 누적
- **R-24 신설 의무** (재발 방지, 다음 turn 즉시 적용) — "신설 hook/스킬 = 공식 docs 사전 fetch + 라이브 형식 단위 테스트 의무"

## §8. 사용자 분노 + 추가 작업 (turn 中반)

사용자 시간 多 소비 + 효율 저하 분노 표명. 솔직히 인정:
- 본질 코드 = 30분 작업을 4배로 늘림 (~2시간)
- 원인: ① 사전 docs 미확인 (turn 12 신설 시 + 본 turn 시작 시) ② 양식 비대화 (PM 협의 dogfood 4회차 + verifier spawn 8건 + R/D 누적) ③ iterative 진단 cycle 자가 누적
- 재발 방지 = R-24 신설 의무 + R-25 잠정 (양식 가지치기 turn 정기화)

사용자 옵션 B 결정 = 가벼운 3건 (b·c·e) 즉시 처리 진입 → b·c·e 完 + 단위 테스트 2/2 PASS + 운영 sync 4쌍 MATCH.

### §8 산출물 (b·c·e 추가)
- 신설: `hooks/data/pm-research-guard-keywords.txt` (sycophancy-keywords.txt 양식 차용 = 9 키워드)
- 수정: `hooks/pretooluse-pm-research-guard.py` (외부 파일 로드 + fallback 로직)
- 수정: `hooks/pretooluse-dast-prod-guard.py` docstring (namesilo URL 박음 + Day 21 turn 1 PASS 사실)
- 수정: `presets/security.yaml` (namesilo URL + 직접 인용 + Day 21 turn 1 PASS 사실)
- 운영 sync 4쌍 SHA256 MATCH

/checklist mode=mixed 14건째, 양식 v2 dogfood 22건째, PM 협의 dogfood 4회차, R-19 dogfood 3회차, R-20 dogfood 6회차.
