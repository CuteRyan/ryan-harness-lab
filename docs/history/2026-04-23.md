# Day 10 — 2026-04-23

## 요약

**/checklist 혼동 제거 + 5차 피드백 Q1~Q3 종결**. 주인님의 한 마디 지적("글로벌에 스킬 지시는 있는데 실제 파일이 없어서 혼동")에서 출발해 원인을 실측하고, 2026-04-17부터 6일간 보류됐던 결정(Q1·Q2·Q3)을 종결. **자체 비판 1회 + `/feedback` 3 CLI 교차 리뷰 1회** 로 체크리스트를 v1→v2→v3까지 3단 개선한 뒤 Phase 2 집행.

## 배경

- **주인님 지적**: "체크리스트 스킬이 있다고 계속 다른 프로젝트에서 그렇게 혼동하는데, 클로드코드가 이거 글로벌 메모리에 있어서 그렇지??"
- **원인**:
  - 글로벌 `~/.claude/CLAUDE.md` 프리플라이트 + `~/.claude/rules/dev-checklist.md` 는 `/checklist` 스킬을 참조
  - 그러나 `~/.claude/skills/checklist/` 폴더는 **비어있음** (2026-04-17에 `.backups/` 로 이동됨)
  - Claude Code는 **글로벌 스킬만 자동 로드** → 프로젝트의 진화본(`Harness-engineering/skills/checklist/SKILL.md`)은 세션에서 보이지 않음
  - 2026-04-17 당시 자체 리뷰에서 "부차 E1"로 이미 인지했으나 **주인님 결정 대기(Q1~Q3) 로 6일 보류**

## 결정 반영 (Q1~Q3 종결)

| 질문 | 결정 | 조치 |
|------|------|------|
| **Q1** 훅 vs 스킬 | **A — 훅 제거, 스킬만 유지** | 프로젝트 `settings/settings.json` 에서 dead hook 4개 참조 제거 (글로벌 settings는 04-16 정리 때 이미 제거 확인) |
| **Q2** 스킬 로드 범위 | **A — 프로젝트 = 스테이징, 글로벌 = 운영** | 프로젝트 93줄 진화본 → `~/.claude/skills/checklist/SKILL.md` 복원 (SHA256 MATCH). rules/dev-checklist.md 에 SSOT 섹션 신설 |
| **Q3** tiny edit 기준 | **보수 유지 (3줄/240자)** | 별도 조치 없음 — 현 기준 유지 |

## 워크플로 (자체 비판 → /feedback → 3단 개선)

이번 세션의 체크리스트는 **3번 개정**됐습니다:

1. **v1 초안**: Q1~Q3 반영 + 5작업
2. **자체 비판 검토 → v2**: 6건 보완 (SSOT 규칙 부재, 이월 장치 부재, 롤백 git 미명시, post-edit-verify 정합 미점검, Read/grep 증거 의무, 프리플라이트 애매) + 실측 교정 (수정 타깃이 프로젝트 drift 쪽이었음을 발견)
3. **/feedback 3 CLI 교차 리뷰 → v3**: 치명 3건 + 높음 2건 + 중간 2건 + 낮음 2건 반영
   - 치명: ①`.checklist.md`가 `git stash` 로 사라질 위험(Codex #1), ②글로벌 파일 사전 백업 누락(Codex #2/Claude Sub M-1), ③venv-guard.sh 선제 실측 필요(Codex #3/Claude Sub H-1)
   - Validation: 1/3 VALID (Claude Sub 태깅 완비 / Codex·Gemini 태깅 누락 → /feedback 스킬 보완 과제로 이월)

관련 파일:
- 체크리스트: `.checklist.md` v3 (작업 완료 후 `.backups/` 이동 예정)
- 종합 리뷰: `docs/feedback/2026-04-23_claude_.checklist_20260423-084406-종합.md`
- 5차 피드백 종결: `.backups/.checklist.md.종결_Q1~3결정반영_2026-04-23.md`

## Phase 2 실행 내역

### 실측으로 드러난 중요 발견 (v2 → v3 에서 교정)

| 항목 | v2 가정 | 실측 결과 |
|------|---------|-----------|
| 글로벌 `settings.json` | dead hook 제거 대상 | **이미 깨끗** (04-16 정리 완료, 추가 조치 불필요) |
| `doc-doublecheck-guard.sh` | dead 후보 6개 중 1개로 이동 예정 | **ALIVE** (PreToolUse/Bash 운영 중) → v3에서 이동 제외. v2 그대로 갔으면 운영 훅 손실 |
| `~/.claude/rules/.backups/dev-checklist.bak.md` | 롤백용으로 사용 | **04-11 drift 상태** → 신규 타임스탬프 백업 필수화 |
| `.checklist.md` | `git stash --include-untracked` 로 보호 | **stash 대상에 포함되어 사라질 위험** → 체크포인트 커밋 방식으로 전환 |

### 작업 항목 실행 결과

0. **체크포인트 + 글로벌 사전 백업**
   - 체크포인트 커밋: `6ae9e9f07ab30f318e1262edf5b39bb71209a30f`
   - 글로벌 rules 백업: `~/.claude/rules/.backups/dev-checklist.md.20260423_091454.bak` (260 bytes)
1. **글로벌 스킬 복원**: `~/.claude/skills/checklist/SKILL.md` (3652 bytes) — 프로젝트 SHA256 MATCH. **세션 내 `available skills` 목록에 `checklist` 등장 실측 확인**.
2. **SSOT 규칙 명문화**: `~/.claude/rules/dev-checklist.md` 에 "스테이징/운영 분리" 섹션 신설 (프로젝트 = 정답, drift 시 글로벌 덮어쓰기)
3. **프로젝트 drift 정리**:
   - `settings.json` dead hook 참조 4개 제거 (Edit matcher 블록 전체 + Write matcher 내 2개)
   - dead 스크립트 5개 → `hooks/.backups/` 이동 (`dev-checklist-guard`, `doc-checklist-guard`, `venv-guard`, `auto-backup`, `check-streamlit`)
   - JSON 유효성 재확인 통과
4. **프로젝트 CLAUDE.md**: 프리플라이트 블록 없음 확인 후 "스테이징/운영" + "프리플라이트 중복 금지" 메모 2줄만 추가
5. **post-edit-verify.sh 정합**: `.checklist.md` 인식 완료 (line 98, 252 재확인)
6. **docs/workflows/dev-checklist.md drift**: 상단에 "2026-04-23 이후 스킬 통합, 이 문서는 역사 기록" notice 추가 (전면 재작성은 이월)
7. **5차 피드백 승인대기본 종결**: `.backups/.checklist.md.승인대기_checklist5차_2026-04-17.md` → `.backups/.checklist.md.종결_Q1~3결정반영_2026-04-23.md` 이름 변경 + 종결 메모 추가
8. **이월 장치**: `docs/history/index.md` 진행 중 섹션에 "Day 10 이월" 엔트리 등록 (나머지 C1·H1~3·M1~3 + /feedback 스킬 태깅 강화 + workflows/dev-checklist.md 재작성)
9. **히스토리 기록**: 이 파일 + 일별 인덱스 표 업데이트

## 범위 제외 (명시적 이월)

아래 항목은 이번 세션 범위 밖으로 분리, `docs/history/index.md` 진행 중 섹션에 등록:

| 분류 | 항목 | 근거 |
|------|------|------|
| C1 | SKILL.md 승인 허점 차단 (`approved: false` 기본값 + 턴 종료 명령형) | 5차 피드백 종결본 |
| H1 | mode 분기 도입 (code/doc/design/mixed) | 5차 피드백 종결본 |
| H2 | tiny edit 의미 조건 보강 (blanket 예외 제거) | 5차 피드백 종결본 |
| H3 | Phase 3 도구·로그 증명 의무 | 5차 피드백 종결본 |
| M1 | git status 기준선 의무 | 5차 피드백 종결본 |
| M2 | 롤백·파일 보존 정책 | 5차 피드백 종결본 |
| M3 | HISTORY 경로 정정 | 5차 피드백 종결본 |
| 추가 | /feedback 스킬 중요도 태깅 강화 (Codex·Gemini rubber-stamp 방지) | 오늘 Validation 2/3 INVALID |
| 추가 | docs/workflows/dev-checklist.md 전면 재작성 (스킬 기반) | 오늘 6번 drift 발견 |

## 회고

- **"자체 비판 → 외부 리뷰 → 실행"** 3단 패턴이 효과적이었음. 자체 비판만으로 6건, 외부 리뷰로 추가 치명 3건이 드러남 (특히 `doc-doublecheck-guard.sh` ALIVE 판정은 실측이 아니면 운영 훅 손실로 이어졌을 것).
- **실측이 메모리보다 우선** 원칙 재확인: 글로벌 settings는 이미 깨끗했음에도 기준선 실측 전에는 "거기에 dead hook 있다"고 믿고 있었음.
- **이월 장치의 실질화**: 2026-04-17의 "보류(보존)" 엔트리가 6일간 진전 없이 유지된 전례가 있으므로, 이번에는 이월 엔트리에 근거 파일 경로 + 구체 항목 명시.

## 다음 세션

- **/todo 스킬 제작** (주인님 원래 요청)
- 위 이월 항목 9건 — 항목별로 체크리스트 분리 권장 (한 세션에 몰면 보류 재발 가능)

---

# Day 10 Part 2 — /feedback 스킬 중요도 태깅 강화

## 요약

**Day 10 이월 ② 종결**. Day 10 Part 1 에서 `/checklist` 5차 피드백 종합 리뷰 결과 Validation 1/3 VALID (Claude Sub ✅, Codex·Gemini ❌) 로 드러난 **Codex/Gemini 태깅 누락 + rubber-stamp 리스크** 를 구조적으로 방어. 프롬프트 강제 + Validation Gate 앵커화 2중 방어를 실측으로 검증 후 스테이징까지 동기화.

## 배경 (Day 10 Part 1 연장선)

- 원문 근거: `docs/feedback/2026-04-23_claude_.checklist_20260423-084406-종합.md` L79 *"향후 개선: `run-codex.ps1` 프롬프트에 태깅 필수 명시 강화 필요"*
- `/feedback` 스킬은 Day 9 에 구조 승격 (md 1장 → SKILL.md + 6 PS1) 됐으나 프롬프트 SSOT(`orchestrate.ps1` `$promptLines`) 가 "중요도별 섹션"만 요구 → CLI 가 Finding 나열 스타일이면 태깅 자체가 누락되는 허점 관찰

## 결정 반영 (Q1~Q3 모두 권장안 채택)

| 질문 | 결정 | 조치 |
|------|------|------|
| **Q1** Validation 엄격화 방식 | **A — 괄호형 태그 접두사** | 정규식 `치명\|높음\|중간\|낮음` → `\[(치명\|높음\|중간\|낮음)\]` |
| **Q2** 프로젝트 스테이징 | **Yes** | Day 9 이후 글로벌 전용이던 것을 `Harness-engineering/skills/feedback/` 에도 복사 (SSOT 원칙 일관성) |
| **Q3** 스모크 대상 | **옵션 1 = SKILL.md 자체 (dogfood)** | 결과 2/3 VALID 개선 실측 |

## 실행 절차 (Phase 1 → 5, 그리고 소급 이슈 해결)

### 1차: 태깅 강제 + Gate 엄격화 (체크리스트 항목 0~6)

1. **체크포인트 + 백업**: 커밋 `0d2a0d4` + `~/.claude/skills/.backups/feedback-{SKILL.md,orchestrate.ps1,validate-outputs.ps1}.20260423_095749.bak`
2. **프롬프트 강화** (`orchestrate.ps1` L38-49):
   - "각 지적 첫 줄 맨 앞에 대괄호 태그 `[치명]`/`[높음]`/`[중간]`/`[낮음]` 중 하나 필수"
   - "장점 나열·rubber-stamp 금지, 문제 없으면 `[낮음] 없음 - 근거: X` 한 줄만"
   - "접근 실패는 `[확인 불가] 사유: X`"
   - "Top 3 우선순위도 태그 필수"
3. **Validation Gate 엄격화** (`validate-outputs.ps1` L48-50): 단순 포함 → 대괄호 접두사 정규식
4. **SKILL.md 동기화** (L40, L89): 유효성 판정 표현 통일
5. **프로젝트 스테이징 신설** (`Harness-engineering/skills/feedback/` + 6 PS1): SHA256 7/7 MATCH
6. **스모크 dogfood** (대상 = `~/.claude/skills/feedback/SKILL.md`): 338s 소요
   - **결과: 1/3 → 2/3 VALID** (Codex INVALID → VALID 전환 실측)
   - CLI별 태그 수: Claude Sub 9개, Codex 5개, Gemini 2개
   - Gemini INVALID 사유: "근거 없음" (태깅은 생겼지만 여전히 장점 나열 위주)

### 2차 — 소급 이슈 해결 (체크리스트 항목 7~10, 주인님 "이슈 해결하자" 지시)

**발견된 우회**: Gemini 출력 L8 *"태그(`[치명]`, `[높음]`, `[중간]`, `[낮음]`) 기반 검증이 매우 우수"* — 본문 인용 형태로도 정규식 통과.

7. **정규식 줄 시작 앵커화**: `(?m)^\s*(?:[-*]\s*|\d+\.\s*)?\[(치명|높음|중간|낮음)\]`
8. **프롬프트에 "본문 인용 금지" 1줄 추가**
9. **ATX 헤더 허용 보완** (2.1차): Claude Sub `### [높음]` 헤더 형식이 과도 엄격화에 걸려 INVALID 판정 → `#{1,6}\s+` 허용 prefix 추가
10. **최종 정규식**: `(?m)^\s*(?:[-*]\s*|\d+\.\s*|#{1,6}\s+)?\[(치명|높음|중간|낮음)\]`

## 단위 + 스모크 재검증 실측

| 테스트 케이스 | 1차 (단순 포함) | 2차 (앵커) | 2.1차 (앵커+ATX) |
|--------------|---------------|-----------|------------------|
| Claude Sub (`### [높음]` 헤더) | VALID | ❌ INVALID (부작용) | ✅ VALID |
| Codex (`[치명]` 단순 라인) | VALID | VALID | ✅ VALID |
| Gemini (본문 인용만) | (이전 INVALID) | INVALID | ✅ INVALID |
| 단위 샘플 본문 인용 (신규) | - | INVALID | ✅ INVALID |
| 단위 샘플 불릿 접두사 | - | VALID | ✅ VALID |

## 효과 및 남은 한계

### 효과
- **Rubber-stamp 방어 2중화**: 프롬프트(태그 강제) + Validation(줄 시작 앵커) → 정규식 우회 구조적 차단
- **Codex 실측 개선**: 태깅 누락 → 완전 준수
- **Claude Sub 보존**: ATX 헤더 허용으로 성실 리뷰 스타일 VALID 유지

### 한계 (신규 이월 ④)
**Gemini rubber-stamp 성향 자체는 잔존** — "우수", "돋보임" 같은 장점 나열 습관은 CLI 성격이라 프롬프트로 100% 제어 불가. 현재 Gate 에서 INVALID 로 걸리지만, **3 CLI 교차검증의 다양성 손실**이라는 부작용 발생. Gemini 프롬프트 추가 강경화 또는 CLI 교체 검토 필요 — **이월 ④ 로 신규 등록**.

## 변경 파일

| 파일 | 변경 |
|------|------|
| `~/.claude/skills/feedback/scripts/orchestrate.ps1` | L38-50 프롬프트 강화 (태그 접두사 필수 + 본문 인용 금지) |
| `~/.claude/skills/feedback/scripts/validate-outputs.ps1` | L13-16 docstring, L48-54 정규식 앵커화 + ATX 허용 |
| `~/.claude/skills/feedback/SKILL.md` | L40, L89 유효성 판정 표현 "줄 시작 + ATX" 반영 |
| `Harness-engineering/skills/feedback/**` (신규) | 스테이징 7개 파일 생성 (SHA256 전부 MATCH) |

## 회고

- **"스모크 실측 → 예상외 부작용 발견 → 즉시 보정"** 패턴이 효과적. 1차 앵커만 적용했을 때 Claude Sub 까지 INVALID 로 떨어지는 부작용을 발견하지 못했다면 성실한 리뷰어를 잃을 뻔함.
- **Gemini CLI 성격 한계 재확인**: Day 9 의 "3 CLI 교차검증 다양성" 이라는 설계 의도가 Gemini 의 rubber-stamp 습관으로 약화되고 있음. Gate 는 방어했지만 본질 해결은 이월.
- **주인님 쉬운 설명 요청에서 배움**: 기술 변경을 "스티커 꼼수 차단" 비유로 풀어 설명하니 핵심이 명확해짐 → 문서화에도 비유 보존 권장.

## 새로 등록된 이월 (Day 10 이월 ④)

**Gemini rubber-stamp 성향 근본 해결** — CLI 자체 성격 이슈. 대안: (a) `run-gemini.ps1` CLI 전용 강경 프롬프트, (b) Gemini 교체, (c) 3 CLI 유지하되 종합 단계에서 Gemini 가중치 축소. 근거 파일: 오늘 스모크 결과 `docs/feedback/2026-04-23_gemini_SKILL_20260423-100623.md`.

---

# Day 10 Part 3 — /feedback 스킬 인코딩 3 레이어 패치

## 요약

**Day 10 Part 2(태깅 강화)가 만든 `.checklist.md`를 대상으로 /feedback 을 돌린 결과 "한글 깨짐" 문제가 일부 재발**한 것을 주인님이 지적. 2026-04-22 1차 대응(`run-claude-sub.ps1` 한 파일에 `[Console]::OutputEncoding = UTF8` 두 줄만 추가)이 **단일 CLI × 단일 레이어** 패치였음을 인지하고, 3 레이어 × 3 CLI 교차점 9개 중 8개의 구멍을 **공통 헬퍼(`_encoding.ps1`) + 전 CLI dot-source + Start-Job 자식 runspace 재설정**으로 구조 방어. 스모크 V-1에서 **3 CLI × 한글 정상 + mojibake 0건** 확인, V-3(3회 연속)에서 **인코딩 목표는 완전 달성**했으나 **Codex/Gemini 연속 호출 시 stateful 누적 실패**라는 별건 이슈를 새로 발견 → 이월 ⑤ 등록.

## 배경 (Day 10 Part 2 연장)

- Part 2에서 5차 체크리스트(`.checklist.md`) 파일을 dogfood로 /feedback 시도 → **2026-04-22.md 를 대상 샘플로 재사용하던 과정에서 주인님 관찰**: "피드백 스킬에서 계속 인코딩이 깨지는 문제가 발생하는데 해결책 찾아보자 웹에서"
- 1차 대응 한계 인식: 2026-04-22 히스토리 H1 과 작업 A(Claude Sub 514B 깨짐) 조치는 **`run-claude-sub.ps1` 한 파일에만** `[Console]::OutputEncoding = UTF-8` 2줄 추가. Codex/Gemini/Start-Job 블록은 방치 → 재발 시 어느 CLI가 깨지는지만 바뀔 뿐 근본 해결 아님

## 근거 문서화 (코드 수정 전)

주인님 지시 "근거 문서화해놓고 진행해" 에 따라 `docs/research/feedback-encoding-fix/` 신설:

| 파일 | 내용 | 근거 수 |
|------|------|---------|
| `01_root-cause.md` | 3 레이어 × 3 CLI 교차점 실코드 인용 | 실측 4 |
| `02_web-evidence.md` | hy2k.dev, MS Learn, gemini-cli #20186, codex #4498, PS 공식 이슈 3건 등 | 외부 10 |
| `03_fix-plan.md` | Step A(최소) / B(중간) / C(Gemini argv 우회, 보류) / D(PS7 이주, 장기) + V-1/V-3 | — |

`docs/index.md` 리서치 카테고리 신설 + `docs/log.md` 1줄 append.

## 3 레이어 진단

| 레이어 | 방향 | 지배 변수 | 기존 구멍 |
|---|---|---|---|
| **L1** stdout 디코딩 | CLI → PS | `[Console]::OutputEncoding` | 3 CLI 중 1개만 설정 |
| **L2** 인자 인코딩 | PS → CLI | `$OutputEncoding`, argv codepage | 전원 누락 + Gemini `-p` 별건 버그 |
| **L3** Start-Job runspace | 부모 → 자식 | ScriptBlock 내부 재설정 | 부모 설정 상속 X |

## 결정 확정 (구현 전 주인님 컨펌)

| 결정 | 확정안 | 근거 |
|---|---|---|
| **D1** BOM 정책 | 유지 (`Set-Content -Encoding UTF8`) | 소비처(LLM + 육안)가 BOM 무해, 제거 리스크 불필요 |
| **D2** Step C(Gemini argv 우회) | 미룸 | MVP 재발 시 착수, 사전 실측 오버헤드 회피 |
| **D3** 한글 경로 점검 | 포함 | 격리 디렉토리가 실제로는 `$HOME\codex-cwd\<slug>` = 영문 고정 (CLAUDE.md 표준 패턴). 확인 완료 |

## 실행 (체크리스트 Phase 2)

### 코드 수정 (스테이징 `skills/feedback/scripts/`)
1. `_encoding.ps1` 신규 — UTF-8 BOM 포함 저장 (H1 교훈)
   - `[Console]::OutputEncoding` + `$OutputEncoding` + `$PSDefaultParameterValues['*:Encoding']`
2. `run-claude-sub.ps1` — 기존 인라인 L33-37 → dot-source 치환 (SSOT 통합)
3. `run-codex.ps1` — `$ErrorActionPreference` 직후 dot-source
4. `run-gemini.ps1` — `$ErrorActionPreference` 직후 dot-source
5. `orchestrate.ps1` 부모 — dot-source + `Write-Verbose` 진단 1줄
6. `orchestrate.ps1` Start-Job ScriptBlock — 첫 줄 dot-source, `$ArgumentList`에 `$scriptDir` 추가
7. `SKILL.md` 스크립트 파일 목록에 `_encoding.ps1` 추가

### BOM + 파싱 실측
- 7개 ps1 모두 **BOM `EF BB BF`** 유지
- 7개 ps1 모두 **`[Parser]::ParseFile` 통과** (에러 0)

### 스테이징 → 운영 동기화
- 7 ps1 + SKILL.md 총 8 파일 SHA256 MATCH (스테이징 ↔ 운영 drift 0)

## 검증 실측 (Phase 3)

### V-1 — 한글 다수 파일 1회 실행 (대상: `docs/history/2026-04-22.md`)

| 지표 | 결과 |
|---|---|
| 3 CLI 파일 생성 | 3/3 ✅ |
| **한글 정상 렌더** | **3/3 ✅** |
| **mojibake 바이트** | **0/3 ✅** |
| 부모 세션 진단 | `Parent encoding: Console=utf-8 Output=utf-8` |
| Validation Gate | 1/3 (Claude Sub VALID, Codex/Gemini 태깅 포맷만 누락 — 이월 ④ 동일 맥락) |
| Elapsed | 352초 |

### V-1 내부 관찰 — 신규 L4 레이어 감지

Codex 가 **자체 PowerShell 서브프로세스** 로 대상 md 를 읽을 때 `Get-Content` 기본 CP949 로 시작 → **파일 내용이 mojibake 로 stdout 에 찍힘**. 그러나 **Codex 스스로** "파일은 UTF-8인데 기본 PS 인코딩으로 읽히면서 깨졌습니다" 인식 후 `-Encoding UTF8` 재시도 → **정상 복구**. 결과물은 깨끗. 이 영역은 우리 스킬 레벨에서 직접 고칠 수 없으나, **`prepare-isolation.ps1` 이 격리 복사 시 BOM 삽입** 하면 회복 단계 자체를 생략시킬 수 있음 → Phase 2 후보로 `03_fix-plan.md` 에 추가 예정.

### V-3 — 3회 연속 실행 (Start-Job runspace 안정성)

| Run | Elapsed | Claude Sub 한글자 | Codex | Gemini | mojibake |
|-----|---------|-------------------|-------|--------|----------|
| r1 | 345s | **900** (정상) | ❌ async timeout + retry stdin 대기 | ❌ async timeout + retry IDE client 실패 | **0** |
| r2 | 305s | **541** (정상) | ❌ 동일 | ❌ 동일 | **0** |
| r3 | 397s | **161** (정상) | ❌ 동일 | ❌ 동일 | **0** |

**인코딩 목표 완전 달성**: Claude Sub 정상 생성 + **mojibake 3회 × 3 CLI × 실패 마커 모두 = 0건**.

### V-3 별건 — 이월 ⑤ 신규 등록

**Codex/Gemini 연속 호출 시 실패**:
- Codex retry 메시지: `"Reading additional input from stdin..."` → Codex CLI 가 이전 세션의 stdin 을 점유
- Gemini retry 메시지: `"[ERROR] [IDEClient] Failed to connect to IDE companion extension"` → IDE client 포트/소켓 리크
- V-1(단발성)에서는 두 CLI 모두 성공 → **인코딩 패치 regression 아님**
- 원인 추정: Start-Sleep 2초 간격이 CLI 내부 정리에 부족. Codex/Gemini 가 세션 종료 후에도 백그라운드 리소스 유지

## 변경 파일

| 파일 | 변경 | 위치 |
|------|------|------|
| `skills/feedback/scripts/_encoding.ps1` | 신규 (25행) | 스테이징 + 운영 |
| `skills/feedback/scripts/run-claude-sub.ps1` | 인라인 L33-37 → dot-source 1줄 | 스테이징 + 운영 |
| `skills/feedback/scripts/run-codex.ps1` | dot-source 1줄 삽입 | 스테이징 + 운영 |
| `skills/feedback/scripts/run-gemini.ps1` | dot-source 1줄 삽입 | 스테이징 + 운영 |
| `skills/feedback/scripts/orchestrate.ps1` | 부모 dot-source + Write-Verbose + Start-Job ScriptBlock dot-source + ArgumentList 1개 추가 | 스테이징 + 운영 |
| `skills/feedback/SKILL.md` | 스크립트 파일 목록에 `_encoding.ps1` 추가 | 스테이징 + 운영 |
| `docs/research/feedback-encoding-fix/01_root-cause.md` | 신규 | — |
| `docs/research/feedback-encoding-fix/02_web-evidence.md` | 신규 | — |
| `docs/research/feedback-encoding-fix/03_fix-plan.md` | 신규 | — |
| `docs/index.md` | 리서치 카테고리 신설 | — |
| `docs/log.md` | 1줄 append | — |

## 효과 및 한계

### 효과
- **3 레이어 × 3 CLI 8개 교차점 방어**: V-1 한글 정상 100%, mojibake 0건
- **중복 제거**: 인라인 2줄 × 3 파일 → `_encoding.ps1` 헬퍼 dot-source 1줄로 통합
- **Start-Job runspace 인지**: PS #4681/#14945 근거로 자식 진입부 재설정 확정
- **진단성**: Write-Verbose 한 줄로 재발 시 어느 레이어인지 빠른 판별

### 한계 및 이월
- **L4 (Codex 내부 spawn PS 의 파일 읽기)**: Codex 자체 회복력 있음. 격리 복사 시 BOM 삽입으로 회복 단계 skip 가능 — 후속 개선 후보
- **이월 ⑤ (CLI 연속 호출 실패)**: Start-Job 간격/정리 정책 + 지수백오프 재시도 설계 필요
- **Gemini argv 버그(#20186)**: MVP 제외. 재발 시 tempfile 기반 프롬프트 전달로 우회

## 회고

- **근거 문서화를 구현 전 선행**한 효과가 컸음. 3 레이어 진단 없이 `run-codex.ps1` 에 똑같이 2줄 추가하는 식으로 갔으면 Start-Job L3 구멍을 또 놓쳤을 것
- **실측 우선 원칙**: V-1 단독 실행 시 Codex 내부 회복 과정이 stdout 에 남아있지 않았다면 L4 존재 자체를 인지 못 했을 것
- **"별건" 판별력**: V-3 Codex/Gemini 실패를 인코딩 regression 으로 오인했다면 3 레이어 패치를 롤백했을 것. V-1 성공 기록과 대조해서 이월 ⑤ 로 분리

## 새로 등록된 이월 (Day 10 이월 ⑤)

**CLI 연속 호출 시 Codex/Gemini stateful 누적 실패** — V-1 단독 성공, V-3 3회 연속 실패. 대안: (a) Start-Sleep 간격 증가(2s → 10~30s), (b) 회차간 `taskkill /IM codex.exe` 명시 정리, (c) retry 시 지수 백오프 (현재 sync 즉시 재시도), (d) 병렬 Start-Job 대신 순차 실행 옵션 추가. 근거 파일: `docs/feedback/2026-04-23_{codex,gemini}_2026-04-22_20260423-{173731,174318,174824}.md` 실패 마커 6개.
