# Day 12 — 2026-04-25 — 5개 프로젝트 CLAUDE.md 옵션 B 보강 (병렬 팀)

## 1. 작업 개요
- 대상: Day 11 audit 의 🟡 5개 (개발 명령 섹션 부재 공통)
- 방식: TeamCreate (`claude-md-batch5`) + TaskCreate × 5 + Agent (team_name) × 5 병렬 spawn + SendMessage 협업
- 옵션: B (헌법 블록 + 개발 명령 + @import). 옵션 C(기술 스택 Why) 미진행

## 2. 진짜 Agent Teams 패턴 적용
- 팀 이름: `claude-md-batch5`
- teammate 5명 (general-purpose, run_in_background): `agent-office` / `harness` / `hsk` / `hsk-analyzer` / `paa`
- 각 prompt 에 헌법 블록 14줄 원문 + 보존 대상 + 사전 정찰 명령 + 검증 V-1~V-5 + 절대 금지(다른 프로젝트 미접근, 헌법 문구 변형 금지) 직접 주입
- 메인 컨텍스트 절약 → 5명 동시 실측·작업 → 보고 → Read 검증

## 3. 결과

| 프로젝트 | 줄수 변경 | 헌법 | 개발 명령 | @import | 보존 |
|---------|----------|-----|----------|---------|-----|
| Agent-office | 37 → 65 | ✅ 1~13행 | 6개 (uvicorn, pytest, /ui, /health 등 실측) | 3개 (research/, design/, MEMORY.md) | ✅ |
| Harness-engineering | 27 → 62 | ✅ template 1~14 복사 | 5개 (`/claude-md audit·init`, Copy-Item+SHA256, `/checklist`) | 3개 (history/index, template, architecture) | ✅ codex_workdir Why 단어 단위 |
| HSK | 35 → 51 | ✅ | 5개 (run.py 4 + verify_splits 추가) | 1개 (`@hsk_analyzer/CLAUDE.md`) | ✅ 핵심 규칙 6개 |
| HSK/hsk_analyzer | 47 → 73 | ✅ | 4개 (모듈 전용: split/precision/merge/verify) | 1개 (`@../CLAUDE.md`) | ✅ 표 전부 |
| PAA | 36 → 65 | ✅ | 5개 (`streamlit run app.py --server.port 8502`, ruff, pytest) | 4개 (README, docs/index, history/index, CHANGELOG) | ✅ 🔴 보안 격리 unchanged |

## 4. 의사결정
- **HSK 51줄 (V-5 60줄 미달) 허용**: 체크리스트 V-5 원문 "60~100줄 범위 (어느 프로젝트가 60줄 미달이면 기존 내용이 짧아서이므로 허용하되 기록)" 단서 적용. teammate 가 drift 위험 때문에 패딩 거부 → 정확한 판단으로 평가
- **PAA 보안 격리 보존이 최우선**: prompt 에 "한 글자도 변경 금지" 강조 → teammate 가 작업 후 Read 재확인까지 self-verify, 메인도 Read 로 cross-check
- **Harness-engineering 부제 1구절 추가** ("CLAUDE.md 템플릿 원본 보유자"): 정보 가치로 수용 — 보존 우선 원칙은 "삭제 금지" 이고 추가는 허용 범주

## 5. 발견 이슈 (별건 후속 후보)
- **Agent-office 가상환경**: `venv/` 사용 — 글로벌 `coding.md` 권장 `.venv/` 와 불일치. teammate 는 실측 원칙으로 `venv\Scripts\activate` 그대로 기록. 추후 마이그레이션 후보 (`coding.md` Phase 1~4 절차)
- **Agent-office `## 참조` + `## 참고 (@import)` 의미 일부 겹침**: 둘 다 docs/research 언급. 보존 우선으로 둘 다 유지. 추후 통합 검토 가능

## 6. 사이드 관찰
- TeamCreate + Agent(team_name) + SendMessage 패턴이 5개 독립 작업에 잘 맞음 (충돌 0, 병렬 효율 높음)
- 자동 라우팅 잔여로 일부 teammate 가 `task_assignment` 메시지 1회씩 더 받음 → TaskGet 으로 already-completed 확인 후 idle 유지로 정상 처리
- 주인님 CLAUDE.md "설계/계획/분석/구현은 무조건 팀으로" 충실 이행

## 7. 변경 파일
- `Agent-office/CLAUDE.md` (37 → 65줄, 별도 git repo)
- `Harness-engineering/CLAUDE.md` (27 → 62줄)
- `HSK/CLAUDE.md` (35 → 51줄, 별도 git repo)
- `HSK/hsk_analyzer/CLAUDE.md` (47 → 73줄, 별도 git repo)
- `PAA/CLAUDE.md` (36 → 65줄, 별도 git repo)
- `Harness-engineering/.checklist.md` → `.backups/.checklist.md.완료_5개CLAUDE보강_2026-04-25.md`
- `Harness-engineering/docs/history/2026-04-25.md` (신규, 이 파일)
- `Harness-engineering/docs/history/index.md` (진행 중 섹션 갱신 + Day 12 row 추가)

## 다음 작업
- [ ] candidate_orchestra 처리 방침 결정 (archive / 삭제 / 재시작) — Day 11 이월
- [ ] ⚠️ Knowledge-platform 축약 141→80줄 (1시간, drift 위험으로 별도 세션 권장) — Day 11 이월
- [ ] (별건 후속) Agent-office `venv/` → `.venv/` 마이그레이션
- [ ] (별건 후속) Agent-office `## 참조` + `## 참고 (@import)` 통합 검토
- [ ] (보류) agent-team-manager v2 §9 의사결정 3건 (U1 충돌 / preset 5개 / YAML 전환)
