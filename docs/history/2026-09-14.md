# 2026-09-14 — 네 가지 원칙 기준 스킬·문서 정리

## 1. 오래된 기준과 내부 용어 정리

- 결론: 폐기된 `rules/` 구조를 가르치던 `memory-manager`를 현재 메모리 기준에 맞춰 다시 썼고, `handoff`·`todo`·`project-history`의 내부 결정 번호와 SSOT 같은 약어를 평범한 말로 바꿨다.
- 변경·이유: `checklist`는 이미 승인 없이 요청할 때만 쓰는데, 다른 스킬 표에는 예전 승인 흐름이 남아 있어 바로잡았다. 스킬 본문의 `~/.claude` 경로를 런타임 중립 표현으로 바꿔 Claude·Codex 운영본이 같은 파일을 쓰게 했다. 기존 Codex 운영본은 `~/.Codex/projects`처럼 없는 경로로 기계 치환된 상태였다.
- 검증: 변경한 스킬 6개를 `~/.claude/skills`와 `~/.agents/skills`에 복사하고 해시 일치를 확인했다. 대상 스킬에 옛 용어가 남지 않았음을 검색으로 확인했다.

## 2. 항상 읽히는 설명과 중복 문장 축소

- 결론: `cross-agent-review`·`agent-reach` 설명을 조건은 유지한 채 줄였다. `agent-structure.md` 구조 요건 중 글로벌 Harness 규칙과 겹치던 항목과 프로젝트 `CLAUDE.md`의 글로벌 중복 문장 3개를 링크나 삭제로 정리했다.
- 변경·이유: 설명은 매 세션 들어가므로 짧아야 하고, 결정은 한 곳에만 둔다.
- 검증: 설계 문서 내부 앵커와 글로벌 지침 앵커가 모두 연결되고 코드 블록 수가 짝수이며, 공백 검사와 지침 사본 4개 일치 검사를 통과했다.
- 제외: humanize 계열은 외부 원본을 그대로 유지한다는 lock 결정에 따라 손대지 않았다.

## 3. 백업 폴더 삭제

- 결론: `skills/`·`hooks/`·`agents/` 아래 Git에 없는 `.backups` 11곳(파일 52개)을 주인님 승인에 따라 영구 삭제했다. 비게 된 `hooks/data`도 지웠다.
- 검증: 삭제 전 Git 추적 파일이 없음을 확인했고, 삭제 후 해당 위치에 `.backups`가 남지 않았다.
- 남은 것: 루트 `.backups/`(인계·할 일 보관 위치로 사용 중), `docs/` 아래 백업, 운영본 `~/.claude/skills/.backups`는 범위 밖이라 두었다.

## 4. 실행 확인과 교차 검토

- 결론: `/memory-manager audit`를 이 프로젝트에 실행해 새 절차가 동작함을 확인했다. Codex 읽기 전용 검토에서 Codex도 새 스킬 설명을 불러오는 것을 확인했다.
- 반영: Codex 지적 중 `memory-manager` 설명에서 빠진 초기화 조건과 `handoff`의 인계 위치 모순을 고쳤다.
- 남은 지적: 메모리 주제 파일의 frontmatter가 현재 Claude 형식(`metadata.type`)과 다르고, 규칙 한 줄이 글로벌 지침과 겹친다. 이는 보고만 했다.

## 5. 먼저 있던 에이전트 구조 개정 커밋

- 결론: 이 세션 전에 작업 트리에 있던 `agent-structure.md` 개정(사람의 피드백·메모리 명령은 요청 범위를 승인하고, 에이전트 제안은 기존 제안 저장소에서 사람 승인을 받은 뒤 적용)을 주인님 지시로 별도 커밋했다. 내용과 검증은 그 문서의 Verification 절에 있다.
- 미해결: Codex가 두 가지를 지적했다. 슬래시 명령으로만 공식 변경을 받는 규칙이 글로벌 지침의 "승인된 변경 요청은 바로 진행"과 충돌해 보인다는 점, `proposal store`의 위치가 정의되지 않았다는 점이다. 설계 결정이라 고치지 않고 주인님 판단으로 남겼다.
