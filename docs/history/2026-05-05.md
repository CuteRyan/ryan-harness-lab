# Day 20 — 2026-05-05 — #009-B PASS (presets/ 5 YAML 신설, Step A 12 agent 호출 박음 + 4-step 프로토콜 + review cycle cap 3)

> Day 19 turn 12 (2026-05-04) 의 #009-B 체크리스트 작성 + 보류 → 본 turn 1 (Day 20, 2026-05-05) 새 세션 진입 후 즉시 승인 + Step B PASS = #009 3-step 분할 中 2/3 PASS (Step A turn 11 + Step B turn 13). Step C (SKILL.md v2) = 별도 turn 보류.

---

## 1. 사전 확인 + HANDOFF 인수 + 체크리스트 승인

### 1-1. /handoff done (소멸 정책 **15회차 검증**)
- HANDOFF turn 12 (2026-05-04, 136줄) Read → 본 세션 진입 의도 확인 (#009-B Step B 즉시 진입)
- `git mv HANDOFF.md .backups/HANDOFF.done.2026-05-05.md` — 정상 소멸 (양식 v2 dogfood 8건 누적 후 첫 archive)
- 소멸 정책 **15회차 검증** (14회차 = turn 10 → turn 12)

### 1-2. 사전 체크 (Quick Start §1)
| 항목 | 결과 |
|------|------|
| `Get-ChildItem Env:CLAUDE_CODE_SUBAGENT_MODEL` | **ABSENT** (env 영구 제거 효과 유지, fallback C+ 메커니즘 3중 全 작동) |
| `git status --short` | **clean** (turn 12 commit `0e386ea` 後 working tree 깔끔) |
| `.checklist.md` (turn 12 산출) | 보존 PASS (`approved: false`/`status: draft`) |
| `.todo.md` #009 진행 | Step A 完 표기 확인, Step B 작업 직전 상태 |

### 1-3. 체크리스트 승인 마킹
- 사용자 "작업 이어서하자... 핸드오프 제대로 파악하고 지금 현황도 잘 파악하고 바로 진행해" = 묵시적 승인
- `.checklist.md` `approved: false` → `true`, `status: draft` → `approved`, 승인일 = 2026-05-05 추가
- 체크리스트 §승인 규칙 정합 (다음 턴에서만 마킹) PASS

## 2. Step 1: presets/ 디렉토리 + YAML 양식 확정

### 2-1. 양식 = wshobson 차용 + 본 비전 확장
- 기준 양식: [wshobson/agents](https://github.com/wshobson/agents) HEAD `ece811f23310a37ceb43496dbac0e244fe6845b6` (2026-05-02) `plugins/agent-teams/skills/team-composition-patterns/references/preset-teams.md` (Configuration + Members + Task Template + Variations)
- 본 비전 확장 4 항목: ① `pm_lead: pm` (모든 preset lead = pm agent, Opus) ② `protocol` 섹션 4-step 강제 ③ `review_cycle_cap: 3` + 출처 인용 ④ `task_template.output_format_required` 4 요소 (#014 turn 9 강화 + 글로벌 `rules/research-mandatory.md` superset)

### 2-2. 공통 키 12종 확정
`name` · `description` · `source` · `pm_lead` · `team_size` · `display_mode` · `members` · `task_template` · `protocol` · `review_cycle_cap` · `escalation_after_cap` · `variations` (옵션 1)

## 3. Step 2-6: 5 YAML 신설 (마스터플랜 §2.4 1:1 정합)

### 3-1. 신설 5 YAML
| Preset | team_size | members (Step A 12 agent 호출) | 단계 의존성 (sequence) |
|--------|-----------|-------------------------------|----------------------|
| `presets/review.yaml` (82 lines) | 3 | security-reviewer, performance-reviewer, correctness-reviewer | 병렬 (3명 동시) |
| `presets/debug.yaml` (87 lines) | 3 | hypothesis-investigator (1) → reproducer (2) → solver (3) | Pipeline (addBlockedBy 체인) |
| `presets/research.yaml` (87 lines) | 3 | docs-researcher (1) ‖ community-researcher (1) → analyst (2) | Parallel + 종합 |
| `presets/docs-research.yaml` (90 lines) | 4 | research 3 + architect (3, addBlockedBy: analyst) | Parallel → 종합 → ADR |
| `presets/harness-design.yaml` (93 lines) | 3 | docs-researcher (1, **D-15** researcher 통합) → architect (2) → auditor (3) | Pipeline |

### 3-2. 모델 배분 (D-4 + D-15 정합)
- **opus**: pm (lead, 별도 agent) + architect (docs-research·harness-design 만)
- **sonnet**: 나머지 9 멤버 슬롯
- **haiku**: **0건** (메모리 `feedback_no_haiku.md` 100% 준수, Day 19 turn 11 R-10 정합)

### 3-3. variations 추가 (wshobson 차용)
- review: `full_review` (3명 全) + `security_focused` (2명, 성능 영향 적은 영역)
- debug: `default` + `hypotheses_n` (가설 N개 병렬, zircote Parallel Specialists 변형)
- research: `default` + `docs_only` (커뮤니티 자료 부족 시)
- docs-research: `default` + `research_only` (ADR 단계 생략, research preset 으로 다운그레이드)
- harness-design: `default` + `design_only` (auditor 단계 별도 turn 분리)

## 4. Step 7: 운영 sync 5쌍 SHA256 MATCH

### 4-1. 운영 디렉토리 신설
- `~/.claude/presets/` 부재 → `New-Item -ItemType Directory` 신설 = D-11 정책 (스테이징/운영 분리, 단방향 sync) 정합
- 운영 = Claude Code 가 자동 로드 위치 (skills/agents/rules 와 동일 패턴)

### 4-2. SHA256 MATCH 5/5 PASS
| 파일 | SHA256 (앞 16자) |
|------|----------------|
| debug.yaml | `3F9D715DEADAD2F0...` |
| docs-research.yaml | `06F2B46EB2A88F2C...` |
| harness-design.yaml | `41A05225B9732BEC...` |
| research.yaml | `52BFA9151FA490FC...` |
| review.yaml | `FD4C7E47A8BF516F...` |

## 5. Step 8: 마스터플랜 §2.4 1:1 정합 검증

### 5-1. 기준 (04_masterplan.md L229~238)
| Preset | 기준 (마스터플랜 §2.4) | YAML members | 정합 |
|--------|---------------------|-------------|------|
| review | 3명 (보안/성능/정확성) | security/performance/correctness-reviewer | ✅ |
| debug | 3명 (가설/재현/해결) | hypothesis-investigator/reproducer/solver | ✅ |
| research | 3명 (공식docs/커뮤니티/analyst) | docs-researcher/community-researcher/analyst | ✅ |
| docs-research | 4명 (위 3 + architect) | research 3 + architect | ✅ |
| harness-design | 3명 (researcher/auditor/architect) | docs-researcher [D-15 통합] /architect/auditor | ✅ |

→ **5/5 PASS, 마스터플랜 본문 무수정 (정합 확인만)**.

## 6. Step 9: 4-step 프로토콜 + review cycle cap 3 검증

### 6-1. protocol 섹션 5/5 PASS
- 모든 5 YAML `protocol.steps` = 4 항목 (TeamCreate → TaskCreate → Agent spawn[model] → SendMessage)
- 글로벌 강제 훅 (`hooks/pretooluse-agent-model-required.{sh,py}`, turn 7 신설) + `~/.claude/rules/agent-spawn-model.md` (turn 6 신설) 정합
- v2 P0 항목 中 O1 4-step 프로토콜 = 본 turn 의 5 YAML 흡수 PASS (마스터플랜 §10.2 v2 P0 우선순위 진행)

### 6-2. review_cycle_cap 5/5 PASS
- 모든 5 YAML `review_cycle_cap: 3` 명시 + `escalation_after_cap` 본문 + 출처 인용 (`aws-samples/sample-claude-code-agent-team` HEAD `67840be3` `skills/spec-workflow/SKILL.md:65` "Max 3 review cycles per group, then escalate")
- 마스터플랜 §4.3 pm.yaml `escalation.review_cycle_cap: 3` 와 정합 PASS

### 6-3. task_template.output_format_required 4 요소 5/5 PASS
- 모든 5 YAML `output_format_required` = ① 결론 ② 출처 (URL+발행일+직접 인용) ③ 추측 표현 금지 ④ 자기비판
- pm-test (turn 9 #014 강화) + 12 agent (turn 11 #009-A) 양식 그대로 흡수 = 워커 spawn 시 출력 일관성 보장 (R-10 정합)

## 7. Step 10: history + .todo + commit + HANDOFF archive

### 7-1. 본 파일 (`docs/history/2026-05-05.md`) 신설
- 양식: 2026-05-04.md 차용
- §1~§9 (사전 + Step 1~10 요약) + §10 결정·R 부수 발견 + §11 잔여

### 7-2. `docs/history/index.md` Day 20 행 신설

### 7-3. `.todo.md` 갱신
- #009 Step B 完 표기 (Step A·B 完, Step C 잔여)
- 잔여 = scripts/ 6 + reference/ 4 + Step C SKILL.md v2

### 7-4. commit + push (한 단위, 메모리 `feedback_commit_push.md`)

## 8. 결정 + R 부수 발견

### 8-1. 결정 (D)
- **D-16 (turn 13, 2026-05-05)** — preset YAML 디렉토리 = `presets/` (스테이징) ↔ `~/.claude/presets/` (운영) 단방향 sync. skills·rules·agents 와 동일 D-11 정책 일관 적용. **이유**: Claude Code 가 자동 로드하는 위치는 운영, 편집은 스테이징, drift 방지 책임은 본 프로젝트.
- **D-17** — preset YAML 양식 = wshobson 차용 (Configuration + Members + Task Template + Variations) + 본 비전 확장 4 항목 (pm_lead · protocol · review_cycle_cap · output_format_required). **이유**: 외부 검증된 양식 (34k★) 차용 + 본 비전 강제 메커니즘 (4-step + cap + 4-요소) 박음.
- **D-18** — `members[].sequence` + `members[].blocked_by` 필드로 단계 의존성 명시 (debug/research/docs-research/harness-design 의 Pipeline + Parallel 혼합 구조). **이유**: zircote 7패턴 中 Pipeline + Parallel Specialists 의 명시적 의존 그래프 표현 = SKILL.md v2 가 `addBlockedBy` 호출 자동화 가능.

### 8-2. R 부수 발견
- **R-11 (turn 13)** — preset YAML 의 `team_size` ≠ `len(members)` 가능 (variations 의 `security_focused` team_size=2). 검증 시 default variation 의 team_size 와 members 수 정합만 확인. **시사점**: SKILL.md v2 의 `validate-team.ps1` (별도 turn 신설) 에서 default variation 검증 우선.
- **R-12** — `members[].dimension` 필드 일관성 = preset 도메인 의존 (review = 보안/성능/정확성, debug = 가설/재현/해결, research = 공식 문서/커뮤니티/종합, ...). 동일 agent 가 다른 preset 에서 다른 dimension 명 갖는 사례: docs-researcher (research·docs-research = "공식 문서" / harness-design = "조사 (researcher 통합)"). **시사점**: dimension 은 agent 본체가 아닌 preset 컨텍스트 속성 — 정합성 자동 검증은 preset 단위.

## 9. 잔여 작업 (#009 Step C + 별도 turn)

- **#009 Step C** — `skills/agent-team-manager/SKILL.md` v1.5 → v2 (PM·preset·hooks 보류 3건 흡수). 보류 3건 흡수 = (a) `pm.md` (turn 11) 호출 박기 (b) `presets/*.yaml` (본 turn 신설) 호출 박기 (c) hooks 연동 = 강제 훅 (`pretooluse-agent-model-required.{sh,py}`) 작동 확인 + 4-step 프로토콜 강제 자체 검증. **추정 = 1 turn (별도 세션 가능)**.
- **잔여 자산** — scripts/ 6 (preflight·resolve-preset·run-team·monitor-team·validate-team·shutdown-team) + reference/ 4 (patterns·anti-patterns·errors·presets). Step C 진입 시 동시 신설 또는 별도 #009-D 종결.
- **HANDOFF turn 12 §결정 3** = 운영 4 election_simulator 전용 agent (D-11 정합성 검토) → 별도 `.todo.md` 신규 항목 등록 후속 처리.

## 10. 양식 v2 dogfood + /checklist mode=mixed 누적 (Day 20 turn 1)

- 양식 v2 dogfood **9건째** (turn 12 = 8건 → 본 turn 1 = 9건)
- /checklist mode=mixed **7건째** (turn 12 = 6건 → 본 turn = 7건)
- HANDOFF 소멸 정책 **15회차 검증** (turn 11 = 14회차 → 본 turn = 15회차)

---

## 11. Day 20 turn 2 — #009-C PASS (agent-team-manager SKILL.md v1.5 → v2, PM·preset·hooks 보류 3건 흡수)

### 11-1. 사용자 명시 + 진입
- 사용자 명시 "이서서 진행해보자" = 묵시적 승인 + Step C 즉시 진입.
- Step A (turn 11 #009-A) 12 agent + Step B (Day 20 turn 1 #009-B) 5 preset + 강제 훅 (turn 7 #018) 全 입력 자료 확보 → SKILL.md v2 가 호출 박는 마지막 단계.

### 11-2. .checklist.md 처리
- 기존 #009-B 체크리스트 → `.backups/.checklist.done.009-B.2026-05-05.md` archive
- #009-C 체크리스트 신설 (`.checklist.md`, status=approved 직접, ~150줄, 11 단계)

### 11-3. SKILL.md v1.5 (244줄) → v2 (355줄, +111) 5 영역 차분
| 영역 | 변경 |
|------|------|
| Description (frontmatter) | "마스터플랜 부트스트랩 단계용 중간 가이드" → "Phase 1 본격 운영 (PM·preset·hooks 全 구현 완료)" + "리뷰 팀 돌려줘" 자연어 트리거 추가 |
| §0 본 스킬의 위치 | "부트스트랩" 표현 제거. PM/preset/강제 훅 신설 turn 명시. R-1~R-12 확장 표시 |
| §1 + §1.1 신설 | 4-step 표 갱신 + **§1.1 글로벌 강제 훅 박스 신설** (turn 7 #018 + turn 8 #019 라이브 검증 인용 + Issue #26923 우회 패턴 + SHA256 박음) |
| §2 + §2.4 + §2.5 신설 | "PM 미구현" 제거 → "PM 추천 + 사장 spawn 대행". **§2.4 ② 회의실 5 preset 카탈로그 신설** (마스터플랜 §2.4 1:1 정합 5/5 PASS, wshobson HEAD `ece811f2` + aws-samples HEAD `67840be3` 출처). **§2.5 preset 자동 매핑 heuristic 신설** (사용자 요청 키워드 → preset 매핑 표) |
| §3 + §3.1 의미 전환 | 모델 배분 표 = 3중 보장 강제 메커니즘 (frontmatter / 명시 model / 강제 훅). **§3.1 박스 의미 전환 = 종전 경고 → "fallback C+ 영구 적용"** (turn 8 #019 PASS, issue#32732 종결). 라이브 4 spawn 검증 결과 인용 + R-7 부수 발견 인용 |
| §5 가드레일 R-1~R-12 확장 | R-6~R-12 신규 7 가드 추가 (turn 8 R-7·R-8 + turn 10 R-9 + turn 11 R-10 + Day 20 turn 1 R-11·R-12) |
| §6 명령어 | `/agent-team run --preset <이름>` 모드 신설 + `/agent-team run --pm-consult` 모드 신설 + `/agent-team list` preset 카탈로그 표시 + `/agent-team show --preset <이름>` 신설. **preset YAML 직접 편집 금지** 명시 (D-11/D-16 정합) |
| §7 한계 표 → §7.1 활용 자산 + §7.2 잔여 한계 분리 | v1.5 §7 한계 中 PM·preset·hooks 3 행 제거 → §7.1 활용 자산 표로 전환 (6 행, 신설 turn 명시). §7.2 잔여 한계 = scripts/ 6 + reference/ 4 + feature·security 2 preset + bypass_threshold + /agent-office + Linux 배포 |
| §8 마이그레이션 | 부트스트랩 표현 제거. Phase 1 → Phase 2 흐름도 갱신. preset/agents/teams 역방향 호환 명시 |
| §변경 이력 | v2 entry 추가 (~10줄 요약, SHA256 ED0A9DD1...8F0C 이전 v1.5 박음) |

### 11-4. 운영 sync (D-11 정책 정합)
- `~/.claude/skills/agent-team-manager/SKILL.md` ← `skills/agent-team-manager/SKILL.md` 단방향 sync
- SHA256 MATCH 1쌍 PASS = `9FC078A6...AFFF` (Step 10 grep 정정 후 v2.1)
- **운영 hot-reload 작동 PASS** = sync 직후 시스템 리마인더 표시 description 즉시 v2 로 갱신 ("Phase 1 본격 운영 — PM·preset·hooks 全 구현 완료, ② 회의실 5 preset 즉시 호출 가능")

### 11-5. Step 10 grep 자체 검증 (5/6 PASS)
| 검증 항목 | 기준 | 실측 | 판정 |
|----------|-----|-----|------|
| `agents/pm.md` 인용 | ≥ 2 | 10 | ✅ |
| `presets/` 인용 | ≥ 3 | 15 | ✅ |
| 강제 훅 \| pretooluse 인용 | ≥ 2 | 19 | ✅ |
| `4-step` 인용 | ≥ 3 | 7 | ✅ |
| `issue#32732 미해결` 잔존 | = 0 | 0 (정정 후) | ✅ |
| `부트스트랩` 잔존 | ≤ 1 | 2 | ⚠️ 의도된 §변경 이력 v1.5+v2 entry 보존 (히스토리 트래킹) — 체크리스트 명세가 좁았음, R-13 잠정 보호 |

### 11-6. 결정 (D-19~D-21) + R 부수 발견 (R-13)
- **D-19** — SKILL.md v2 = `presets/<이름>.yaml` Read → `members[].name` 추출 → `members[].blocked_by` mapping → `Agent({subagent_type, model, team_name})` spawn 흐름. **이유**: preset YAML 양식 (Day 20 turn 1) 의 `sequence`+`blocked_by`+`model` 필드를 SKILL.md 가 직접 호출 박음 = 자산 재사용 최대화.
- **D-20** — preset YAML 직접 편집 금지 (스테이징 only, 운영 sync 의무). **이유**: D-11 (agents/skills/rules) + D-16 (presets) 단방향 정책 일관. v2 §6 `/agent-team edit` 명령어에 명시.
- **D-21** — §변경 이력 entry 다중 보존 정책. **이유**: 단일 entry 만 두면 v1 → v1.5 → v2 진화 추적 불가. 체크리스트 §10-2 "≤ 1" 명세는 좁은 표현, 실제는 N entry 보존이 정상 (R-13).
- **R-13** — 체크리스트 grep 검증 명세 작성 시 §변경 이력 다중 entry 가능성 사전 고려 의무. **시사점**: 향후 체크리스트 작성 시 "단어 N개 = 의도된 히스토리 entry M개 × 평균 단어/entry K" 산식으로 명세 정확화.

### 11-7. 산출물
- `skills/agent-team-manager/SKILL.md` v1.5 244줄 → v2 355줄 (+111 lines)
- `~/.claude/skills/agent-team-manager/SKILL.md` 운영 sync MATCH (SHA256 `9FC078A6...AFFF`)
- 본 §11 신설 (~75줄, 120 → ~195 줄)
- `index.md` Day 20 행에 turn 2 결과 append
- `.todo.md` #009 Step C 完 표기 (#009 全 PASS, 잔여 = scripts/ 6 + reference/ 4 = 별도 turn)
- `.checklist.md` → `.backups/.checklist.done.009-C.2026-05-05.md` archive
- commit: `feat+docs: Day 20 turn 2 — #009-C PASS (SKILL.md v1.5 → v2, PM·preset·hooks 보류 3건 흡수)`

### 11-8. #009 全 사이클 정리 (3-step 분할 PASS)
- ✅ **#009-A** (turn 11, 2026-05-04) — 정식 직책별 agent 12명 신설 (PM 1 + 워커 11)
- ✅ **#009-B** (Day 20 turn 1, 2026-05-05) — presets/ 5 YAML 신설 (review·debug·research·docs-research·harness-design)
- ✅ **#009-C** (Day 20 turn 2, 2026-05-05) — agent-team-manager SKILL.md v1.5 → v2 (PM·preset·hooks 흡수)
- 잔여 (#009-D 또는 별도 turn): scripts/ 6 (preflight·resolve-preset·run-team·monitor-team·validate-team·shutdown-team) + reference/ 4 (patterns·anti-patterns·errors·presets) + feature·security 2 preset

### 11-9. 양식 v2 dogfood + /checklist mode=mixed 누적 (Day 20 turn 2)
- 양식 v2 dogfood **10건째** (turn 13 = 9건 → 본 turn = 10건)
- /checklist mode=mixed **8건째** (Day 20 turn 1 = 7건 → 본 turn = 8건)

---

## 12. Day 20 turn 3 — #009-D-1 PASS (scripts/ 6 PowerShell 헬퍼 신설)

### 12-1. 사용자 명시 + 진입
- 사용자 명시 "진행하자 이어서" = 묵시적 승인 + Day 20 turn 3 즉시 진입.
- #009-C 직후 잔여 #009-D 진입. **분할 결정**: D-1 = scripts/ 6 / D-2 = reference/ 4 + SKILL.md v2.5 / **#009-E 분리** = feature·security 2 preset (멤버가 Step A 12 agent에 없음 — lead/frontend/backend/tester + SAST/DAST/compliance, 새 agent 7 신설 선행 필요).

### 12-2. 입력 자료 + 양식 결정
- v2 spec `04_redesign-spec.md §1·§2·§3` 의 scripts 책임 분리 매트릭스 차용 (단, preset YAML 양식은 Day 20 turn 1 본 비전 SSOT, D-17 정합)
- 본 비전 양식 매핑: `members[].name/model/sequence/blocked_by` → spawn 인자 / `task_template.subject/description/output_format_required` → SendMessage 페이로드 / `protocol.steps/review_cycle_cap` → SKILL.md Phase 흐름
- **결정론은 스크립트, 판단·종합은 LLM** (feedback 차용)

### 12-3. 6 PowerShell 헬퍼 신설 (총 ~750줄)
| 스크립트 | 라인 | 역할 |
|---------|-----|------|
| `preflight.ps1` | ~135 | env (SUBAGENT_MODEL 부재) + claude-code 버전 (≥2.1.126) + subagent 컨텍스트 + PowerShell 5.1+ + pyyaml 5 검사. JSON stdout. |
| `resolve-preset.ps1` | ~245 | preset YAML → JSON 메타. Mode 3종: `-Preset <name>` / `-List` / `-ValidateOnly -Path <file>`. Python `yaml.safe_load` 호출 + `PYTHONIOENCODING=utf-8` 강제 |
| `run-team.ps1` | ~95 | sentinel.json 신설 (start_time + deadline + members + review_cycle_count). **tool 호출 안 함** (v2 spec §2 경계선) |
| `monitor-team.ps1` | ~140 | 주기 상태 덤프. status = active/stale/zombie/orphan. 표 또는 JSON 출력 |
| `validate-team.ps1` | ~165 | 5 검증 (orphan/deadline/cycle_cap/duplicate_owner/zombie_task). `-AllTeams` 일괄 모드 |
| `shutdown-team.ps1` | ~135 | R-5 정합 (기본=archive `.archived/<team>-<timestamp>/`, `-Force`만 즉시 삭제). `.shutdown_signal` 파일 생성 |

### 12-4. 인코딩 이슈 2건 발견 + 정정
- **이슈 1 (PS 5.1 here-string parse fail)**: 한글 주석 포함 ps1 파일이 UTF-8 BOM 부재 시 PS 5.1 가 CP949 fallback → here-string 종결 (`"@`) 인식 실패 → `Unexpected token '}'` parse 에러. **정정**: 6 파일 모두 `[System.IO.File]::WriteAllText($path, $content, [UTF8Encoding]::new($true))` 로 BOM 추가. 재 parse 6/6 OK.
- **이슈 2 (Python stdout cp949 codec)**: `resolve-preset.ps1` 의 `python -c` 호출 시 stdout 인코딩이 cp949 fallback → em-dash (—) 같은 유니코드 문자 출력 실패. **정정**: `$env:PYTHONIOENCODING = 'utf-8'` + `sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')` 이중 보장.
- **R-15 신설**: PowerShell 5.1 한글 주석 ps1 = UTF-8 BOM 의무 (Day 10 이월 ⑤ 인코딩 이슈 재발 방지)

### 12-5. 단위 테스트 9/9 PASS
| 테스트 | 결과 |
|-------|------|
| `preflight -SkipTmux` | exit=0, ok=True, 5/5 ok PASS (env 부재 + claude-code 2.1.128 ≥ 2.1.126 + 메인 컨텍스트 + PS 5.1.26100 + pyyaml 6.0.3) |
| `resolve-preset -List` | 5 preset 정확 추출 (review/debug/research/docs-research/harness-design) |
| `resolve-preset -Preset review` | members=3 + task_graph=3 + cap=3 + proto_steps=4 정합 |
| `resolve-preset -ValidateOnly -Path debug.yaml` | valid=True, issues=0 |
| `run-team -Team test-001 -SentinelInit -TimeoutMinutes 5` | sentinel.json 신설 PASS |
| `monitor-team -Team test-001` | status=active, deadline_remaining=5min, members=2 |
| `shutdown-team -DryRun` | action=dry-run, moved=1 |
| `shutdown-team` (archive) | action=archive, archive_path=`.archived/test-001-<timestamp>` PASS |
| `validate-team -AllTeams` | **부수 발견 R-14**: 운영 ~/.claude/teams/ 71개 팀 잔존 = 全 orphan (sentinel.json 부재) — 본 스크립트의 실제 가치 결정적 검증 |

### 12-6. 운영 sync 6쌍 SHA256 MATCH
- `~/.claude/skills/agent-team-manager/scripts/` 신설 + 6 ps1 sync
- D-11/D-16/D-22 (scripts 단방향 sync) 정책 정합

| 파일 | SHA256 (앞 16자) |
|------|----------------|
| preflight.ps1 | `8CE07A56B870338A...` |
| resolve-preset.ps1 | `1CEE810875A33E90...` |
| run-team.ps1 | `45CA0BD9A8C72FB2...` |
| monitor-team.ps1 | `6C6FE002B70BA259...` |
| validate-team.ps1 | `E73DC0E56F50BDA4...` |
| shutdown-team.ps1 | `ADD303E8A79F09AB...` |

### 12-7. 결정 (D-22~D-23) + R 부수 발견 (R-14·R-15)
- **D-22** — scripts/ 디렉토리 = 본 비전 D-11/D-16 단방향 sync 정책 일관 (스테이징 `skills/agent-team-manager/scripts/` ↔ 운영 `~/.claude/skills/agent-team-manager/scripts/`). **이유**: skills/agents/rules/presets 와 동일 패턴 = drift 방지 통일.
- **D-23** — 본 비전 양식 (Day 20 turn 1) vs v2 spec 양식 충돌 시 = **D-17 본 비전 SSOT**. resolve-preset.ps1 는 본 비전 양식 (`members[].sequence/blocked_by/dimension/focus_areas`) 만 파싱. v2 spec 양식 (`members[].role/tools_allowlist/initial_task/depends_on`) 은 미지원. **이유**: Step A·B·C 자산 1:1 호환 = 자산 재사용 최대화.
- **R-14 (부수 발견)** — `validate-team -AllTeams` 결과 = 운영 ~/.claude/teams/ 71개 팀 잔존, 全 orphan 판정. **시사점**: 본 스크립트의 실제 가치 결정적 검증 + 운영 정리 작업 별도 turn 필요 (`.todo.md` 신규 항목 #021 등록).
- **R-15 (이슈 1·2 통합 교훈)** — PowerShell 5.1 한글 주석 ps1 = UTF-8 BOM 의무 + Python `print` 호출 시 `PYTHONIOENCODING=utf-8` 강제 의무. **시사점**: 향후 ps1 헬퍼 신설 시 본 패턴 dogfood 적용 (Day 10 이월 ⑤ 인코딩 회귀 방지).

### 12-8. 산출물
- `skills/agent-team-manager/scripts/*.ps1` × 6 신설 (~750줄)
- `~/.claude/skills/agent-team-manager/scripts/*.ps1` × 6 sync MATCH
- `docs/history/2026-05-05.md` §12 신설 (~75줄, 이전 ~195 → ~270줄)
- `docs/history/index.md` Day 20 행에 turn 3 결과 append
- `.todo.md` #009-D 갱신 (D-1 完, D-2 + #009-E 잔여) + #021 신설 (운영 71 orphan 팀 정리)
- `.checklist.md` → `.backups/.checklist.done.009-D-1.2026-05-05.md` archive (git add -f)
- commit: `feat+docs: Day 20 turn 3 — #009-D-1 PASS (scripts/ 6 PowerShell 헬퍼 신설, preset YAML 파싱 자동화)`

### 12-9. 잔여 작업 (#009-D-2 + #009-E + #021)
- **#009-D-2**: reference/ 4 (patterns·anti-patterns·errors·presets) + SKILL.md v2 → v2.5 (scripts/reference 호출 박기). 추정 1 turn.
- **#009-E**: feature·security 2 preset = 새 agent 7 (lead/frontend/backend/tester + SAST/DAST/compliance) 신설 선행 + preset 2 신설. 추정 2 turn.
- **#021**: 운영 ~/.claude/teams/ 71 orphan 팀 정리 (`validate-team -AllTeams` 실행 → archive 대상 결정 → `shutdown-team` 일괄 호출). 추정 30분.

### 12-10. 양식 v2 dogfood + /checklist mode=code 누적 (Day 20 turn 3)
- 양식 v2 dogfood **11건째** (turn 2 = 10건 → 본 turn = 11건)
- `/checklist mode=code` **첫 사례** (이전 全 mode=mixed)
