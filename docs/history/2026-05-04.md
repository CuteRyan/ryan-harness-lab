# Day 19 — 2026-05-04 — issue#32732 종결 (#015 PASS + #018 PASS + #019 PASS) + #014 PASS (PM 외부 리서치 의무화 + agents 스테이징 도입) + Phase 1 진입 가능 최종 마킹

> Day 18 후속 turn 5 (Day 19, 2026-05-03) 의 settings.json B-1 setup (env 임시 제거) → 사용자 메인 Claude Code 재실행 → 본 turn 6 (Day 19, 2026-05-04) 새 세션 진입 후 #015 검증 PASS + 글로벌 강제 규칙 신설 → turn 7 #018 강제 훅 신설 + 우회 패턴 세계 1호 검증 → turn 8 #019 fallback C+ 효과 검증 완료 = **issue#32732 종결** → turn 9 #014 PM 외부 리서치 의무화 + agents 스테이징 도입 = **Phase 1 사전 정리 1/2 PASS, #012 진입 대기**.

---

## 1. HANDOFF turn 5 인수 + 사전 체크 (turn 6 첫 단계)

### 1-1. /handoff done (소멸 정책 **11회차 검증**)
- HANDOFF turn 5 (2026-05-03 setup 인계서, 172줄) Read → settings.json B-1 setup 완료 / 백업 보존 / 본 세션 = 메인 재실행 후 첫 세션
- `mv HANDOFF.md .backups/HANDOFF.done.2026-05-04.md` — 정상 소멸
- 소멸 정책 **11회차 검증** (10회차 = turn 4 → turn 5)

### 1-2. 사전 체크 (사용자 "체크해봐" 요청 → 정합성 검사 4건)
| 항목 | 결과 |
|------|------|
| settings.json B-1 setup | JSON VALID, env=`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` 만 잔존 ✓ |
| 백업 (`settings.json.bak.20260503_phaseB1`) | 4030B 보존, env=sonnet 포함 ✓ |
| 라이브 process env | `CLAUDE_CODE_SUBAGENT_MODEL` 부재 (Quick Start ① 사전 PASS) |
| .todo.md / HANDOFF 정합 | 발견 stale 2건 (HANDOFF L168 commit hash + 양쪽 경로 `2026-05-01_master-design/` → `agent-office-masterplan/`) → turn 6 흡수 정정 |

## 2. /checklist 작성 + 사용자 지적 흡수 (글로벌 갱신)

### 2-1. 1차 체크리스트 (Step 1~6)
- mode=mixed, 작업명 = "issue#32732 fallback C+ 검증 (#015) Step 1~6"
- Step 1 env 재확인 / Step 2 TeamCreate + spawn × 4 / Step 3 자기보고 / Step 4 PASS 판정 / Step 5 환원 (mandatory) / Step 6 마스터플랜 갱신 + 인계 정리

### 2-2. 사용자 지적 (게이트 5 자기 비판 영구 보존)
> "글로벌 메모리랑 룰도 관련해서 수정해야하지 않니?? 지금 에이전트 스킬관련한거 맞지?"

- 1차 체크리스트 = 마스터플랜 + 인계 정리만 포함, **글로벌 갱신 누락** 자체 검증 부족
- 보강 후 Step 6 분할: **6-A** (마스터플랜) / **6-B** (글로벌 강제 규칙 신설/갱신, 신규) / **6-C** (인계 정리 + stale 정정)
- 보강 없었으면 fallback C+ 결정이 운영에 강제되지 않음 = 본 검증 의미 절반 손실

### 2-3. 권장안 컨펌 (사용자 승인)
- (B) memory `agent-office-vision.md` 흡수 (별도 신설 X) — D-4 SSOT 와 자연 정합
- (A) 본 #015 commit 에 묶기 — 의미 단위 동일

## 3. Step 1~5 검증 (#015 PASS)

### 3-1. Step 1 — env 빈 값 확인
- 라이브 process env: `CLAUDE_CODE_SUBAGENT_MODEL` 부재 (Quick Start ① PASS, 06 §9.7 §1 한계 해소)
- settings.json env 블록: `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` 만 잔존

### 3-2. Step 2 — TeamCreate `model-fallback-verify-v2` + Agent spawn × 4
- TaskCreate × 4 (병렬 spawn 추적)
- 4 spawn 병렬 호출 (Agent tool, team_name=model-fallback-verify-v2)

### 3-3. Step 3 — 자기보고 수집 (SendMessage 회신 의무 발견)
- spawn 4건 모두 prompt 처리 후 idle 만 보고, **응답 메시지 송신 안 함** = SendMessage 도구 미사용 + 텍스트 출력만
- 메인 lead 가 4건에 SendMessage 로 "회신 의무" 명시 → 4 spawn 모두 자기보고 SendMessage 회신 받음
- **운영 교훈** (Phase 1 PM/워커 시스템 프롬프트 흡수 가치): "spawn 응답은 SendMessage 도구 사용 의무, 텍스트 출력만으로는 lead 가 받지 못함"

### 3-4. Step 4 — 자기보고 결과 + PASS 판정
| spawn | 명세 | 시스템 프롬프트 model | 결론 (자기보고) | 자식 env SUBAGENT_MODEL |
|---|---|---|---|---|
| A | `model="opus"` 명시 | Opus 4.7 (1M context) | "불확정 — 추정 Opus" (메타 자각) | 부재 |
| B | `pm-test` (frontmatter `model: opus`) | Opus 4.7 | Opus | 부재 |
| C | model 생략 + frontmatter X | **Opus 4.7** (예상 외) | Opus | 부재 |
| D | `model="sonnet"` 명시 | Sonnet 4.6 (`claude-sonnet-4-6`) | Sonnet | 부재 |

**PASS 판정**: A=Opus AND B=Opus AND D=Sonnet 4/4 정합 → ✅ PASS

### 3-5. ⚠️ 부수 발견 (강제 훅 정당성 결정적 강화)
- C (디폴트) = Opus → "워커 디폴트 = Sonnet" 보장 메커니즘이 settings.json env 외 부재
- → fallback C+ 의 "모든 spawn model 강제 명시" 는 강제 훅 (PreToolUse Agent matcher) 없이는 인간 실수로 Opus 자동 배치 + 비용 폭증 위험
- **#018 신설** (강제 훅, Phase 1 사전조건) 정당성 = 부수 발견에서 결정적 도출

### 3-6. Step 5 — settings.json mandatory 환원
- `Copy-Item ~/.claude/settings.json.bak.20260503_phaseB1 ~/.claude/settings.json -Force`
- JSON VALID, env keys = `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` + `CLAUDE_CODE_SUBAGENT_MODEL=sonnet` 둘 다 잔존
- SHA256 백업↔환원본 = MATCH (`5D708DBA...089E4`)

## 4. Step 6-A — 마스터플랜 갱신

- `06_issue32732_experiment.md §10` 신설 (~75줄): 10.1 Step 1 / 10.2 Step 2~3 spawn 결과표 / 10.3 Step 4 PASS 판정 + 핵심 검증 3건 / 10.4 부수 발견 (강제 훅 정당성) / 10.5 fallback C+ 최종 확정 메커니즘 3중화 + Phase 1 진입 가능 마킹 / 10.6 다음 작업 (Step 5~6 + Phase 1 인프라)
- `04_masterplan.md §8.2`: "현 상태" 행 갱신 ("3차 실험 완료, fallback C+ 최종 확정") + **3차 실험 결과 박스 추가** (D-32732-A3, ~10줄)
- `04 §9.1`: model override 행 출처 갱신 ("06 §9.4 (turn 3) + §10 (turn 6 PASS)") + 글로벌 강제 규칙 위치 명시

## 5. Step 6-B — 글로벌 강제 규칙 신설/갱신 (사용자 지적 흡수, **본 turn 핵심 부수 효과**)

### 5-1. `rules/agent-spawn-model.md` 신설
- 스테이징: `Harness-engineering/rules/agent-spawn-model.md` (~50줄, 6 섹션)
- 운영 sync: `~/.claude/rules/agent-spawn-model.md` — SHA256 MATCH `7AF2BB63AF36DC8D...A174E`
- 내용: 핵심 원칙 (모든 spawn `model` 명시 필수) + 의무 조건 + 의무 형식 + 예외 + 위험·강제 훅 (Phase 1) + 외부 리서치 + 출처

### 5-2. `~/.claude/CLAUDE.md` Agent Preferences 5번째 규칙 추가
- L31 ⚠️ 금지 다음에 ⚠️ model 명시 필수 항목 1줄 추가
- 45줄 → 46줄 (200줄 한계 충분)
- 내용: "모든 Agent spawn 에 `model` 파라미터 명시 필수 (PM=opus / 워커=sonnet) — 누락 시 디폴트 = 메인 model 상속(Opus) → D-4 비용 배분 깨짐. 상세: `~/.claude/rules/agent-spawn-model.md`"

### 5-3. `memory/agent-office-vision.md` L115 정정
- 이전: "⚠️ 모델 지정 issue#32732 — `CLAUDE_CODE_SUBAGENT_MODEL=sonnet` env + frontmatter 이중 보장"
- 이후: "✅ 모델 지정 issue#32732 — **fallback C+ 최종 확정** (2026-05-04 turn 6 PASS, 06 §10): env=잔존 시 frontmatter+명시 model 모두 무력 / env=제거 시 명시 model + frontmatter 정상 작동 + 디폴트 = 메인 model 상속(Opus). 운영 강제 = ① settings.json env 영구 제거 (강제 훅 후) ② 모든 spawn `model` 파라미터 명시 ③ PreToolUse Agent matcher 강제 훅 (Phase 1 인프라). 글로벌 규칙 = `~/.claude/rules/agent-spawn-model.md` + `~/.claude/CLAUDE.md` Agent Preferences 5번째 규칙"

## 6. Step 6-C — 인계 정리

- 4 spawn shutdown_request 발송 → TeamDelete 호출 → `~/.claude/teams/model-fallback-verify-v2/` 디렉토리 + worktree 자동 정리 (archive 아닌 직접 삭제, success 응답)
- `.todo.md`: #015 → 완료 섹션 이동 (정확한 경로 = `agent-office-masterplan/`, stale 정정 흡수) + #014 unblock + **#018 신설** (강제 훅, priority high)
- HANDOFF turn 5 archive (`.backups/HANDOFF.done.2026-05-04.md`, 172줄, 소멸 정책 11회차 검증)
- HANDOFF turn 6 신설 (양식 v2 두 번째 적용 케이스, dogfood 누적 1건)
- `docs/history/2026-05-04.md` 신설 (본 파일) + `index.md` Day 19 행 추가

## 7. 결정 사항 (Day 19 turn 6)

| # | 결정 | Why |
|---|---|---|
| **D-1** | fallback C+ **최종 확정** = 메커니즘 3중화 (settings.json env 영구 제거 + 메인 재시작 + 모든 spawn `model` 명시 + PreToolUse Agent matcher 강제 훅) | 06 §10 PASS = env 제거 시 명시 model + frontmatter 정상 작동 결정적 검증 |
| **D-2** | settings.json env=sonnet **임시 환원** (강제 훅 신설 전까지) | env=빈 값 보존 시 워커 디폴트 = Opus 자동 배치 위험 (Spawn C 부수 발견). 강제 훅 신설 후 영구 제거 안전. |
| **D-3** | 글로벌 강제 규칙 신설 = `rules/agent-spawn-model.md` + `~/.claude/CLAUDE.md` Agent Preferences 5번째 규칙 + `memory/agent-office-vision.md` L115 정정 | 사용자 지적 = 검증 결과를 글로벌 운영에 강제하지 않으면 의미 절반 손실. 게이트 5 자기 비판 (1차 체크리스트 누락 자체 검증). |
| **D-4** | #014 unblock + priority normal 유지 + 다음 세션 인계 | PM 운영 메커니즘 안정 후 의무 추가 적합. fallback C+ 메커니즘 = base, PM 외부 리서치 = α. |
| **D-5** | #018 신설 = priority high (Phase 1 진입 사전조건) | 강제 훅 미신설 시 settings.json env 영구 제거 불가 → fallback C+ 영구 적용 불가 → Phase 1 진입 불가. |

## 8. 반박 이력 (R-N, 본 turn 신규)

> 메모리 `agent-office-vision.md §주인님 반박 이력` 누적 정합 유지

- **(메모리 R-1~R-5 와 별개, 본 turn 한정)** 메인 Claude 의 1차 체크리스트가 글로벌 갱신 누락 → 사용자 즉시 지적 ("글로벌 메모리랑 룰도 관련해서 수정해야하지 않니??") → 보강 + Step 6-B 신설
- **다음 turn 메인 Claude 가 동일 패턴 회피**: 검증 작업 시 "결과 보고" 만 보지 말고 **결과의 운영 강제 메커니즘** 까지 함께 설계 (글로벌 rules / CLAUDE.md / memory 자동 영향 분석)
- **회고 시점**: #008 due 2026-05-08 (1주 후) + 4 스킬 양식 v2 dogfood 회고 (#016·#017) 와 통합

## 9. dogfood 누적 (양식 v2)

- HANDOFF.md 양식 v2 (2026-05-02 turn 4 신설, turn 5 첫 적용) → turn 6 두 번째 적용 = **dogfood 1건 누적**
- drift 발견 안 됨 (turn 5 → turn 6 전환 자연스러움). 단 turn 5 의 "관련 파일" 섹션 경로 stale (이력 보존, turn 6 자체 정확성으로 자연 정정)
- 양식 v2 회고 (#016 4 스킬 메타 디테일·#017 양식 정합) 1주 후 통합

## 10-A. 마스터플랜 정합성 보강 (사용자 "빠진거 없이" 지적 흡수, 추가 commit)

본 turn 6 commit (`670a863`) 후 사용자 "빠진거 없이 하면 좋겠는데" 지적 → **Phase 4 더블 체크 정밀 재실행** → 마스터플랜 4 stale 발견:

| 파일 | 위치 | 정정 전 | 정정 후 |
|---|---|---|---|
| `05_migration_plan.md` | L249 (롤백 트리거) | "issue#32732로 PM이 Sonnet으로 spawn → env 설정 재확인" | "fallback C+ 절차 실행 (env 영구 제거 + 메인 재시작 + spawn `model="opus"` 명시 + 강제 훅)" |
| `05_migration_plan.md` | L377 (트러블슈팅) | "CLAUDE_CODE_SUBAGENT_MODEL + pm.yaml model 재확인" | "fallback C+ 절차 + 글로벌 규칙 `agent-spawn-model.md`" |
| `05_migration_plan.md` | L399 (위험 표) | "중간 위험 — 이중 보장 설정 + 실험 확인" | "✅ 해소 (2026-05-04 turn 6) — fallback C+ 최종 확정" |
| `agent-office-vision.md` (docs SSOT 본체) | D-4 (L194) | "운영 강제 footnote 부재" | "운영 강제 (turn 6 PASS 후 확정) footnote 추가 — fallback C+ 메커니즘 3중화 + 글로벌 규칙 + #018 사전조건 명시" |

**Why 빠뜨렸나** (회고 가치): 1차 Step 6-B 에서 `rules` + `~/.claude/CLAUDE.md` + `memory` 만 갱신, **마스터플랜 다른 파일 (05 + vision SSOT 본체)** 정합성 미반영. memory architecture 규칙 (docs = SSOT) 위반 — memory 만 정정하고 docs SSOT 안 정정 = SSOT 깨짐.

**다음 turn 회피 패턴**: 검증 결과 반영 시 (a) 핵심 보고서 (06 §10) (b) 직접 출처 마스터플랜 (04 §8.2) (c) **연관 마스터플랜 파일 전체** (00·05·vision SSOT) (d) 글로벌 강제 (rules·CLAUDE.md·memory) **4 단계 모두** 점검 의무.

## 10. 영구 기록 가치 평가 (Phase 6 의무)

다음 5 조건 中 4 해당 → `/project-history update` (본 파일) 의무 실행:
- ✅ 새로운 결정 사항 발생 (D-1 ~ D-5)
- ❌ 외부 검수 (`/feedback`) 결과 반영 (본 turn 미사용 — 1차 검증은 자체 spawn 검증)
- ✅ 마이그레이션 / 신설 스킬 / 신설 규칙 (`rules/agent-spawn-model.md` 신설 + 글로벌 CLAUDE.md 갱신)
- ✅ dogfood 데이터 누적 (양식 v2 turn 5 → turn 6 1건)
- ✅ 회고 가치 있는 패턴 발견 (메인 Claude 의 글로벌 갱신 누락 = 검증 작업 기본 누락 패턴)

---

# Day 19 turn 7 (2026-05-04 후속) — #018 강제 훅 신설 + 우회 패턴 세계 1호 검증 PASS

## 1. 미션 + 결정 사항 (D-6 ~ D-9)

**미션**: turn 6 §10.5 의 "Phase 1 진입 사전조건 = 강제 훅 신설" 직접 구현 (.todo #018) + Issue #26923 reporter 미검증 가설 (`permissionDecision: deny` 우회) 세계 1호 검증.

**진행 옵션**: HANDOFF turn 6 §🚨 결정 1 = **A 안 단독 진행** (사용자 확정).

**D-6 (turn 7 신규) — 우회 패턴 채택**: Step 0 외부 리서치에서 Issue #26923 (CLOSED, exit 2 100% failure) + #40580 (OPEN) 발견 → 강제 훅 본래 설계 (exit 2 차단) **외부 알려진 버그로 무력화 가능** 위험 → 사용자 재승인 거쳐 `permissionDecision: deny` JSON + exit 0 우회 패턴 채택. 작동 시 fallback C+ 살림, 실패 시 D 안 후퇴.

**D-7 (turn 7 신규) — `Task|Agent` 정규식 OR matcher**: 검색 결과 통념은 `Task` matcher 였으나, 본 환경 (claude-code 2.1.126) 은 `Agent` 라는 가능성. 양쪽 등록 (`"matcher": "Task|Agent"`) = 환경 호환성 + 미래 변경 대비.

**D-8 (turn 7 신규) — env 제거 시점**: 강제 훅 라이브 검증 PASS 직후 즉시 (HANDOFF turn 6 결정 4 = "PASS 직후 즉시" 정합). 효과 발효는 사용자 메인 재시작 후 다음 세션.

**D-9 (turn 7 신규) — #018 완료 vs #019 신설**: #018 = 본 turn 강제 훅 신설 + 라이브 차단 검증 PASS 시점에 완료 마킹. 다음 세션 env 부재 환경 4 spawn 라이브 재검증은 **#019 신설** 로 분리. 사유 = #018 의 본질 = "강제 훅 신설" 이지 "fallback C+ 효과 측정" 이 아님. 분리해야 책임 경계 명확 + 다음 세션 진입 의무 명확화.

## 2. 외부 리서치 + 충격 (Step 0)

**자기 적용 리서치 의무** (`rules/research-mandatory.md` §1) — 강제 훅 설계 진입 전 PreToolUse 훅 사양 + Agent matcher 외부 사실 확인.

**핵심 충격 출처 2건**:
- [Issue #26923 (CLOSED, 2026-02-19~03-03)](https://github.com/anthropics/claude-code/issues/26923) — `PreToolUse hook exit code 2 does not block Task tool calls`. 19/19 (100%) failure 결정적 재현. exit 2 stdout JSON 도 동시 무시. 우회 가설 = `permissionDecision: deny` (reporter 본인 미검증 명시).
- [Issue #40580 (OPEN, 2026-03-29)](https://github.com/anthropics/claude-code/issues/40580) — 동일 버그 재확인. "This makes it impossible to enforce tool usage policies on subagents via hooks." 패치 미언급.

**충격**: 본 작업의 핵심 가정 (exit 2 차단) 이 외부 사실로 무력화 가능 → 체크리스트 §승인 규칙 = 치명 항목 변경 = **사용자 재승인 거쳐 A 안 채택**.

**대안 평가** (사용자에게 4 안 제시 후 A 안 결정):
- A. 우회 패턴 검증 (선택 ✅) — 비용 저, 손실 0, 작동 시 ++EV
- B. 훅 = 경고 격하 (strict → soft) — 강도 약화
- C. PostToolUse 사후 감지 — 차단 못함, 가시화만
- D. env 보존 + 글로벌 룰 자기 검열 — 하네스 컨셉 위반

## 3. 인프라 신설 (Step 1·2)

### 3.1 훅 본체 (`hooks/pretooluse-agent-model-required.{sh,py}`)

| 파일 | 줄수 | SHA256 (스테이징↔운영 MATCH) |
|---|---|---|
| `.sh` (wrapper) | 30 | `8559463CB6D5...3029` |
| `.py` (본체) | 95 | `E4B0B37D575C...BEE4` |

**검사 순위** (rules `agent-spawn-model.md` §3 정합):
1. `tool_name in ("Task", "Agent")` 매칭 (settings.json matcher 1차 거름 + 본체 방어적 재검사)
2. `tool_input.model in {opus, sonnet, haiku}` → 통과
3. `subagent_type` 의 `~/.claude/agents/{name}.md` frontmatter `model:` 명시 → 통과 (rules §3 예외)
4. 위 모두 아니면 → `permissionDecision: deny` JSON 출력 + exit 0 + stderr 가시화

**단위 테스트 5/5 PASS**:
| Test | 입력 | 결과 |
|---|---|---|
| 1 | tool_name="Bash" | exit 0 무출력 (무관 tool 통과) |
| 2 | Task + model="opus" | exit 0 + stderr "PASS via model=opus" |
| 3 | Task + missing + general-purpose | exit 0 + stdout JSON deny + stderr 메시지 |
| 4 | Task + missing + pm-test (frontmatter) | exit 0 + stderr "PASS via frontmatter" |
| 5 | Task + model="invalid" | exit 0 + stdout JSON deny |

**부수 정정 (UTF-8)**: stderr 출력의 `§` 가 Git Bash 표시 단계에서 깨짐 (`��`) → ASCII 호환 (`section 2`, `section 3`) 으로 변경. SHA256 재산출 매칭.

### 3.2 settings.json 등록

- 백업: `~/.claude/settings.json.bak.20260504_phase1` (SHA256 `5D708DBA9E63...089E4` = turn 6 환원본 동일)
- 추가: `hooks.PreToolUse[]` 5번째 객체 = `{"matcher": "Task|Agent", "hooks": [{"type":"command","command":"powershell ... 'pretooluse-agent-model-required.sh'"}]}`
- JSON valid 검증 PASS

## 4. 라이브 4 spawn 검증 (Step 3)

**결정적 PASS 결과**:

| Spawn | model 입력 | tool_name (메인 호출) | 차단/통과 | 자식 model 자기보고 | 메커니즘 |
|---|---|---|---|---|---|
| **C** (model 누락) | (없음) | Agent | **차단 PASS** ✅ | (spawn 미실행) | `permissionDecision: deny` 우회 작동 |
| A | `opus` | Agent | 통과 | `claude-sonnet-4-6` ⚠️ | 훅 통과 + env 덮어씀 (turn 6 anomaly 재해석) |
| B | `sonnet` | Agent | 통과 | `claude-sonnet-4-6` | 훅 통과 + env 정합 |
| D | `gpt-5` (invalid) | Agent | **SDK 차단 PASS** ✅ | (spawn 미실행) | InputValidationError (이중 보장) |

**3가지 결정적 가설 동시 PASS**:
1. **`Agent` matcher 작동** — 메인 에러 메시지 = `"PreToolUse:Agent hook blocking error from command: ..."` 명시 → 본 환경 tool name = `Agent` (Issue #26923 의 "Task" 통념과 다름, claude-code 2.1.126 기준)
2. **settings.json `hooks` hot-reload 작동** — 새 matcher 등록 직후 즉시 차단 효과 발효 → turn 4·6 의 "hot-reload 비작동" 가설은 **env 섹션 한정** 결정적 확인 (hooks vs env 분리 메커니즘)
3. **`permissionDecision: deny` + exit 0 우회 작동** — Issue #26923 reporter 본인 미검증 가설 = **세계 1호 검증 PASS**. exit 2 무시 버그를 회피하는 차단 메커니즘 확정.

## 5. ⚠️ turn 6 anomaly 재해석 (R-6 turn 7 신규)

**Spawn A 결과 충돌**:
- §10 turn 6: A `model="opus"` → 자식 = Opus (정합)
- 본 turn 7: A `model="opus"` → 자식 = Sonnet (env 덮어씀)

**해석**: 동일 환경 (env=sonnet 보존, 메인 재시작 후) 에서 결과 다름 → turn 4 + turn 7 결과 일치 ("env 가 명시 model 도 무력화") = **결정적 재현 강화**. turn 6 결과 = anomaly 가능성 (메인 재시작 직후 cache miss 또는 자기보고 신뢰도 한계, 06 §11.4 §9.5 인용).

**의의 (R-6)**: "fallback C+ 의 핵심 메커니즘은 env 제거 우선" 이 결정적 확정. 강제 훅만으로는 env 덮어쓰기 차단 못함. **env 제거 + 강제 훅 동시 조합** 이 fallback C+ 의 영구 적용 조건.

## 6. Step 4·5·6 처리

**Step 4 (env 영구 제거)**:
- `~/.claude/settings.json` 의 `env.CLAUDE_CODE_SUBAGENT_MODEL` 라인 영구 삭제 (commit)
- JSON valid PASS, 백업 보존
- **효과 검증** = 사용자 메인 재시작 후 다음 세션 (turn 8) — env 변수는 메인 프로세스 cache 라 hot-reload 비작동 (turn 4·6 §6-1 결정적 재현)

**Step 5 (마스터플랜·rules·memory 갱신)**:
- `06_issue32732_experiment.md §11` 신설 ~115줄 (402→517줄)
- `04_masterplan.md §8.2` 4차 실험 박스 추가 + turn 6 박스에 anomaly 재해석 메모 추가
- `04_masterplan.md §9.1` model override 행 갱신 — 강제 훅 활성 마킹 + Issue #26923/#40580 인용
- `rules/agent-spawn-model.md §4` "강제 훅 (Phase 1 신설 예정)" → "강제 훅 (활성, 2026-05-04 turn 7 PASS)" 상태 변경 + 검사 순위 + 외부 출처 추가
- 운영 sync `~/.claude/rules/agent-spawn-model.md` SHA256 MATCH `A68091A1F684...84C4`
- `memory/agent-office-vision.md` L115 정정 — turn 6 → turn 7 + "fallback C+ 영구 적용 진입" + 우회 패턴 인용

**Step 6 (인계)**:
- `.todo.md` #018 완료 마킹 + #019 신설 (priority high, turn 8 env 부재 라이브 재검증)
- 본 history Day 19 turn 7 섹션 추가
- `index.md` Day 19 행 갱신 (turn 7 추가)
- HANDOFF turn 7 신설
- `.checklist.md` → `.backups/`
- commit + push

## 7. 결정·반박·④ 누적 (turn 7)

- D-6: 우회 패턴 채택 (Issue #26923 reporter 미검증 가설 검증)
- D-7: `Task|Agent` 정규식 OR matcher
- D-8: env 제거 시점 = 강제 훅 PASS 직후 즉시
- D-9: #018 완료 + #019 신설 (책임 경계 분리)
- R-6: turn 6 A=Opus anomaly 재해석 (turn 4 + turn 7 일치 = env 덮어쓰기 결정적 재현)

## 8. dogfood 누적 (양식 v2)

- HANDOFF.md 양식 v2 적용: turn 5 (1번째) → turn 6 (2번째) → **turn 7 (3번째)** = dogfood 2건 누적
- drift 발견: turn 6 §🚨 결정 6 (A·B·C 묶음 처리 단위) 의 "B = #018 + #014 + #012 + #016/#017 4 묶음 turn" 안 = 본 turn 에 4 안 모두 처리 가능했지만, **#018 자체에 외부 리서치 충격 (Issue #26923) 발생으로 분량 +30분** → A 안 단독으로 자연 결정. 양식 v2 의 결정 사항 enum 우선순위 체계 + 분기 가능성 마킹은 적절 (drift 0).
- 양식 v2 회고 (#016 4 스킬 메타 디테일·#017 양식 정합) 1주 후 통합

## 9. 영구 기록 가치 평가 (Phase 6 의무)

다음 5 조건 中 5 모두 해당 → `/project-history update` (본 파일) 의무 실행:
- ✅ 새로운 결정 사항 발생 (D-6 ~ D-9 + R-6)
- ✅ **외부 검수 / 외부 리서치 결과 반영** (Issue #26923 + #40580 인용 = 본 작업 핵심 변경 트리거)
- ✅ 마이그레이션 / 신설 스킬 / 신설 규칙 (`pretooluse-agent-model-required.{sh,py}` 신설 = 강제 훅 1건 + settings.json env 제거 = 운영 환경 영구 변경)
- ✅ dogfood 데이터 누적 (양식 v2 turn 7 = 3건 누적)
- ✅ 회고 가치 있는 패턴 발견 — **세계 1호 검증 PASS** (Issue #26923 reporter 미검증 가설 = `permissionDecision: deny` 우회 작동 결정적 확인). 향후 Anthropic Issue 본 turn 결과 인용 가치.

## 10. 본 turn 후속 대화 (HANDOFF 보강 입력)

본 turn 7 종결 후 (commit `3fd2fd4` push 완료 후) 사용자와의 후속 대화에서 다음 합의·영감 추출 → HANDOFF 결정 2/5/6 보강:

### 10-1. PM=Sonnet 후퇴 합의 (결정 5)
사용자 명시: "이게 실패로 끝나면 그냥 어쩔 수 없이 pm 도 소넷으로 가야겠다"
- #019 FAIL 시 → 자동 후퇴안 = env 부활 + PM=Sonnet + 메인 (Opus, 사용자 직접 선택) PM 비판자 흡수
- 검증 근거: turn 7 Step 3 Test B (`model="sonnet"` 명시 → 자식=Sonnet 정합) PASS = Sonnet teammate spawn 100% 작동 확인
- 5층 위계 → 3층 단순화 (사용자 / 메인=Opus / 워커=Sonnet) — D-4 의 "+90.2% 성능" 패턴 (Opus lead + Sonnet worker) 그대로 작동. 단지 별도 PM layer 폐기.

### 10-2. agent-team-manager v2 (#009) 의 /feedback 패턴 차용 영감 (결정 6)
사용자 직관: "에이전트 팀 스킬에서 피드백 스킬에서 힌트를 얻을 수 있을 것 같다"
- /feedback 5 핵심 패턴 추출: ① 단발 + 격리 spawn ② scripts 외부화 (orchestrate / run-X / validate / prompts) ③ orchestrate 병렬 + 종합 ④ Validation Gate 5게이트 ⑤ 외부 검증 훅 (sycophancy-check)
- 가장 강한 시너지 = ① + ② + ④ (구조 + 검증 동시). 마스터플랜 §5 (scripts 6개 + presets 5 YAML) 와 정합.
- 다음 세션 #009 v2 본체 신설 시 본 영감 적용 의무.

### 10-3. statusline c 풍부형 설정 (부수)
사용자 `/statusline` 호출 → statusline-setup agent (`model="sonnet"` 명시 spawn → 강제 훅 정상 통과 = 본 turn 7 강제 훅 라이브 추가 검증) → c 풍부형 (`claude-sonnet-4-6 | ~/projects/myapp | ctx: 42%`) 채택. `~/.claude/settings.json` L130-133 + `~/.claude/statusline-command.sh` 신설. 메인 재시작 시 발효 (env 부재 + 강제 훅 + statusline 동시 발효).

### 10-4. 의의
사용자가 fallback D 의 운영 가능성 + agent-team-manager v2 설계 방향 (peer 스킬 영감) 모두 사전 합의 → 다음 세션이 #019 결과 어느 쪽으로 갈지 (PASS/FAIL) 와 무관하게 **즉시 진입 가능 상태** 보장. HANDOFF turn 7 의 의사결정 부담 감소.

---

## 11. Day 19 turn 8 — #019 PASS (fallback C+ 효과 검증 완료, env 부재 환경 라이브 재검증)

> 본 turn 미션 = HANDOFF turn 7 §🚨 Quick Start §5~7 = `.todo.md` #019 = **fallback C+ 효과 라이브 재검증** (Phase 1 진입 최종 사전조건). turn 7 (#018 PASS) 에서 강제 훅 신설 + settings.json env 영구 제거 commit 완료, 메인 재시작 후 env 부재 환경 진입 = 본 turn 8.

### 11-1. HANDOFF turn 7 인수 + 사전 확인 (Quick Start)

- HANDOFF turn 7 (양식 v2 dogfood 3건째) Read → Quick Start §1~4 자동 검증
- **§1 PASS**: PowerShell `Get-ChildItem Env:` → `CLAUDE_CODE_SUBAGENT_MODEL` **부재** (변수 자체 미존재) → 메인 재시작 + settings.json env 제거 효과 발효 결정적 확인 (잔존 변수 = `CLAUDE_CODE_ENTRYPOINT`, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`, `CLAUDE_CODE_SSE_PORT`, `CLAUDECODE=1` — 본 검증과 무관)
- **§2 PASS**: HANDOFF.md → `.backups/HANDOFF.done.2026-05-04-v3.md` 정상 소멸 (소멸 정책 **13회차 검증**)
- **§3 PASS**: `.todo.md` Read → #019 priority high (1순위) 확인
- **§4 PASS**: `.checklist.md` 부재 (turn 7 종료 시 정리 완료 상태) — 본 turn 신규 작성 진입 가능

### 11-2. /checklist 작성 + 사용자 승인

- mode = mixed (라이브 검증 + 결과 분기 시 문서·운영 변경 가능성 모두 포함)
- 작업 항목: Step 1 (4 spawn 라이브 검증) → Step 2 (분기 판정) → Step 3-A/B (결과별 산출물) → Step 4 (history) → Step 5 (commit) → Step 6 (정리)
- **C spawn 만 의도적 model 누락** (강제 훅 검증 = 글로벌 규칙 §3 "검증 실험" 예외 적용)
- 사용자 승인 즉시 → `approved: true` + `status: approved` 마킹 후 Phase 2 진입

### 11-3. Step 1 — 4 spawn 라이브 검증 (TeamCreate `model-fallback-verify-v3`)

| Test | spawn 명시 model | 자기보고 모델 ID | 예상 | 결과 |
|---|---|---|---|---|
| A | `opus` | `claude-opus-4-7` | Opus | ✅ **PASS** |
| B | `sonnet` | `claude-sonnet-4-6` | Sonnet | ✅ **PASS** |
| C | (누락) | — | 강제 훅 차단 | ✅ **PASS** (`permissionDecision: deny` + exit 0 우회 라이브 재현) |
| D | `haiku` | `claude-haiku-4-5-20251001` | Haiku | ✅ **PASS** (3종 valid model 모두 spawn 가능 검증) |

> **D 변경 사유**: 원래 체크리스트 D=invalid (`gpt-5`) 로 SDK 차단 검증 의도였으나 SDK enum 이 invalid 값을 거부 = spawn 호출 자체 불가 (turn 7 §11.3 D 패턴 재확인 = 이중 보장 자체는 turn 7 으로 완료). 본 turn 에서 D 를 valid 한 세 번째 model (haiku) 로 대체 → "model 명시가 정확히 해당 model 을 선택하는지" 검증 (3종 valid model `opus|sonnet|haiku` 모두 작동 확인 + 4단 비용 배분 가능성).

### 11-4. Step 2 — PASS 판정 + 핵심 발견

**PASS 조건 4/4 정합** = A=Opus AND B=Sonnet AND C=차단 AND D=Haiku → fallback C+ **효과 검증 완료**.

**3중 메커니즘 全 작동 결정적 확인**:
1. **강제 훅** (Test C 차단) — turn 7 PASS 의 `permissionDecision: deny` + exit 0 우회 라이브 재현 + rules section 2/3 인용 메시지 정확 (`[BLOCKED] Agent spawn requires model parameter. ... Per ~/.claude/rules/agent-spawn-model.md section 2: ...`)
2. **env 제거 + 메인 재시작** (Test A·B·D 모두 명시 model 그대로) — turn 7 의 env 덮어쓰기 (A=opus → Sonnet) 가 env=sonnet 환경 한정임을 결정적 재현
3. **3종 valid model 모두 spawn 가능** — D 변경으로 새로 검증 → 4단 비용 배분 (PM=Opus / 리뷰어=Opus / 워커=Sonnet / 트리비얼 워커=Haiku) 가능성 확인

### 11-5. turn 7 anomaly 정리 (env=sonnet 환경 한정 결정적 재확인)

turn 4 (env=sonnet, A=opus → Sonnet) + turn 7 (env=sonnet, A=opus → Sonnet) 일치 vs turn 6 (env=빈 값, A=opus → Opus) + 본 turn 8 (env=빈 값, A=opus → Opus) 일치 = 해석:

- **env=sonnet 환경**: env 가 명시 model 덮어씀 (turn 4·7 결정적 재현)
- **env=빈 값 환경**: 명시 model 그대로 작동 (turn 6·8 결정적 재현)
- **turn 6 anomaly 가설 기각** — turn 7 §11.4 의 "메인 재시작 직후 cache miss" 가설은 본 turn 8 으로 정합 결과 결정적 재현 = turn 6 결과 정합 확인

→ **issue#32732 의 본질 = env 가 1순위, 명시 model 은 env unset 시에만 작동**. 외부 출처 ([Issue #26923](https://github.com/anthropics/claude-code/issues/26923) + [#40580](https://github.com/anthropics/claude-code/issues/40580)) 의 "Task hook + exit 2" 통념과 다름 = "Agent matcher + permissionDecision" 이 본 환경 (claude-code 2.1.126) 의 정확한 우회 패턴.

### 11-6. Step 3-A — 산출물 (PASS 경로)

- ✅ `docs/research/agent-office-masterplan/06_issue32732_experiment.md §12` 신설 (~85줄, 517→602)
- ✅ `docs/research/agent-office-masterplan/04_masterplan.md §8.2` 5차 실험 박스 추가 (~12줄) + 헤더 "현 상태" 갱신 + §9.1 model override 행 갱신 (효과 검증 완료 마킹)
- ✅ `~/.claude/projects/.../memory/agent-office-vision.md` L115 정정 ("fallback C+ 영구 적용 + 효과 검증 완료" + "issue#32732 종결, Phase 1 진입 가능 최종 마킹")
- ✅ `.todo.md` #019 완료 + 백로그에서 제거

### 11-7. Step 4·5·6 — 정리

- `docs/history/2026-05-04.md` Day 19 turn 8 섹션 (본 §11) 추가 + 헤더 갱신
- `docs/history/index.md` Day 19 행 갱신 (turn 8 결과 반영)
- commit 단일 (메모리 `feedback_commit_push.md` 한 단위 처리)
- `.checklist.md` → `.backups/.checklist.md.완료_019-fallback-cplus-verify_2026-05-04.md` 이동

### 11-8. 결정 (D-10) — fallback C+ 영구 적용 최종 확정

| 메커니즘 | 본 turn 검증 결과 | 상태 |
|---|---|---|
| 1. settings.json env `CLAUDE_CODE_SUBAGENT_MODEL` 영구 제거 | ✅ **효과 검증 PASS** (§11-3 의 A·B·D 정합) | **확정** |
| 2. 메인 Claude Code 재시작 (process env cache 갱신) | ✅ **본 turn 진입 시 충족** | **확정** |
| 3. 모든 Agent spawn 에 `model` 파라미터 강제 명시 | ✅ **라이브 차단 검증** (Test C) | **확정** |

**Phase 1 진입 가능 — 최종 마킹**: turn 6 의 잠정 마킹 + turn 7 의 조건부 마킹 → 본 turn 8 으로 **무조건 마킹** 전환. **issue#32732 종결**.

### 11-9. 부수 발견 (R-7) — 3종 valid model 동시 작동 확인

Anthropic Claude Code SDK `model` 파라미터 enum = `opus|sonnet|haiku` 3종 모두 spawn 가능. 마스터플랜 §3.2 (D-4) 의 2단 배분 (PM=Opus / 워커=Sonnet) 외에 4단 배분 가능성 제시:
- **PM·리뷰어** = Opus (비판자 역할)
- **일반 워커** = Sonnet ("+90.2% 성능" 패턴 핵심)
- **트리비얼 워커** = Haiku (단순 조회·분류·포맷팅, 비용 ≪ Sonnet)

→ 다음 세션 (Phase 1 진입) 시 마스터플랜 §3.2 비용 효과 재검토 가치. 단 Phase 1 미션은 #009 (agent-team-manager v2 본체) → 4단 배분은 후속 단계로 분리.

### 11-10. SendMessage 회신 의무 안정성 검증 (R-8)

turn 6 §6-1 교훈 = "spawn 텍스트 출력만으로는 lead 미수신, SendMessage 강제 의무" → 본 turn 4 spawn 모두 SendMessage 회신 도착 (Test A·B·D, Test C 는 spawn 자체 차단으로 무관) → 의무 패턴 결정적 안정 → Phase 1 PM-워커 통신 신뢰 가능.

### 11-11. 영구 기록 가치 평가 (Phase 6 의무)

다음 5 조건 中 **4 해당** → `/project-history update` (본 파일) 의무 실행:
- ✅ 새로운 결정 사항 발생 (D-10 + R-7 + R-8)
- ❌ 외부 검수 / 외부 리서치 결과 반영 (본 turn 외부 리서치 미수행 — turn 7 의 Issue 인용 그대로 차용)
- ✅ 마이그레이션 / 신설 스킬 / 신설 규칙 (없음, 단 issue#32732 종결 마킹 = 운영 환경 영구 변경 효과 검증)
- ✅ dogfood 데이터 누적 (양식 v2 turn 8 = 4건 누적, /checklist mode=mixed 두 번째)
- ✅ 회고 가치 있는 패턴 발견 — fallback C+ 효과 검증 PASS 결정적 확인 (turn 6·8 일치 vs turn 4·7 일치 패턴 = 외부 자료 가능성)

---

## 12. Day 19 turn 9 — #014 PASS (PM 외부 리서치 의무화 + agents 스테이징 도입)

> 본 turn 미션 = HANDOFF turn 7 미결 결정 1 기울기 (B → A 순차) = `.todo.md` #014 (priority normal, unblocked: turn 6 #015 PASS) + 사용자 본 turn 명시 ("제대로 한 번에") = **agents 스테이징/운영 분리 정책 정상화 + pm-test PM 강화 + 마스터플랜 보강 통합**. Phase 1 (#009) 진입 사전 정리 1/2.

### 12-1. 사용자 합의 (큰 그림 4단계)

본 turn 진입 시점 = turn 8 commit `f252915` push 완료 후 working tree clean. 사용자 합의 흐름:

1. **"이제 문제 다 해결됐나?"** → 사전 조건 1건만 (model 호출 보장) PASS, PM·직책 agent 미신설 = Phase 0 끝, Phase 1 진입 가능
2. **"PM 등장 + 협의 + 4가지 워커 中 선택 흐름 맞나?"** → 비전 5층 위계 + 4갈래 워커 구상 정확히 일치 확인 (단 협의는 PM ↔ 사장 사이, 사용자는 컨펌)
3. **"진입 순서?"** → #014 → #012 → #009 순차 합의 (HANDOFF turn 7 미결 결정 1 기울기 적용)
4. **"글로벌 단위?"** → 부수 발견: agents/ 스테이징 폴더 부재 → 사용자 명시 "제대로 한 번에" = agents 스테이징 도입 통합 합의

### 12-2. /checklist 작성 + 사용자 승인

- mode = mixed (글로벌 운영 + 프로젝트 문서 + 동기화 정책)
- 작업 항목: Step 1 (agents 스테이징 신설) → Step 2 (pm-test 강화) → Step 3 (sync) → Step 4 (마스터플랜 보강) → Step 5 (정리) → Step 6 (commit) → Step 7 (.checklist.md 정리)
- 사용자 승인 즉시 → `approved: true` + `status: approved` 마킹 후 Phase 2 진입

### 12-3. Step 1 — agents 스테이징 폴더 신설 + 일회성 역방향 복사

**부수 발견 정상화**: skills/rules 는 스테이징↔운영 분리 잡혀 있었으나 agents/ 는 부재 → 본 turn 신설:

| 영역 | 경로 | 상태 |
|------|------|------|
| 스테이징 (편집 대상) | `Harness-engineering/agents/` | **신설** |
| 운영 (자동 로드) | `~/.claude/agents/` | 기존 (5 파일: election-specialist·persona-designer·pm-test·policy-analyst·polling-expert) |

- `mkdir -p Harness-engineering/agents/` + `cp ~/.claude/agents/pm-test.md → 스테이징` (Day 16 skills 일회성 역방향 sync 패턴 차용 — 정상화 목적 일회성 예외)
- SHA256 MATCH PASS = `273E9F184872B6C0D46802D7DC1CC04DA906BA4690C392714266DC8C7B638C4C` (양측 일치)

### 12-4. Step 2 — 스테이징 `pm-test.md` 강화 (외부 리서치 의무 + 출력 형식 4 요소)

기존 41줄 (issue#32732 자기보고 임시 agent) → 강화 후 ~70줄 (PM 부장 정식 역할 + 외부 리서치 의무 + 4 요소 + 자기보고 deprecated 마킹).

**주요 추가 항목**:

1. **`description` 갱신** — "역할 = PM 부장, 외부 리서치 + 출처 인용 의무" 명시 + "#009 정식 PM 신설 시 archive 예정"
2. **핵심 행동 규칙 1~5번** — 마스터플랜 §2.3 의 4번까지 + **5번 외부 리서치 의무 신설**:
   - WebSearch / WebFetch / 외부 CLI 1순위 + 보조
   - 글로벌 `~/.claude/rules/research-mandatory.md` superset + PM 한정 강도
3. **출력 형식 강제 4 요소**:
   - ① 결론 (1~2줄)
   - ② 출처 (URL + 발행일 + 직접 인용 1~2줄, paraphrase 금지)
   - ③ 추측 표현 금지 (`아마`·`보통`·`일반적으로`·`대체로`·`경험상`)
   - ④ 자기비판 1줄 (반박 가능성 0건이면 "반박 후보 없음" 명시)
4. **외부 리서치 면제 예외** — 코드/파일/git/내부 메모리/환경변수는 Read·Grep·Glob·git 으로 충분 (글로벌 §4 차용)
5. **Haiku 사용 제외 명시** — `feedback_no_haiku.md` 메모리 차용 (자기보고 시 Opus / Sonnet / 불확정 中 선택, Haiku 운영 제외)

### 12-5. Step 3 — 정방향 sync (스테이징 → 운영) + SHA256 MATCH

- `Copy-Item -LiteralPath "$HOME\OneDrive\문서\Harness-engineering\agents\pm-test.md" -Destination "$HOME\.claude\agents\pm-test.md" -Force`
- SHA256 MATCH PASS = `C6EFD48C19FA64A7C7E52549B8E5158600951AD38062AE9A048A798322E05CBB` (양측 일치)

### 12-6. Step 4 — 마스터플랜 04 보강

**§2.3 PM α 옵션 system prompt** 의 핵심 행동 규칙 4번 다음에 **5번 신규 추가** (~10줄):
- 외부 리서치 의무 (WebSearch / WebFetch / 외부 CLI 1순위 → 보조)
- 4 요소 강제 (결론·출처·추측 금지·자기비판)
- 면제 예외 명시
- 글로벌 superset 참조

**§9.1 운영 가드레일 표** 의 model override 행 다음에 **PM 외부 리서치 + 출처 인용 강제 행 신규 추가**:
- 가드레일 본문: PM agent system prompt 의 외부 리서치 의무 + 4 요소 박음
- 출처: 글로벌 `rules/research-mandatory.md` + #014 PASS

### 12-7. Step 5·6·7 — 정리

- `.todo.md` #014 백로그 제거 + 완료 섹션 추가
- `docs/history/2026-05-04.md` Day 19 turn 9 섹션 (본 §12) 추가 + 헤더 갱신 (#014 PASS 추가)
- `docs/history/index.md` Day 19 행 갱신
- 단일 commit (메모리 `feedback_commit_push.md` 한 단위)
- `.checklist.md` → `.backups/.checklist.md.완료_014-pm-research-mandatory_2026-05-04.md` 이동

### 12-8. 결정 (D-11) — agents 스테이징/운영 분리 정책 채택

| 영역 | 정책 | 일관성 |
|------|------|--------|
| skills | 스테이징 = `Harness-engineering/skills/` / 운영 = `~/.claude/skills/` / 단방향 sync | 기존 |
| rules | 스테이징 = `Harness-engineering/rules/` / 운영 = `~/.claude/rules/` / 단방향 sync | 기존 |
| **agents** | **스테이징 = `Harness-engineering/agents/` / 운영 = `~/.claude/agents/` / 단방향 sync** | **본 turn 신설 (#014 부수 효과)** |

**Why**: 사용자 명시 "제대로 한 번에" + skills/rules 와 일관성 (drift 방지) + #009 정식 PM agent 본체 신설 시 스테이징 위치 미리 정상화 = 본 공사 입력 매끄러움.

**How to apply**:
- 새 agent 신설 시 = `Harness-engineering/agents/<agent-name>.md` 작성 후 운영 sync
- 기존 운영 agent 수정 시 = 1회 역방향 복사 (정상화) → 스테이징 편집 → 정방향 sync (반복)
- drift 발견 시 = 프로젝트 (스테이징) = 정답

### 12-9. 결정 (D-12) — pm-test 임시 PM 강화 = 정식 PM 입력 자료

본 turn 강화한 pm-test 의 system prompt 7 항목 (행동 규칙 1~5번 + 출력 형식 4 요소 + 면제 예외) = **#009 정식 PM agent 신설 시 그대로 흡수**. 임시 → 정식 전환 시 매끄러운 마이그레이션 보장.

### 12-10. 영구 기록 가치 평가 (Phase 6 의무)

다음 5 조건 中 **3 해당** → `/project-history update` (본 파일) 의무 실행:
- ✅ 새로운 결정 사항 발생 (D-11 agents 스테이징 정책 + D-12 pm-test 강화 = 정식 PM 입력)
- ❌ 외부 검수 / 외부 리서치 결과 반영 (본 turn 외부 리서치 미수행 — 글로벌 `rules/research-mandatory.md` §6 의 4 출처 인용 그대로 차용)
- ✅ 마이그레이션 / 신설 스킬 / 신설 규칙 (agents/ 스테이징 폴더 신설 = 정책 정상화 + 마이그레이션)
- ✅ dogfood 데이터 누적 (양식 v2 turn 9 = 5건 누적, /checklist mode=mixed 세 번째)
- ❌ 회고 가치 있는 패턴 발견 (없음, 단순 정상화 + 강화)

### 12-11. 다음 작업

**Phase 1 사전 정리**:
- [ ] **#012** 출처 보강 (Anthropic 블로그 URL + aws-samples 리포 SHA) — Phase 1 진입 전 의무, 외부 리서치 (WebSearch + WebFetch) 활용
- [ ] **#009** Phase 1 본 공사 (agent-team-manager v2 본체 = scripts/ 6개 + presets/ 5 YAML + reference/ 4개 + 직책별 정식 agent) — #012 PASS 후 진입
