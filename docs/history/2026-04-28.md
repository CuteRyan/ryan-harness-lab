# Day 14 — 2026-04-28 — /feedback 스킬 B방식 (프롬프트 파일화) + 메인 Claude 5게이트

## 1. 작업 개요

Day 13에 시작한 A2 (영어 prompt + few-shot) 검증을 마무리하고 **B 방식**(프롬프트 파일 분리)으로 재설계 + **메인 Claude 종합 게이트** 강제 장치 도입. 주인님 핀트 재정렬에서 출발한 작업.

- **AM 작업**: A2 검증 완료 (영어 전환 효과 미미: 38% → 40%) + NativeCommandError fix
- **PM 작업**: 핀트 재정렬 → B 방식 1단계 + 2단계 + 종합 보고 검증

## 2. AM — A2 마무리 (영어 prompt 검증 미달)

### NativeCommandError fix (인프라 블로커 해소)
- **발견**: G1 1차(2026-04-27) 실패 추적 — Codex/Gemini retry 모두 catch 발동
- **진짜 원인**: PS 5.1 NativeCommandError + `$ErrorActionPreference='Stop'` 조합. CLI stderr 정보 메시지가 RemoteException으로 wrap → terminating error 승격 → try 블록 catch 추락
- **재현 매트릭스 실측**:
  - 부모 세션 직접 호출: ✅ 정상 (7.4s, PONG)
  - Start-Job + EAP 미설정: ✅ 정상 (6.7s, PONG)
  - Start-Job + EAP='Stop' + try/catch: ❌ catch 발동 (FullyQualifiedErrorId: NativeCommandError)
- **수정**: `run-codex.ps1` + `run-gemini.ps1` 의 native CLI 호출 직전·직후만 EAP를 'Continue'로 격리. `$LASTEXITCODE` 검사는 EAP 무영향이라 그대로 작동
- 검증: Start-Job + EAP='Stop' + try/catch 재현 테스트 → catch 발동 안 함, "TRY-OK exit=0 outputLen=4 PONG"

### A2 영어 전환 효과 검증
| 케이스 | 1차 (한국어) | 2차 (영어) | 차이 |
|---|---|---|---|
| G1 (한국어 SKILL.md) | 20% | 15% | -5%p |
| G2 (한국어 sycophancy 산출물) | 20% | 29% | +9%p |
| G3 (영어 Python 코드) | 75% | 75% | 0%p |
| **평균** | **38%** | **40%** | **+2%p** |

→ 영어 전환 효과 미미. Gemini 한국어 recall 약점은 모델 본성 (영어 prompt로 안 풀림). 오히려 Claude Sub이 G2'에서 `**[CRITICAL]**` 볼드 형식으로 INVALID 회귀 발생 → 부작용.

→ A2 (영어 전환) **롤백 결정**.

## 3. PM — 핀트 재정렬 (주인님 발의)

주인님 요지: "스크립트가 문제가 아니라, 프롬프트를 PowerShell argv로 직접 전달하는 구조 자체가 문제. **프롬프트 파일을 만들어넣고 CLI가 그걸 읽게** 하는 식으로 가자."

### B 방식 = "우편함" 비유
- A 방식 = PowerShell이 prompt 파일 읽어서 argv로 옮겨 말함 (지금 방식과 본질 동일)
- **B 방식** = prompt 파일 자체를 격리 디렉토리에 복사 → CLI가 자기 read 도구로 직접 읽음 (PowerShell이 옮겨 말할 일 0)

### AI 본성 인정 (메인 Claude 게이트 필요성 도출)
주인님 지적: "너의 피드백을 겉핥기 식으로 안 되게 해야 한다."

→ AI 솔직 답변: "감정상 귀찮은 게 아니라 **확률 분포 + RLHF 부작용**으로 sycophancy 회귀가 통계적으로 발생. 양심이 아니라 시스템 게이트로 강제해야 함."

## 4. B 방식 1단계 — 인프라 (8 항목 A~H)

### 변경 파일 (스테이징)
- 신규: `skills/feedback/prompts/review.md` (한국어 비판 prompt SSOT)
- 신규: `skills/feedback/scripts/gemini-system.md` (한/영 혼용 critic 모드, A2에서 신설된 것 유지)
- 수정: `skills/feedback/scripts/prepare-isolation.ps1` — 격리 디렉토리에 review.md도 복사
- 수정: `skills/feedback/scripts/orchestrate.ps1` — 긴 promptLines 제거 → 짧은 메타 지시("이 폴더의 review.md 읽고 따르라")
- 수정: `skills/feedback/scripts/validate-outputs.ps1` — 한국어 태그 정규식 복구
- 수정: `skills/feedback/SKILL.md` — SSOT 위치 변경 명시 (`prompts/review.md`)

### 운영 동기화 SHA256
6/6 MATCH (review.md, orchestrate.ps1, prepare-isolation.ps1, validate-outputs.ps1, gemini-system.md, SKILL.md).

### H 실측 (g3_sample.py로 dogfood)
| 항목 | 결과 |
|---|---|
| Elapsed | 133.7s |
| PowerShell exit | 0 |
| 격리 디렉토리 | review.md (3312 bytes) + g3_sample.py (659 bytes) 둘 다 복사 ✅ |
| 비판 형식 준수 | **3/3 CLI** (1차/2차에선 항상 1~2/3) |
| Validation Gate | **3/3 VALID** (이전엔 항상 1~2/3) |
| 비율 (Codex+Claude 합집합 8건 중 Gemini 일치) | 5/8 = 62.5% |

→ **B 방식의 진짜 효과**: PowerShell argv 인코딩 위험 0 + Codex/Claude의 마크다운 볼드 형식 INVALID 회귀 자동 해소 (review.md 예시를 보고 따름).

## 5. B 방식 2단계 — 메인 Claude 5게이트 (5 항목 A~E)

### SKILL.md Step 3 5게이트 박음
1. **라인 실측 검증** — 환각 차단. 인용 라인 ≥ 원본 라인 수면 자동 [반박] 환각
2. **반박/유보 최소 1건** — 모두 [반영]이면 자기 검열 의무
3. **근거 강도 표시** — 강(코드 인용) / 중(원리) / 약(추상). 약 ≥ 50%면 헤더 경고
4. **통계 표 강제** — 첫 블록에 전체 N / [반영] N / [유보] N / [반박] N / [환각] N
5. **자기 비판 한 줄** — 마지막 블록에 "이번 종합에서 놓쳤을 가능성 1가지" 강제. "없음" 답변 금지

### E 검증 — H 결과로 종합 보고 작성
파일: `docs/feedback/2026-04-28_claude_g3_sample_20260428-161902-종합.md`

5게이트 발현 자체 검증 결과 **이전 1차 보고에서 빠뜨렸던 것 2가지가 자동 노출**:
1. **Gemini 라인 정밀도**: 5건 중 4건이 ±1~2 인접 라인 부정확. 환각은 아니지만 정밀도 약점. 1차 보고에선 매크로 비율(75%)만 봤지 정밀도까진 안 짚었음
2. **시스템 설계 차원 사각지대**: 동시성·트랜잭션·테스트 격리·로깅 부재. 3 CLI 모두 능동 탐색 안 함. 게이트 5가 없었으면 이번에도 통과시켰을 것

→ **게이트가 실제로 sycophancy 회귀를 막아준다**는 게 1회 실측으로 확인.

## 6. 의사결정

- **A2 영어 전환 롤백**: 효과 미미(38% → 40%) + 부작용(Claude Sub G2' INVALID 회귀). Why: 모델 본성을 prompt 언어로 못 풀음
- **B 방식 채택**: 프롬프트 파일화의 진짜 가치 = 인코딩 위험 0 + 형식 준수 100% (실측 입증). Why: PowerShell argv 길이·인코딩 한계 회피 + prompt 수정 시 코드 무수정
- **5게이트 명시 (의지 X 시스템 O)**: AI는 RLHF·확률 분포 영향으로 sycophancy 회귀 위험 상존. 게이트 형식 채워야 끝나는 작업으로 분포 강제 이동. Why: 주인님 지적 "너의 피드백 겉핥기 안 되게" 직접 대응
- **3단계(정보 제공형 훅) 별도 세션 분리**: 셸 선택·sycophancy 키워드 사전 등 별도 디자인 필요. 오늘 무리 안 함

## 7. 사이드 관찰

- **메인 Claude의 자기 비판 능력 검증**: 게이트 5 발현으로 "동시성 사각지대 능동 탐색 부재"를 스스로 명시. 게이트 없는 1차 보고에선 안 적었던 것
- **Codex 포맷 일관성**: review.md 예시 형식(`[치명] 파일:줄 - 근거: X`)을 그대로 따름. 영어 모드 시 `**Critical:**` 볼드 형식 회귀가 1단계로 자동 해소
- **메인 Claude(저)도 LLM 동일 본성 인정**: "감정상 귀찮은 게 아니라 확률 분포". 솔직성·자기비판 능력이 게이트 작동의 전제

## 8. 변경 파일 (이번 commit 범위)

### 스테이징 (Harness-engineering/)
- 신규:
  - `skills/feedback/prompts/review.md`
  - `skills/feedback/scripts/gemini-system.md`
  - `skills/feedback/scripts/g3_sample.py` (dogfood testbed, 보존)
  - `docs/feedback/2026-04-2[78]_*` (다수, 1차/2차/H 결과 + 종합)
  - `docs/research/2026-04-27_gemini_sycophancy.md`
  - `docs/research/2026-04-27_gpt55_codex.md`
  - `docs/history/2026-04-28.md` (이 파일)
- 수정:
  - `skills/feedback/SKILL.md` (1단계 SSOT 위치 + 2단계 5게이트)
  - `skills/feedback/scripts/orchestrate.ps1` (1단계 promptLines 변경)
  - `skills/feedback/scripts/prepare-isolation.ps1` (1단계 review.md 복사)
  - `skills/feedback/scripts/validate-outputs.ps1` (영어 전환 롤백)
  - `skills/feedback/scripts/run-codex.ps1` (NativeCommandError fix)
  - `skills/feedback/scripts/run-gemini.ps1` (NativeCommandError fix)
  - `docs/history/index.md` (Day 14 entry + 진행 중 섹션 갱신)
- 백업:
  - `.backups/.checklist.md.종결_A2_영어전환미달_2026-04-28.md` (1차 A2 체크리스트)
  - `.backups/.checklist.md.완료_B방식1단계_2026-04-28.md`
  - `.backups/.checklist.md.완료_B방식2단계_2026-04-28.md` (이 commit 시점)

### 운영 동기화 (~/.claude/skills/feedback/)
- 6 파일 SHA256 MATCH (1단계) + 1 파일 MATCH (2단계 SKILL.md)

## 9. PM 후속 — 3단계 훅 신설 (외부 검증 레이어)

게이트 1~5 가 모델 self-discipline 이라면, 3단계 훅 = **외부 정규식·grep 기반 검수** 레이어. 두 짝 구조 = 내부 의무(SKILL.md) + 외부 표시(훅).

### 9.1 결정사항 (주인님 합의 5건)

| # | 결정 |
|---|---|
| 1 | 두 목적 모두 검수 (보고서 품질 + 비판적 자세) |
| 2 | 7개 카테고리 (sycophancy / 환각 / 누락 / 1차 sycophancy 전이 / 약한 반박 / 1차 충돌 / 약한 비판) |
| 3 | 외부 장치(훅) + 내부 의무(SKILL.md 후속 검수) |
| 4 | 키워드 23개 외부 파일(`hooks/data/sycophancy-keywords.txt`) — 추가/삭제 시 훅 코드 무수정 |
| 5 | 출력 B안 — 콘솔 직접 카테고리별 표시, 항상 `exit 0`, 검출 0건이면 침묵 |

### 9.2 신규/수정 파일

| 파일 | 종류 | 비고 |
|---|---|---|
| `hooks/feedback-sycophancy-check.sh` | 신규 | bash thin wrapper, stdin JSON 파싱 → python 본체 호출 |
| `hooks/feedback-sycophancy-check.py` | 신규 | 7개 카테고리 검출 본체. UTF-8 stdout reconfigure 포함 (Windows CP949 회피) |
| `hooks/data/sycophancy-keywords.txt` | 신규 | 시드 22개 (한국어 15 + 영어 7) |
| `~/.claude/settings.json` | 수정 | PostToolUse `Edit`/`Write`/`MultiEdit` 3 matcher에 등록. 백업 `.backups/settings.json.before-3단계훅_2026-04-28.json` |
| `skills/feedback/SKILL.md` | 수정 | Step 3 에 "게이트 6 — 외부 훅 검수 의무" 신설 + 종합 파일 골격에 `## (게이트 6 발동 시) 외부 훅 검수 결과` 섹션 추가 |

### 9.3 dogfood 6 케이스 결과

| # | 케이스 | 기대 | 실측 | 판정 |
|---|---|---|---|---|
| C1 | 정상 본문 (0건 기대) | 침묵 | 1차 1건(valid) → "valid" 시드 제거 후 0건 | ✓ (즉시 보강) |
| C2 | sycophancy 키워드 5개 | ≥5건 검출 | 9건 검출 | ✓ |
| C3 | 환각 (라인 99999 + 미존재 파일) | 2건 검출 | 2건 검출 (미존재 + 실제 340줄) | ✓ |
| C4 | 1차 [치명]/[높음] + 종합에 없음 | 2건 검출 | 2건 검출 | ✓ |
| C5 | 차단 안 됨 (exit 0) | 모든 케이스 exit 0 | 모든 케이스 exit 0 | ✓ |
| C6 | 실파일 (Day 14 g3_sample 종합) | 동작만 OK | 22건(1차) → 17건(valid 제거 후) | ✓ |

### 9.4 즉시 보강 1건 — `valid` 시드 제거

C1 dogfood 결과 정상 본문에서도 1건 검출. 원인: 통계 표 마크 `VALID CLI: N/3` 의 "VALID" 가 시드 `valid` 와 substring 매칭. **명백한 false positive** → 즉시 시드에서 제거.

한국어 `유효` 는 보존 (시스템 마크가 영어이므로 충돌 없음). 시드 23 → 22 개로 1건 축소.

### 9.5 self-discipline (게이트 5 정신) — 솔직 한계

- **환각 false positive**: 격리 디렉토리에 있다가 정리된 파일을 종합이 인용 → 훅이 "미존재"로 판정. 알고리즘 한계 (격리 디렉토리 추적 미구현). 메인 Claude 가 종합 검수 시 [오탐] 처리하는 워크플로로 우회
- **누락/충돌 정밀도 낮음**: LLM 호출 없이 키워드·정규식 매칭으로는 한계. 1차 [치명]이 종합에서 다른 단어로 처리됐을 때 false positive 유발
- **시드 추가 보강 필요**: dogfood 1회 만으로 시드 적정성 단정 불가. 향후 실사용에서 sycophancy 누락 발견 시 단어 추가 / false positive 재발 시 단어 제거. 키워드 사전이 외부 파일이라 훅 코드 무수정

### 9.6 SHA256 운영 동기화 검증 (4 파일 MATCH)

```
sh:        9cfda433a42551630f438f88f0b23f62e617a0cfa58536845f92abe004c9f7fb
py:        14dc5c655fa7fff670e9fc1fbae3906004674634c020cd98f733cc4a8cdb9a3c
keywords:  e38d259e454641a6fc3748806c0c6678100d2299ba3a9ddd239c3bcbabc5a598
SKILL.md:  938c1ca151b144add99f2fcaead4b0d9f1ebcad04775440cd13ec75f0700359c
```

(settings.json 은 운영 직접 수정, 백업본 `.backups/settings.json.before-3단계훅_2026-04-28.json` 보관)

### 9.7 의사결정 (Why)

- **bash thin wrapper + python 본체**: 7개 카테고리 검출 로직이 bash 단독으로는 가독성 ↓. python 으로 단위별 함수 분리. 다른 훅(`code-doc-sync.sh`)이 이미 python 인라인 사용 중이라 의존성 일관성. **Why bash 래퍼 유지**: settings.json 등록·`run-hook.ps1` 인프라 그대로 활용
- **운영 직접 수정한 settings.json**: ~/.claude/ 가 운영 = SSOT. 스테이징 카피본을 만들 이유 없음. 백업 보관으로 롤백 보장
- **차단형 X / 표시형 O 고수**: PreToolUse 차단 가능성 검토 → 거부. Why: /feedback 워크플로가 자동화돼 있어 종합 보고 저장이 차단되면 메인 Claude 진행 멈춤. PostToolUse 표시형이면 메인 Claude 가 같은 턴에 자기 보고서 검수 시작 가능 (sycophancy 회귀 즉시 보강)

## 10. 다음 작업

- [x] **3단계 — 정보 제공형 훅** (Day 14 종결, 위 §9)
- [ ] (보류) agent-team-manager v2 §9 의사결정 3건 (U1 충돌 / preset 5개 / YAML 전환)
- [ ] (보류) Knowledge-platform CLAUDE.md 축약 141→80줄 (Day 11 이월)
- [ ] /todo 스킬 신설 (Day 9 보류, 본 작업 종결 후 별도)
- [ ] (관찰) 3단계 훅 false positive 비율 — 1주 사용 후 키워드 사전·휴리스틱 보강 결정

---

## 종결 메모 (2026-04-30, Day 16)

본 일자의 종결 항목 (/feedback B 방식 1·2·3단계) 을 진행 중 섹션에서 정리 (옵션 C 적용). 5-5 1주 회고는 단일 세션 작업이 아니라 예약된 후속 → `.todo.md` #004 로 이전 (priority: normal, due: 2026-05-05).

§10 다음 작업의 미결 항목 (agent-team-manager v2, Knowledge-platform CLAUDE.md 축약, /todo 스킬 신설) 도 `.todo.md` 로 이전 — `/todo` 스킬은 본 turn (4-30) 에 신설되어 자체 처리.
